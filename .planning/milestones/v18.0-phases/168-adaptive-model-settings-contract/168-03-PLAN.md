---
phase: 168-adaptive-model-settings-contract
plan: 03
type: execute
wave: 2
depends_on:
  - 168-02
files_modified:
  - src/lib/helpers.js
  - src/lib/decision-rules.js
  - src/plugin/command-enricher.js
  - src/commands/init.js
  - src/commands/misc.js
  - tests/decisions.test.cjs
  - tests/enricher-decisions.test.cjs
  - tests/integration.test.cjs
autonomous: true
requirements:
  - MODEL-02
  - MODEL-03
outcome_ids:
  - DO-117
user_setup: []
must_haves:
  truths:
    - "Phase 168 runtime model selection resolves concrete agent models from the canonical config contract instead of static Anthropic tier tables or SQLite-first lookups on the touched paths"
    - "Project default profile resolution and sparse per-agent overrides behave the same in helper functions, decision rules, init output, and command diagnostics"
    - "Workflow-facing model state stays provider-agnostic: resolved outputs no longer present `opus` / `sonnet` / `haiku` as the canonical public contract"
  artifacts:
    - path: "src/lib/helpers.js"
      provides: "Shared runtime resolver entrypoint for agent model selection"
      contains: "canonical config contract, project default profile, agent override precedence"
    - path: "src/lib/decision-rules.js"
      provides: "Decision-rule parity for model-selection"
      contains: "canonical resolver path"
    - path: "src/commands/init.js"
      provides: "Workflow injection of resolved model values"
      contains: "planner_model, executor_model, verifier_model"
    - path: "src/plugin/command-enricher.js"
      provides: "Enriched decision inputs built from canonical model settings"
      contains: "agent_type, selected profile, resolved model"
  key_links:
    - from: "src/lib/helpers.js"
      to: "src/lib/decision-rules.js"
      via: "shared model resolution semantics"
      pattern: "default profile|agent override|resolveModel"
    - from: "src/plugin/command-enricher.js"
      to: "src/commands/init.js"
      via: "bgsd-context model decision injection"
      pattern: "model-selection|planner_model|executor_model|verifier_model"
    - from: "src/commands/misc.js"
      to: "src/lib/helpers.js"
      via: "command-surface resolve-model parity"
      pattern: "cmdResolveModel|resolveModel"
---

<objective>
Make the touched Phase 168 runtime paths resolve models from the new settings contract in exactly one way.

Purpose: after Plan 02 defines the contract, Phase 168 still fails if helpers, decision rules, init injection, and diagnostics keep separate fallback logic. This plan removes that drift on the current touched paths so changing the selected profile or an agent override changes real behavior without adding a broader routing redesign.
Output: one shared resolution path used by helpers, decision rules, init/enricher output, and command-level diagnostics touched by this phase.
</objective>

> **TDD Decision:** Selected - resolver precedence and injected outputs are deterministic and should be locked by regression tests before refactoring runtime call sites.

<execution_context>
@.planning/phases/168-adaptive-model-settings-contract/168-CONTEXT.md
@.planning/phases/168-adaptive-model-settings-contract/168-RESEARCH.md
@.planning/phases/168-adaptive-model-settings-contract/168-02-PLAN.md
</execution_context>

<context>
@src/lib/helpers.js
@src/lib/decision-rules.js
@src/plugin/command-enricher.js
@src/commands/init.js
@src/commands/misc.js
</context>

<tasks>

<task type="auto">
  <name>Centralize runtime model resolution on the canonical contract</name>
  <files>
src/lib/helpers.js
src/lib/decision-rules.js
  </files>
  <action>
Introduce or extract one shared resolver path that takes the canonical model-settings contract and returns the concrete model for a given agent.

Refactor helper and decision-rule code so they both honor the same precedence order:
1. sparse direct agent override
2. selected project default profile
3. built-in shipped profile definitions

Remove static `opus` / `sonnet` / `haiku` tables and SQLite-first lookup from the canonical runtime path for the touched Phase 168 surfaces. If host/runtime compatibility still needs a special internal translation such as `inherit`, keep it as a final transport concern rather than the public contract itself. Do not widen this into a bigger routing redesign that belongs in Phase 169.
  </action>
  <verify>npm run test:file -- tests/decisions.test.cjs tests/enricher-decisions.test.cjs tests/integration.test.cjs</verify>
  <done>Helper and decision-rule code resolve models through one shared canonical path</done>
</task>

<task type="auto">
  <name>Propagate canonical resolution into init, enrichment, and diagnostics</name>
  <files>
src/plugin/command-enricher.js
src/commands/init.js
src/commands/misc.js
tests/decisions.test.cjs
tests/enricher-decisions.test.cjs
tests/integration.test.cjs
  </files>
  <action>
Update the remaining model-facing command surfaces so they consume the same shared resolver and present provider-agnostic state.

Cover at least these surfaces:
- `command-enricher` decision inputs and output fields used by workflows
- `init` injection of resolved `*_model` values for spawned agents
- `misc` resolve-model command output

Keep outputs consistent with Phase 169 boundaries: show the selected profile and concrete model where this phase already surfaces model state, but do not expand into new visibility features beyond the paths already touched.
  </action>
  <verify>npm run test:file -- tests/decisions.test.cjs tests/enricher-decisions.test.cjs tests/integration.test.cjs</verify>
  <done>Init, enrichment, and diagnostics all reflect the same project-default-first model-resolution behavior</done>
</task>

</tasks>

<verification>
- Run: npm run test:file -- tests/decisions.test.cjs tests/enricher-decisions.test.cjs tests/integration.test.cjs
- Run: npm run build
</verification>

<success_criteria>
- [ ] Runtime model selection no longer depends on duplicated legacy fallback tables
- [ ] Resolver precedence is consistent across helpers, decision rules, and diagnostics
- [ ] Init and enriched workflow context use the canonical model-settings contract
- [ ] Regression tests cover override precedence and default-profile resolution
</success_criteria>

<output>
After completion, create `.planning/phases/168-adaptive-model-settings-contract/168-03-SUMMARY.md`
</output>
