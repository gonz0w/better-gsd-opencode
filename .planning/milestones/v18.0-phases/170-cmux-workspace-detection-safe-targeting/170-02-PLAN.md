---
phase: 170-cmux-workspace-detection-safe-targeting
plan: 02
type: tdd
wave: 2
depends_on:
  - 170-01
files_modified:
  - src/plugin/cmux-targeting.js
  - tests/plugin-cmux-targeting.test.cjs
autonomous: true
requirements:
  - CMUX-01
  - CMUX-07
  - CMUX-08
outcome_ids:
  - DO-121
user_setup: []
must_haves:
  truths:
    - "Managed-terminal sessions attach only when CMUX_WORKSPACE_ID and CMUX_SURFACE_ID match cmux identify output"
    - "Alongside sessions attach only when access mode permits them and exactly one workspace matches the project cwd"
    - "Conflicting or ambiguous workspace evidence suppresses attachment instead of falling back to heuristics"
  artifacts:
    - path: "src/plugin/cmux-targeting.js"
      provides: "Exact managed-terminal-first workspace proof with conservative alongside fallback"
      contains: "resolveManagedWorkspaceTarget, resolveAlongsideWorkspaceTarget, access-mode-blocked, ambiguous-cwd"
    - path: "tests/plugin-cmux-targeting.test.cjs"
      provides: "Regression coverage for env proof, cwd proof, and ambiguity suppression"
      contains: "workspace-mismatch, surface-mismatch, unique cwd match, ambiguous-cwd"
  key_links:
    - from: "src/plugin/cmux-targeting.js"
      to: "src/plugin/cmux-cli.js"
      via: "managed proof and alongside proof consuming identify, capabilities, list-workspaces, and sidebar-state"
      pattern: "identify|capabilities|listWorkspaces|sidebarState"
    - from: "tests/plugin-cmux-targeting.test.cjs"
      to: "src/plugin/cmux-targeting.js"
      via: "exact-match regressions proving env wins and ambiguity suppresses"
      pattern: "workspace-mismatch|surface-mismatch|ambiguous-cwd|access-mode-blocked"
---

<objective>
Prove exactly one target workspace or suppress cmux attachment entirely.

Purpose: Phase 170 succeeds only if it prevents cross-workspace leakage. This plan implements the core proof rules from context and research: managed-terminal env first, exact cwd fallback only when access mode allows it, and suppression whenever evidence is conflicting or non-unique.
Output: exact workspace targeting logic that either returns one proven workspace id or a specific suppression reason.
</objective>

> **TDD Decision:** Selected - env matching, access-mode handling, cwd uniqueness, and ambiguity suppression are deterministic attachment rules with clear pass/fail expectations.

<feature>
  <name>Managed-terminal-first exact workspace proof</name>
  <files>
    src/plugin/cmux-targeting.js
    tests/plugin-cmux-targeting.test.cjs
  </files>
  <tdd-targets>
    <red>npm run test:file -- tests/plugin-cmux-targeting.test.cjs</red>
    <green>npm run test:file -- tests/plugin-cmux-targeting.test.cjs</green>
    <refactor>npm run test:file -- tests/plugin-cmux-targeting.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    Managed terminals attach only when env ids and `identify --json` agree, while compatibility-only alongside sessions attach only when `allowAll` access and an exact unique cwd match prove the target.
    Cases: matching managed ids -> target proven; env mismatch -> suppressed; one exact cwd match in alongside mode -> target proven; zero or multiple cwd matches -> suppressed; conflicting managed evidence never falls back to heuristics in the same session.
  </behavior>
  <implementation>
    Extend the targeting layer from Plan 01 with exact proof helpers for managed-terminal and alongside modes. Keep the fallback conservative and exact-match-only, reusing normalized cwd semantics where needed but refusing project-name, git-branch, or partial-path guesses.
  </implementation>
</feature>

<execution_context>
@.planning/phases/170-cmux-workspace-detection-safe-targeting/170-CONTEXT.md
@.planning/phases/170-cmux-workspace-detection-safe-targeting/170-RESEARCH.md
@.planning/phases/170-cmux-workspace-detection-safe-targeting/170-01-PLAN.md
</execution_context>

<context>
@src/plugin/cmux-targeting.js
@src/commands/workspace.js
@tests/workspace.test.cjs
@tests/plugin-cmux-targeting.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Add regressions for exact workspace proof and ambiguity suppression</name>
  <files>tests/plugin-cmux-targeting.test.cjs</files>
  <action>
Add regressions that prove the targeting contract for the cases most likely to violate `CMUX-07` and `CMUX-08`.

Cover at least these cases: matching managed env ids, managed workspace mismatch, managed surface mismatch, alongside success with one exact cwd match, zero cwd matches, multiple cwd matches, access mode blocking a non-managed caller, and a conflicting managed proof that must not drop into heuristics.
  </action>
  <verify>rg -n "workspace-mismatch|surface-mismatch|ambiguous-cwd|access-mode-blocked" tests/plugin-cmux-targeting.test.cjs</verify>
  <done>The targeting test file names the exact mismatch, ambiguity, and access-mode suppression cases before implementation fills them in</done>
</task>

<task type="auto">
  <name>Implement managed-terminal proof and conservative alongside fallback</name>
  <files>src/plugin/cmux-targeting.js</files>
  <action>
Implement a managed-terminal proof chain that requires `CMUX_WORKSPACE_ID`, `CMUX_SURFACE_ID`, successful `ping`, usable `capabilities`, and `identify --json` agreement before a workspace is considered proven.

Add compatibility-only alongside proof that runs only when managed proof is unavailable, access mode allows a non-managed caller, and one exact normalized cwd match can be established via `sidebar-state --workspace`. Suppress for zero matches, multiple matches, access-mode blocks, or any managed-proof conflict. Do not fall back from conflicting env evidence to cwd heuristics.
  </action>
  <verify>rg -n "resolveManagedWorkspaceTarget|resolveAlongsideWorkspaceTarget|ambiguous-cwd|access-mode-blocked" src/plugin/cmux-targeting.js</verify>
  <done>`src/plugin/cmux-targeting.js` can return exactly one proven workspace id or a machine-readable suppression reason, with no best-guess fallback</done>
</task>

</tasks>

<verification>
- Run: npm run test:file -- tests/plugin-cmux-targeting.test.cjs
</verification>

<success_criteria>
- [ ] Managed-terminal proof requires env plus `identify --json` agreement
- [ ] Alongside attachment only works when access mode permits it and cwd matching proves exactly one target
- [ ] Ambiguous or conflicting workspace evidence suppresses attachment
- [ ] Regression tests cover zero/one/many workspace candidates and access-mode blocks
</success_criteria>

<output>
After completion, create `.planning/phases/170-cmux-workspace-detection-safe-targeting/170-02-SUMMARY.md`
</output>
