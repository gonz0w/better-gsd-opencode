---
phase: 169-canonical-model-resolution-visibility
plan: 04
type: execute
wave: 3
depends_on:
  - 169-01
  - 169-03
files_modified:
  - src/lib/planning-cache.js
  - src/plugin/lib/db-cache.js
  - src/lib/db.js
  - tests/session-state.test.cjs
  - tests/infra.test.cjs
autonomous: true
requirements:
  - MODEL-04
outcome_ids:
  - DO-117
user_setup: []
must_haves:
  truths:
    - "No live cache or SQLite helper path can become an alternate truth source for current model selection after canonical resolver and routing changes land"
    - "Legacy provider-shaped model-profile helpers are either removed or clearly fenced as non-live compatibility debt"
    - "Regression coverage preserves any remaining schema or migration compatibility without letting old provider-tier APIs back onto the active path"
  artifacts:
    - path: "src/lib/planning-cache.js"
      provides: "Planning-cache model-profile APIs removed or fenced away from live model resolution"
      contains: "no live model profile truth path"
    - path: "src/plugin/lib/db-cache.js"
      provides: "Plugin cache model-profile helpers removed or marked compatibility-only"
      contains: "no current-model read path"
    - path: "src/lib/db.js"
      provides: "SQLite compatibility boundary that no longer seeds or advertises provider-tier model selection as live behavior"
      contains: "compatibility-only model_profiles handling or explicit dead-path boundary"
  key_links:
    - from: "src/lib/planning-cache.js"
      to: "src/lib/db.js"
      via: "legacy model-profile persistence boundary no longer used for live selection"
      pattern: "model_profiles|getModelProfile|getModelProfiles"
    - from: "src/plugin/lib/db-cache.js"
      to: "src/lib/db.js"
      via: "plugin-side cache helpers fenced away from canonical model resolution"
      pattern: "model_profiles|quality_model|balanced_model|budget_model"
---

<objective>
Remove or fence the remaining legacy model-profile persistence surfaces so canonical config stays the only live truth source.

Purpose: once Plans 01 and 03 make helper output and routing canonical, leaving old provider-shaped cache APIs around creates unnecessary risk that a future read path will resurrect them. This plan trims or fences those legacy APIs with evidence-backed cleanup while preserving any schema or migration compatibility the repo still needs.
Output: legacy model-profile cache and SQLite surfaces that are no longer usable as active model-selection sources, plus regression coverage for the retained compatibility boundary.
</objective>

> **TDD Decision:** Skipped - this slice is primarily dead-surface cleanup and compatibility fencing rather than introducing a new behavior contract best driven feature-first.

<execution_context>
@.planning/phases/169-canonical-model-resolution-visibility/169-CONTEXT.md
@.planning/phases/169-canonical-model-resolution-visibility/169-RESEARCH.md
@.planning/phases/169-canonical-model-resolution-visibility/169-01-PLAN.md
@.planning/phases/169-canonical-model-resolution-visibility/169-03-PLAN.md
</execution_context>

<context>
@src/lib/planning-cache.js
@src/plugin/lib/db-cache.js
@src/lib/db.js
@tests/session-state.test.cjs
@tests/infra.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Remove or fence dead model-profile cache APIs</name>
  <files>
src/lib/planning-cache.js
src/plugin/lib/db-cache.js
  </files>
  <action>
Use the no-live-caller evidence from research to remove legacy model-profile read helpers where safe, or fence them behind explicit compatibility-only boundaries if full deletion would create avoidable churn.

Focus on ensuring no active planning or plugin cache surface can be mistaken for the current source of truth for agent model selection. Keep the cleanup narrow: do not introduce new storage or migration behavior, and do not revive provider-tier defaults anywhere on the live path.
  </action>
  <verify>rg -n "getModelProfile|getModelProfiles|storeModelProfile|seedModelDefaults|model_profiles" src/lib/planning-cache.js src/plugin/lib/db-cache.js</verify>
  <done>Planning and plugin cache layers can no longer serve legacy model-profile reads as a live model-selection API</done>
</task>

<task type="auto">
  <name>Preserve only the SQLite compatibility boundary needed after cleanup</name>
  <files>
src/lib/db.js
tests/session-state.test.cjs
tests/infra.test.cjs
  </files>
  <action>
Review the remaining `model_profiles` schema and migration handling in `src/lib/db.js`, then keep only the minimum compatibility behavior still justified by tests.

If the schema must remain for migration or historical state compatibility, make that boundary explicit and ensure the tests prove it is not a live resolution path. If parts of the schema or seed logic are unused after the cache cleanup, remove them. The end state must make canonical config the only active model-selection truth source.
  </action>
  <verify>npm run test:file -- tests/session-state.test.cjs tests/infra.test.cjs</verify>
  <done>SQLite compatibility remains only where justified by tests, without leaving provider-tier model selection available as live behavior</done>
</task>

</tasks>

<verification>
- Run: npm run build
</verification>

<success_criteria>
- [ ] Legacy planning/plugin cache model-profile APIs are removed or fenced from live use
- [ ] Any retained SQLite `model_profiles` support is compatibility-only, not active truth
- [ ] Regression tests protect the retained compatibility boundary
- [ ] Canonical config and resolver remain the only live source of model selection after cleanup
</success_criteria>

<output>
After completion, create `.planning/phases/169-canonical-model-resolution-visibility/169-04-SUMMARY.md`
</output>
