---
phase: 169-canonical-model-resolution-visibility
plan: 02
type: tdd
wave: 2
depends_on:
  - 169-01
files_modified:
  - src/commands/init.js
  - tests/init.test.cjs
autonomous: true
requirements:
  - MODEL-04
  - MODEL-05
outcome_ids:
  - DO-117
user_setup: []
must_haves:
  truths:
    - "Compact init output shows the active configured choice together with the resolved concrete model instead of hiding one side of the story"
    - "Verbose init output expands model-state visibility per relevant agent so overrides and default-profile resolution are obvious"
    - "Every init path that exposes model state reads from the shared presenter instead of hand-formatting separate model fields"
  artifacts:
    - path: "src/commands/init.js"
      provides: "Canonical configured-versus-resolved model-state reporting across init surfaces"
      contains: "compact summary, verbose per-agent details, configured, resolved_model"
    - path: "tests/init.test.cjs"
      provides: "Regression coverage for compact and verbose init model-state output"
      contains: "configured, resolved_model, override/default-profile cases"
  key_links:
    - from: "src/commands/init.js"
      to: "src/lib/helpers.js"
      via: "shared model-state presenter powering init output"
      pattern: "configured|selected_profile|resolved_model|source"
    - from: "tests/init.test.cjs"
      to: "src/commands/init.js"
      via: "CLI output assertions for compact and verbose init paths"
      pattern: "init:execute-phase|--verbose|configured|resolved"
---

<objective>
Finish visibility parity for init, the most user-visible model-state surface in this phase.

Purpose: users cannot trust model settings end-to-end if init still shows only concrete model ids or drops model state entirely in compact output. This plan applies the shared presenter from Plan 01 to every relevant init surface so users can see both configured intent and runtime resolution without leaving the canonical workflows.
Output: compact and verbose init output that consistently shows configured-versus-resolved model state for the active workflow path.
</objective>

> **TDD Decision:** Selected - init output is deterministic CLI behavior with clear expected compact and verbose cases that should be captured in regression tests first.

<feature>
  <name>Init configured-versus-resolved model visibility</name>
  <files>
    src/commands/init.js
    tests/init.test.cjs
  </files>
  <tdd-targets>
    <red>npm run test:file -- tests/init.test.cjs</red>
    <green>npm run test:file -- tests/init.test.cjs</green>
    <refactor>npm run test:file -- tests/init.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    Compact init output keeps one concise configured-plus-resolved summary, while verbose init output shows per-agent configured source and resolved model details.
    Cases: default profile -> compact summary plus verbose per-agent detail; direct override -> explicit override source plus resolved model; touched init paths stop emitting concrete-model-only state.
  </behavior>
  <implementation>
    Reuse the shared presenter from Plan 01 across the init entrypoints that currently show model state, then lock the compact and verbose output shape in regression tests.
  </implementation>
</feature>

<execution_context>
@.planning/phases/169-canonical-model-resolution-visibility/169-CONTEXT.md
@.planning/phases/169-canonical-model-resolution-visibility/169-RESEARCH.md
@.planning/phases/169-canonical-model-resolution-visibility/169-01-PLAN.md
</execution_context>

<context>
@src/commands/init.js
@tests/init.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Apply configured-versus-resolved model-state reporting across init surfaces</name>
  <files>src/commands/init.js</files>
  <action>
Refactor the init command paths that currently emit bare `*_model` values or omit model-state visibility so they all consume the shared presenter introduced in Plan 01.

Cover the compact and verbose variants used by the planning/execution workflow entrypoints touched in research, including `execute-phase`, `plan-phase`, `quick`, and progress-oriented init output that exposes model state today. Keep compact mode concise, but always show the active configured choice together with the resolved model. Keep verbose mode explicit about whether the value came from a direct override or the selected default profile.
  </action>
  <verify>rg -n "configured|resolved_model|selected_profile|source" src/commands/init.js</verify>
  <done>All relevant init outputs present configured and resolved model state through one shared formatting path</done>
</task>

<task type="auto">
  <name>Add init regressions for compact and verbose model visibility</name>
  <files>tests/init.test.cjs</files>
  <action>
Add or update init regression coverage so compact and verbose output both prove the new visibility contract.

Include cases for:
- default-profile resolution
- direct agent override resolution
- compact output keeping only the active concise summary
- verbose output expanding the per-agent detail users need to audit model selection

Make the assertions literal enough that a future regression cannot silently collapse back to concrete-model-only output.
  </action>
  <verify>npm run test:file -- tests/init.test.cjs</verify>
  <done>Init tests fail if any touched workflow path hides configured state or drops resolved-model visibility</done>
</task>

</tasks>

<verification>
- Run: npm run build
</verification>

<success_criteria>
- [ ] Compact init output shows the active configured choice plus resolved model
- [ ] Verbose init output makes override versus default-profile sourcing explicit
- [ ] `src/commands/init.js` no longer hand-formats separate legacy model fields on touched surfaces
- [ ] Regression tests cover compact and verbose visibility parity
</success_criteria>

<output>
After completion, create `.planning/phases/169-canonical-model-resolution-visibility/169-02-SUMMARY.md`
</output>
