---
phase: 181-workspace-root-truth-safe-fallback
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - workflows/execute-phase.md
  - workflows/execute-plan.md
  - tests/integration.test.cjs
  - tests/workflow.test.cjs
autonomous: true
requirements:
  - JJ-01
  - JJ-03
intent:
  outcome_ids: []
  rationale: "Effective intent exposes the Phase 181 focus but no explicit DO-* IDs, so this plan traces directly to the same phase objective: require proof before workspace work begins and keep repo-relative execution contained to the assigned workspace when proof succeeds."
must_haves:
  truths:
    - "Workspace-mode execution runs the proof gate before any plan work, summary creation, or repo-relative artifact write begins."
    - "When proof or workspace availability fails, the execution contract downgrades to the supported sequential path before plan work starts and tells the operator why."
    - "While workspace mode is active, the execution guidance keeps repo-relative reads, writes, and plan-local outputs rooted inside the assigned workspace instead of drifting back to the main checkout."
  artifacts:
    - path: "workflows/execute-phase.md"
      provides: "Workspace execution preflight and fallback contract"
      contains: "workspace prove {plan_id}"
    - path: "workflows/execute-plan.md"
      provides: "Executor guidance for workspace-rooted plan-local outputs"
      contains: "SUMMARY.md"
    - path: "tests/integration.test.cjs"
      provides: "Workflow contract regression coverage for workspace proof and fallback wording"
      contains: "workspace prove"
  key_links:
    - from: "workflows/execute-phase.md"
      to: "workflows/execute-plan.md"
      via: "Workspace-mode preflight must prove root pinning before delegating execution or writing plan-local artifacts"
      pattern: "workspace prove \{plan_id\}|fall back to sequential|before any plan work begins"
    - from: "tests/integration.test.cjs"
      to: "workflows/execute-phase.md"
      via: "Workflow regressions lock the proof-first and generic fallback contract so later edits do not drift back to advisory-only workspace targeting"
      pattern: "workspace prove|fallback to sequential|workspace root"
---

<objective>
Wire the proof gate into the workspace execution workflow so Phase 181 changes real behavior: workspace mode must prove root pinning before any work starts, and plan-local outputs stay rooted inside the assigned workspace while that mode is active.

Purpose: A proof helper alone does not protect operators if the execution workflow can still start work, write artifacts, or silently continue in workspace mode before using it. This slice makes the proof contract actionable.
Output: updated `execute-phase` and `execute-plan` workflow instructions plus regression tests that lock proof-first fallback behavior and workspace-rooted artifact guidance.
</objective>

> **TDD Decision:** Skipped — this slice is workflow-contract and runtime-guidance hardening rather than one isolated behavior function, so the lighter execute template is the right fit even though it still carries targeted regression checks.

<execution_context>
@__OPENCODE_CONFIG__/bgsd-oc/workflows/execute-plan.md
@__OPENCODE_CONFIG__/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/181-workspace-root-truth-safe-fallback/181-CONTEXT.md
@.planning/phases/181-workspace-root-truth-safe-fallback/181-RESEARCH.md
@src/commands/init.js
@workflows/execute-phase.md
@workflows/execute-plan.md
@tests/integration.test.cjs
@tests/workflow.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make workspace-mode execution prove root pinning before any plan work begins</name>
  <files>
    workflows/execute-phase.md
    tests/integration.test.cjs
    tests/workflow.test.cjs
  </files>
  <action>
  Update `workflows/execute-phase.md` so Mode A explicitly runs the new `workspace prove {plan_id}` preflight after workspace creation and before executor plan work starts.

  Keep the locked contract visible and explicit: if proof succeeds, the workflow can continue with workspace-parallel execution; if proof fails or workspace mode is unavailable, the workflow must downgrade to Mode B sequential execution before summaries, plan-local outputs, or other repo-relative work begin. Operator-facing guidance should name the intended workspace, observed executor cwd, observed `jj workspace root`, and one generic fallback reason instead of inventing multiple user-visible failure branches. Add or update integration/workflow regressions that lock those phrases in the shipped contract.
  </action>
  <verify>node --test tests/integration.test.cjs</verify>
  <done>The execution workflow is proof-first and explicitly downgrades to sequential before work when workspace pinning cannot be proven.</done>
</task>

<task type="auto">
  <name>Task 2: Keep workspace-mode repo-relative outputs rooted inside the assigned workspace</name>
  <files>
    workflows/execute-phase.md
    workflows/execute-plan.md
    tests/integration.test.cjs
    tests/workflow.test.cjs
  </files>
  <action>
  Tighten the executor guidance so repo-relative reads, writes, and plan-local outputs stay rooted in the assigned workspace while workspace mode is active.

  Update `workflows/execute-plan.md` to state that the executor's current repo root is the assigned workspace root in workspace mode, that plan-local artifacts such as `SUMMARY.md` and proof sidecars are created only after proof status is known, and that those artifacts remain inside the workspace checkout rather than drifting back to the main checkout. Keep the guidance phase-local: do not reopen shared `.planning/` finalize ownership or later verification-routing policy. Extend the workflow regressions so these containment callouts remain part of the shipped contract.
  </action>
  <verify>node --test tests/workflow.test.cjs</verify>
  <done>The shipped execution guidance makes workspace-rooted containment explicit for plan-local outputs and keeps that wording locked with regression tests.</done>
</task>

</tasks>

<verification>
```bash
node --test tests/integration.test.cjs tests/workflow.test.cjs
```
</verification>

<success_criteria>
- [ ] Workspace mode now requires a proof-first preflight and downgrades to supported sequential execution before work when proof or availability fails
- [ ] The execution contract explicitly keeps workspace-mode repo-relative reads, writes, and plan-local outputs rooted inside the assigned workspace
- [ ] Regression tests lock the proof gate, generic fallback wording, and containment guidance in the shipped workflows
</success_criteria>

<output>
After completion, create `.planning/phases/181-workspace-root-truth-safe-fallback/181-02-SUMMARY.md`
</output>
