---
phase: 181-workspace-root-truth-safe-fallback
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/jj-workspace.js
  - src/commands/workspace.js
  - src/router.js
  - src/lib/constants.js
  - tests/workspace.test.cjs
autonomous: true
requirements:
  - JJ-01
  - JJ-03
intent:
  outcome_ids: []
  rationale: "No explicit DO-* IDs are present in effective_intent for Phase 181, so this plan traces directly to the locked phase focus: prove the assigned JJ workspace root from runtime evidence and return one generic safe-fallback result when proof is missing."
must_haves:
  truths:
    - "A workspace-targeted executor can collect intended-root, observed cwd, and observed `jj workspace root` evidence before work starts and only report `parallel_allowed` when all three canonical paths match."
    - "Subdirectory starts, root mismatches, missing JJ workspace truth, or other proof failures all return one generic fallback-to-sequential result with explicit evidence instead of silently proceeding as workspace-parallel."
  artifacts:
    - path: "src/lib/jj-workspace.js"
      provides: "Reusable workspace proof collection and fallback classification"
      contains: "parallel_allowed"
    - path: "src/commands/workspace.js"
      provides: "Operator-facing workspace proof command"
      contains: "workspace prove"
    - path: "tests/workspace.test.cjs"
      provides: "Regression coverage for proof success and generic fallback cases"
      contains: "workspace prove"
  key_links:
    - from: "src/commands/workspace.js"
      to: "src/lib/jj-workspace.js"
      via: "Workspace command delegates proof gathering to the shared helper instead of duplicating JJ root logic"
      pattern: "prove|parallel_allowed|fallback_reason"
    - from: "tests/workspace.test.cjs"
      to: "src/commands/workspace.js"
      via: "Focused CLI regressions lock the triple-match proof contract and generic fallback payload"
      pattern: "workspace prove|parallel_allowed|fallback_reason|jj workspace root"
---

<objective>
Add the reusable runtime proof gate first by teaching the workspace command family to verify the intended workspace root, the executor cwd realpath, and `jj workspace root` before workspace mode is trusted.

Purpose: Phase 181 fails if workspace targeting is still advisory. This slice makes the trust contract executable and reusable before execution workflows depend on it.
Output: a shared proof helper in `src/lib/jj-workspace.js`, a surfaced `workspace prove` command, routed help text, and focused regressions that lock the success and fallback cases.
</objective>

> **TDD Decision:** Selected — the proof gate is deterministic runtime behavior with clear pass/fail outcomes, so RED tests should lock the triple-match unlock rule and the generic fallback contract before the helper and command path are changed.

<execution_context>
@__OPENCODE_CONFIG__/bgsd-oc/workflows/execute-plan.md
@__OPENCODE_CONFIG__/bgsd-oc/templates/summary.md
@__OPENCODE_CONFIG__/bgsd-oc/skills/tdd-execution/SKILL.md
@__OPENCODE_CONFIG__/bgsd-oc/skills/tdd-execution/tdd-reference.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/181-workspace-root-truth-safe-fallback/181-CONTEXT.md
@.planning/phases/181-workspace-root-truth-safe-fallback/181-RESEARCH.md
@src/lib/jj-workspace.js
@src/commands/workspace.js
@src/router.js
@src/lib/constants.js
@tests/workspace.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED coverage for the triple-match unlock rule and generic fallback cases</name>
  <files>
    tests/workspace.test.cjs
  </files>
  <action>
  Extend `tests/workspace.test.cjs` with focused failing regressions for the Phase 181 proof contract before touching the implementation.

  Cover the exact locked cases from context and research: a successful proof when the intended workspace root, observed executor cwd realpath, and observed `jj workspace root` all match; a subdirectory start that downgrades because cwd is inside the workspace but not root-pinned; and a generic fallback result when authoritative workspace proof cannot be established. Keep the assertions on the surfaced payload narrow and stable: the response should expose intended root, observed cwd, observed JJ root, `parallel_allowed`, and one fallback reason path instead of branching into multiple user-visible failure taxonomies.
  </action>
  <verify>node --test tests/workspace.test.cjs</verify>
  <done>The workspace test suite fails on the current code because the triple-match proof gate and generic fallback payload are not yet implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the shared proof helper and surface it through `workspace prove`</name>
  <files>
    src/lib/jj-workspace.js
    src/commands/workspace.js
    src/router.js
    src/lib/constants.js
    tests/workspace.test.cjs
  </files>
  <action>
  Implement the proof gate in the existing JJ workspace surfaces instead of inventing a second registry or workspace abstraction.

  Add a reusable helper in `src/lib/jj-workspace.js` that resolves the intended workspace root canonically, captures the observed executor cwd realpath plus `jj workspace root`, and returns one structured result shape that either unlocks workspace-parallel execution or reports a generic fallback-to-sequential reason with evidence. Then expose that helper through a routed `workspace prove` command in `src/commands/workspace.js`, wire the new subcommand in `src/router.js`, and add help text in `src/lib/constants.js`. Preserve the locked operator-facing evidence fields from context, and keep all mismatch or availability problems converged to one safe fallback classification.
  </action>
  <verify>npm run build</verify>
  <done>The repo ships one reusable proof helper and `workspace prove` surfaces the exact triple-match contract plus one generic fallback result without weakening the sequential fallback path.</done>
</task>

</tasks>

<feature>
  <name>Runtime workspace proof gate for managed JJ execution</name>
  <files>
    src/lib/jj-workspace.js
    src/commands/workspace.js
    src/router.js
    src/lib/constants.js
    tests/workspace.test.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/workspace.test.cjs</red>
    <green>npm run build && node --test tests/workspace.test.cjs</green>
    <refactor>node --test tests/workspace.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    Workspace mode is trusted only after runtime evidence proves the executor is pinned to the intended JJ workspace root.

    Cases:
      - running `workspace prove` from the assigned workspace root returns the intended root, observed cwd, observed `jj workspace root`, and `parallel_allowed: true`
      - starting from a child directory in the right workspace reports `parallel_allowed: false` and a generic fallback-to-sequential reason rather than claiming full proof
      - missing or failed authoritative root lookup also reports the same generic fallback outcome while preserving the evidence fields operators need to inspect
  </behavior>
  <implementation>
    Reuse the current JJ workspace command family and helper module. Strengthen them with canonical realpath comparison and one structured proof payload rather than introducing a new workspace registry, a prompt-only proof contract, or multiple user-visible failure classes.
  </implementation>
</feature>

<verification>
```bash
npm run build && node --test tests/workspace.test.cjs
```
</verification>

<success_criteria>
- [ ] `workspace prove` only unlocks workspace-parallel execution when intended root, observed cwd realpath, and observed `jj workspace root` all match
- [ ] Proof mismatch, subdirectory start, or missing workspace truth returns one generic fallback-to-sequential result with explicit evidence instead of proceeding in workspace mode
- [ ] The proof contract is locked by focused regressions in `tests/workspace.test.cjs`
</success_criteria>

<output>
After completion, create `.planning/phases/181-workspace-root-truth-safe-fallback/181-01-SUMMARY.md`
</output>
