---
phase: 185-cmux-coordination-backbone
plan: 02
type: tdd
verification_route: full
wave: 2
depends_on:
  - 185-01
files_modified:
  - src/plugin/index.js
  - tests/plugin.test.cjs
autonomous: true
requirements:
  - CMUX-01
  - CMUX-04
user_setup: []
must_haves:
  truths:
    - "All plugin hook sources enqueue the same refresh backbone instead of calling sidebar and attention refresh paths separately"
    - "Plugin startup still performs one immediate refresh, while later bursts coalesce through the shared backbone"
    - "When cmux is unavailable, unreachable, or untrusted, refresh work stays quiet under bounded suppression and can wake early on planning-file changes"
  artifacts:
    - path: "src/plugin/index.js"
      provides: "Plugin wiring that routes startup and later runtime events through one coordinated refresh backbone"
      contains: "createCmuxRefreshBackbone, handleExternalCmuxPlanningChange, tool.execute.after"
    - path: "tests/plugin.test.cjs"
      provides: "Plugin integration proof for coordinated refresh, bounded suppression, and planning-change wake-up"
      contains: "Plugin cmux coordinated refresh"
  key_links:
    - from: "src/plugin/index.js"
      to: "src/plugin/cmux-refresh-backbone.js"
      via: "startup refresh and later hook enqueue calls using one shared coordinator"
      pattern: "createCmuxRefreshBackbone|enqueue|refreshNow"
    - from: "src/plugin/index.js"
      to: "src/plugin/cmux-targeting.js"
      via: "bounded retry and early wake using the existing quiet fail-open attachment contract"
      pattern: "allowRetry|suppressionReason|getCurrentCmuxAdapter"
    - from: "tests/plugin.test.cjs"
      to: "src/plugin/index.js"
      via: "integration regressions proving burst coalescing, quiet suppression, and planning-file wake-up"
      pattern: "Plugin cmux coordinated refresh|file.watcher.updated|command.executed|tool.execute.after"
---

<objective>
Wire every relevant plugin hook into the shared `cmux` backbone while preserving quiet fail-open recovery semantics.

Purpose: the backbone only matters if startup, watcher, idle, command, and tool hooks stop refreshing `cmux` independently. This plan makes the plugin route all later refresh work through one shared cycle, keeps a single immediate startup refresh, and adds bounded retry plus planning-file wake-up behavior so suppressed `cmux` stays quiet without getting stuck until restart.
Output: plugin hook wiring and integration tests proving coordinated refresh, quiet suppression, and bounded recovery.
</objective>

> **TDD Decision:** Selected — shared-hook coordination, bounded suppression, and early-wake recovery are deterministic integration behaviors with clear pass/fail outcomes.

<feature>
  <name>Plugin hook wiring for coordinated cmux refresh</name>
  <files>
    src/plugin/index.js
    tests/plugin.test.cjs
  </files>
  <tdd-targets>
    <red>node --test --test-force-exit --test-name-pattern "Plugin cmux coordinated refresh" tests/plugin.test.cjs</red>
    <green>node --test --test-force-exit --test-name-pattern "Plugin cmux coordinated refresh" tests/plugin.test.cjs</green>
    <refactor>node --test --test-force-exit --test-name-pattern "Plugin cmux coordinated refresh" tests/plugin.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    Startup performs one immediate shared refresh, then file-watcher, idle, command, and tool events enqueue the same debounced backbone instead of refreshing sidebar and attention independently.
    Cases: bursty hook activity coalesces into one shared cycle; suppressed cmux stays quiet during bounded backoff instead of retrying every trigger; a planning-file change can break suppression early and request a fresh availability check through the same backbone.
  </behavior>
  <implementation>
    Replace the plugin's separate `refreshCmuxSidebar()` and `refreshCmuxAttention()` hook fan-out with a single coordinator instance from Plan 01. Preserve the existing attached-or-suppressed adapter boundary, sequential fallback, and visible sidebar/attention surfaces. If you give urgent attention any special timing treatment, keep it inside the same snapshot/cycle rather than introducing a second parser or adapter read path.
  </implementation>
</feature>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/185-cmux-coordination-backbone/185-CONTEXT.md
@.planning/phases/185-cmux-coordination-backbone/185-RESEARCH.md
@.planning/phases/185-cmux-coordination-backbone/185-01-PLAN.md
@src/plugin/index.js
@src/plugin/file-watcher.js
@src/plugin/cmux-targeting.js
@tests/plugin.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Add plugin-level regressions for coordinated refresh and bounded suppression wake-up</name>
  <files>tests/plugin.test.cjs</files>
  <action>
    Add focused integration coverage under a dedicated `Plugin cmux coordinated refresh` test name pattern. Prove that startup performs one immediate shared cycle, later watcher/command/tool bursts enqueue the same backbone instead of duplicating sidebar and attention refreshes, suppressed adapters stay quiet during bounded backoff, and a planning-file change can wake suppression early for one shared re-check.

    Keep the assertions phase-scoped: verify coordination and quiet recovery, not the fuller lifecycle/status vocabulary that belongs to Phase 186.
  </action>
  <verify>rg -n "Plugin cmux coordinated refresh|planning-file change|bounded suppression|shared cycle" tests/plugin.test.cjs</verify>
  <done>`tests/plugin.test.cjs` contains integration regressions that pin the shared-cycle and fail-open recovery contract for Phase 185</done>
</task>

<task type="auto">
  <name>Replace per-hook direct refreshes with one shared backbone in the plugin</name>
  <files>src/plugin/index.js</files>
  <action>
    Instantiate the Plan 01 backbone in `BgsdPlugin`, keep one immediate startup refresh, and route later `file.watcher.updated`, external planning-file change, `session.idle`, `command.executed`, and `tool.execute.after` activity through `enqueue()` on the shared coordinator. Ensure one cycle performs one parser invalidation plus one fresh runtime snapshot, then fans out to sidebar and attention.

    Preserve quiet fail-open behavior by keeping retry/bounded wake logic centralized, reusing the existing cmux targeting contract, and avoiding direct sidebar/attention refresh calls from hooks. Do not add dependencies, do not remove sequential fallback, and do not expand scope into general multi-user coordination.
  </action>
  <verify>node --test --test-force-exit --test-name-pattern "Plugin cmux coordinated refresh" tests/plugin.test.cjs</verify>
  <done>The plugin routes all later cmux refresh activity through one shared backbone, stays quiet when cmux is suppressed, and supports bounded early wake-up on planning-file changes</done>
</task>

</tasks>

<verification>
- Run: npm run build
</verification>

<success_criteria>
- [ ] Startup performs one immediate shared refresh, and later bursts use one coordinated backbone instead of separate sidebar and attention refresh paths
- [ ] All relevant hooks enqueue the same backbone without reintroducing duplicate parser invalidation or project-state reads
- [ ] Suppressed or untrusted cmux stays quiet under bounded retry rather than probing on every trigger
- [ ] Planning-file changes can wake suppression early through the same shared cycle, without expanding into Phase 186 lifecycle semantics
</success_criteria>

<output>
After completion, create `.planning/phases/185-cmux-coordination-backbone/185-02-SUMMARY.md`
</output>
