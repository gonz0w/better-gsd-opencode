---
phase: 185-cmux-coordination-backbone
plan: 01
type: tdd
verification_route: full
wave: 1
depends_on: []
files_modified:
  - src/plugin/cmux-refresh-backbone.js
  - tests/plugin-cmux-refresh-backbone.test.cjs
autonomous: true
requirements:
  - CMUX-01
user_setup: []
must_haves:
  truths:
    - "Bursty runtime triggers collapse into one coordinated refresh cycle instead of spawning separate sidebar and attention refresh work"
    - "One coordinated cycle invalidates parser caches once, reads one fresh project snapshot, and fans that snapshot out to both cmux sinks"
    - "Overlapping refresh requests never run concurrent cycles; they request one bounded rerun with merged trigger detail"
  artifacts:
    - path: "src/plugin/cmux-refresh-backbone.js"
      provides: "Single-flight refresh coordinator with debounce, in-flight dedupe, and shared snapshot fan-out"
      contains: "createCmuxRefreshBackbone, enqueue, refreshNow"
    - path: "tests/plugin-cmux-refresh-backbone.test.cjs"
      provides: "Focused proof for debounce, rerun, and shared-snapshot behavior"
      contains: "createCmuxRefreshBackbone"
  key_links:
    - from: "src/plugin/cmux-refresh-backbone.js"
      to: "src/plugin/parsers/index.js"
      via: "one coordinated cycle invalidating parser caches before rebuilding runtime state"
      pattern: "invalidateAll|getProjectState"
    - from: "src/plugin/cmux-refresh-backbone.js"
      to: "src/plugin/cmux-sidebar-sync.js"
      via: "shared refresh payload fanning out to sidebar and attention sinks"
      pattern: "syncCmuxSidebar|syncCmuxAttention"
    - from: "tests/plugin-cmux-refresh-backbone.test.cjs"
      to: "src/plugin/cmux-refresh-backbone.js"
      via: "focused single-flight regressions covering coalescing and rerun behavior"
      pattern: "createCmuxRefreshBackbone|enqueue|refreshNow"
---

<objective>
Define the shared `cmux` refresh backbone that coalesces bursty runtime triggers into one trustworthy refresh cycle.

Purpose: the milestone needs `cmux` to reflect real runtime state without duplicate parse or process churn. This plan creates the single-flight coordinator that owns debounce, bounded reruns, and one shared snapshot fan-out before plugin hooks start enqueueing into it.
Output: a dedicated backbone module plus focused tests proving one-cycle snapshot refresh semantics.
</objective>

> **TDD Decision:** Selected — debounce, rerun, and one-snapshot fan-out are deterministic runtime contracts with clear observable outcomes.

<feature>
  <name>Single-flight cmux refresh backbone</name>
  <files>
    src/plugin/cmux-refresh-backbone.js
    tests/plugin-cmux-refresh-backbone.test.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/plugin-cmux-refresh-backbone.test.cjs</red>
    <green>node --test tests/plugin-cmux-refresh-backbone.test.cjs</green>
    <refactor>node --test tests/plugin-cmux-refresh-backbone.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    The coordinator accepts many runtime triggers, merges only the trigger detail needed for truthful attention behavior, and runs at most one refresh cycle at a time.
    Cases: bursty enqueue calls collapse into one debounced run; a trigger arriving during an in-flight run schedules exactly one follow-up cycle; each cycle performs one parser invalidation and one fresh project-state build before both sinks consume the same payload.
  </behavior>
  <implementation>
    Add a focused `src/plugin/cmux-refresh-backbone.js` module that owns timer state, in-flight dedupe, rerun bookkeeping, and shared payload construction. Keep it small and in-process: no new dependencies, no general event bus, no Phase 186 lifecycle vocabulary, and no separate urgent-attention read path.
  </implementation>
</feature>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/185-cmux-coordination-backbone/185-CONTEXT.md
@.planning/phases/185-cmux-coordination-backbone/185-RESEARCH.md
@src/plugin/parsers/index.js
@src/plugin/project-state.js
@src/plugin/cmux-sidebar-sync.js
@src/plugin/cmux-attention-sync.js
</context>

<tasks>

<task type="auto">
  <name>Author focused backbone regressions before wiring the plugin</name>
  <files>tests/plugin-cmux-refresh-backbone.test.cjs</files>
  <action>
    Create a dedicated backbone test file that specifies the shared-cycle contract before implementation. Cover debounced burst coalescing, one invalidation plus one project-state read per cycle, merged trigger fidelity for attention, and the single follow-up rerun when a new trigger arrives while a cycle is already in flight.

    Keep the tests module-focused rather than plugin-wide so Plan 02 can wire hooks without re-specifying coordinator semantics.
  </action>
  <verify>rg -n "createCmuxRefreshBackbone|coalesces|rerun|shared snapshot" tests/plugin-cmux-refresh-backbone.test.cjs</verify>
  <done>`tests/plugin-cmux-refresh-backbone.test.cjs` defines the debounced single-flight contract the coordinator must satisfy</done>
</task>

<task type="auto">
  <name>Implement the shared refresh coordinator with one-snapshot fan-out</name>
  <files>src/plugin/cmux-refresh-backbone.js</files>
  <action>
    Implement `createCmuxRefreshBackbone` so all later hook sources can call `enqueue(trigger, options)` and the plugin can request one immediate startup pass through `refreshNow(trigger, options)`. The cycle must invalidate planning parsers once, build one fresh runtime snapshot, then call both `syncCmuxSidebar` and `syncCmuxAttention` with the same payload and merged trigger metadata.

    Bound concurrency with explicit timer, `inFlight`, and rerun bookkeeping. Do not accumulate an unbounded trigger queue, do not fetch separate sidebar and attention snapshots, and do not embed failover vocabulary or multi-user coordination behavior that belongs to later phases.
  </action>
  <verify>node --test tests/plugin-cmux-refresh-backbone.test.cjs</verify>
  <done>The coordinator exports an implementation-ready single-flight backbone that passes focused debounce, rerun, and one-snapshot fan-out tests</done>
</task>

</tasks>

<verification>
- Run: rg -n "createCmuxRefreshBackbone" src/plugin/cmux-refresh-backbone.js tests/plugin-cmux-refresh-backbone.test.cjs
</verification>

<success_criteria>
- [ ] A dedicated backbone module owns debouncing, in-flight dedupe, and one follow-up rerun path
- [ ] Each coordinated cycle invalidates parsers once and rebuilds runtime state once before both cmux sinks run
- [ ] Trigger merging preserves only bounded attention-relevant fidelity instead of growing an unbounded queue
- [ ] The backbone stays phase-scoped: no new dependencies, no Phase 186 lifecycle vocabulary, no second read path for attention
</success_criteria>

<output>
After completion, create `.planning/phases/185-cmux-coordination-backbone/185-01-SUMMARY.md`
</output>
