---
phase: 187-reconstruct-phase-182-verification-coverage
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - workflows/execute-phase.md
  - tests/workflow.test.cjs
autonomous: true
gap_closure: true
requirements:
  - TEST-02
verification_route: light
verification_route_reason: "Repairs one locked workflow-contract phrase and proves it with the focused workflow suite without widening scope into broader runtime changes."
must_haves:
  truths:
    - "Rebuilt-runtime guidance again includes the exact locked phrase `run `npm run build`, then rerun the focused proof` so workflow-contract coverage stops failing on wording drift."
    - "The focused workflow contract suite passes without changing the surrounding risk-routing guidance beyond the exact locked phrase repair."
  artifacts:
    - path: "workflows/execute-phase.md"
      provides: "Locked rebuilt-runtime proof wording for execution guidance"
      contains: "run `npm run build`, then rerun the focused proof"
    - path: "tests/workflow.test.cjs"
      provides: "Exact workflow-contract assertion for rebuilt-runtime proof wording"
      contains: "run `npm run build`, then rerun the focused proof"
  key_links:
    - from: "tests/workflow.test.cjs"
      to: "workflows/execute-phase.md"
      via: "Exact rebuilt-runtime proof wording assertion"
      pattern: "run `npm run build`, then rerun the focused proof"
---

<objective>
Restore the locked rebuilt-runtime proof wording in the execute-phase workflow so the live Phase 182 integration blocker disappears without reopening verification-route policy design.

Purpose: Phase 187 is acceptance recovery work, and the current active regression is one exact-phrase mismatch tied to TEST-02. Fixing only that contract keeps the milestone aligned to the `effective_intent` priority of carrying `verification_route` truthfully through execution without creating fresh wording drift.
Output: repaired `workflows/execute-phase.md` wording plus focused workflow proof that turns the active red contract green.
</objective>

> **TDD Decision:** Selected — the slice changes one testable workflow contract with an existing failing assertion and a single expected wording outcome, so RED/GREEN proof is the safest floor.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/SKILL.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v19.0-MILESTONE-AUDIT.md
@.planning/phases/187-reconstruct-phase-182-verification-coverage/187-RESEARCH.md
@.planning/phases/182-risk-routed-hardening-proof-policy/182-01-PLAN.md
@.planning/phases/182-risk-routed-hardening-proof-policy/182-01-SUMMARY.md
@workflows/execute-phase.md
@tests/workflow.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve RED coverage for the locked rebuilt-runtime wording</name>
  <files>
    tests/workflow.test.cjs
  </files>
  <action>
  Reproduce the active blocker through the existing focused workflow contract suite before changing workflow text.

  Keep the locked expectation anchored to the exact phrase `run \`npm run build\`, then rerun the focused proof`. Only touch `tests/workflow.test.cjs` if inspection proves the test itself drifted away from that locked contract; otherwise preserve the current assertion and use it as the RED gate.
  </action>
  <verify>node --test tests/workflow.test.cjs</verify>
  <done>The focused workflow suite reproduces the rebuilt-runtime wording failure before any workflow edits, proving the blocker is a real RED-state contract break instead of a grep-only mismatch.</done>
</task>

<task type="auto">
  <name>Task 2: Repair the execute-phase wording with the smallest exact-phrase change</name>
  <files>
    workflows/execute-phase.md
    tests/workflow.test.cjs
  </files>
  <action>
  Update `workflows/execute-phase.md` so the rebuilt-runtime proof guidance matches the locked wording contract exactly: `run \`npm run build\`, then rerun the focused proof`.

  Keep the rest of the Phase 182 verification-route language intact, including the surrounding risk-routing guidance, so this remains a narrow contract repair rather than a workflow rewrite. If Task 1 required a test-file correction because the assertion itself drifted, keep that correction limited to the same exact phrase.
  </action>
  <verify>rg -n "run `npm run build`, then rerun the focused proof" workflows/execute-phase.md tests/workflow.test.cjs</verify>
  <done>The workflow source and focused contract test both carry the same locked phrase, with no unrelated wording churn around the verification-route guidance.</done>
</task>

</tasks>

<feature>
  <name>Locked rebuilt-runtime workflow wording contract</name>
  <files>
    workflows/execute-phase.md
    tests/workflow.test.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/workflow.test.cjs</red>
    <green>node --test tests/workflow.test.cjs</green>
    <refactor>rg -n "run `npm run build`, then rerun the focused proof" workflows/execute-phase.md tests/workflow.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    Execute-phase guidance once again includes the exact rebuilt-runtime proof wording the workflow contract locks.

    Cases:
      - the workflow text says `run \`npm run build\`, then rerun the focused proof`
      - the focused workflow suite turns green from that exact phrase repair
      - surrounding `verification_route` guidance stays unchanged unless the locked contract itself requires the edit
  </behavior>
  <implementation>
    Reuse the existing workflow wording and contract test surfaces. Make the smallest possible text repair instead of changing route policy, proof scope, or other workflow paragraphs.
  </implementation>
</feature>

<verification>
```bash
rg -n "run `npm run build`, then rerun the focused proof" workflows/execute-phase.md && node --test tests/workflow.test.cjs
```
</verification>

<success_criteria>
- [ ] `tests/workflow.test.cjs` no longer fails on the rebuilt-runtime proof wording contract
- [ ] `workflows/execute-phase.md` contains the exact locked phrase `run `npm run build`, then rerun the focused proof`
- [ ] No unrelated verification-route wording drift is introduced while repairing TEST-02
</success_criteria>

<output>
After completion, create `.planning/phases/187-reconstruct-phase-182-verification-coverage/187-01-SUMMARY.md`
</output>
