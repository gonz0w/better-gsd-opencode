---
phase: 183-plan-local-workspace-ownership
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/jj-workspace.js
  - src/commands/workspace.js
  - workflows/execute-phase.md
  - workflows/execute-plan.md
  - tests/workspace-ownership.test.cjs
  - tests/workflow.test.cjs
  - tests/workspace.test.cjs
autonomous: true
requirements:
  - JJ-02
verification_route: full
verification_route_reason: "Defines runtime workspace-result ownership, reconcile preview metadata, and executor workflow boundaries that prevent shared planning writes before finalize."
outcome_ids: []
must_haves:
  truths:
    - "A workspace run records summaries, proof, and reconcile-ready result metadata inside its assigned workspace without promoting shared `.planning` truth during execution."
    - "Operators can inspect a compact workspace completion summary first, then follow direct proof paths when the result claims major completion or touches risky runtime/shared-state surfaces."
    - "A first containable shared-planning write can be repaired and recorded, while repeated or serious boundary violations are marked for quarantine before finalize is allowed to promote anything."
  artifacts:
    - path: "src/lib/jj-workspace.js"
      provides: "Workspace result manifest normalization and boundary triage metadata"
      contains: "shared_planning_violation"
    - path: "src/commands/workspace.js"
      provides: "Preview-only reconcile output with result-manifest inspection data"
      contains: "mode: 'preview'"
    - path: "workflows/execute-plan.md"
      provides: "Executor contract that keeps plan-local artifacts inside the assigned workspace until finalize"
      contains: "Do not create `SUMMARY.md`, workspace proof sidecars, or other plan-local artifacts until workspace proof status is known."
  key_links:
    - from: "workflows/execute-phase.md"
      to: "src/commands/workspace.js"
      via: "Mode A reconcile remains preview-only while surfacing workspace-local result metadata for later finalize"
      pattern: "workspace reconcile|mode: 'preview'|result_manifest"
    - from: "src/commands/workspace.js"
      to: "src/lib/jj-workspace.js"
      via: "Reconcile output reuses one normalized workspace manifest and violation classification instead of inferring readiness from prose"
      pattern: "result_manifest|shared_planning_violation|inspection_level|quarantine"
---

<objective>
Make plan-local workspace output ownership explicit and machine-readable so workspace execution stays inspectable without mutating shared planning artifacts before finalize.

Purpose: Phase 183 only works if workspace execution owns summaries, proof, and reconcile diagnostics locally while shared `.planning` truth remains untouched. This slice locks the runtime boundary, the inspection contract, and the violation triage policy before any finalize writer exists.
Output: preview-only workspace result metadata, updated workspace execution guidance, and focused regressions that prove shared planning state stays untouched before finalize.
</objective>

> **TDD Decision:** Selected — workspace result eligibility, inspection escalation, and boundary-violation triage are deterministic runtime behavior with clear pass/fail outcomes, so RED tests should lock the ownership boundary before implementation changes.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
@/Users/cam/.config/opencode/skills/tdd-execution/SKILL.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/183-plan-local-workspace-ownership/183-CONTEXT.md
@.planning/phases/183-plan-local-workspace-ownership/183-RESEARCH.md
@.planning/phases/181-workspace-root-truth-safe-fallback/181-02-SUMMARY.md
@.planning/phases/182-risk-routed-hardening-proof-policy/182-02-SUMMARY.md
@src/lib/jj-workspace.js
@src/commands/workspace.js
@workflows/execute-phase.md
@workflows/execute-plan.md
@tests/workflow.test.cjs
@tests/workspace.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED coverage for workspace-local result manifests and pre-finalize ownership boundaries</name>
  <files>
    tests/workspace-ownership.test.cjs
    tests/workflow.test.cjs
  </files>
  <action>
  Add failing coverage that makes the locked Phase 183 boundary executable before changing implementation.

  Cover the preview-first contract: workspace execution should keep summary/proof/result metadata local, `workspace reconcile` must remain preview-only, shared `.planning/STATE.md`, `ROADMAP.md`, and `REQUIREMENTS.md` must stay untouched before finalize, and inspection should stay summary-first unless the manifest marks the result as a major completion claim or risky area. Lock the violation policy too: the first clearly containable shared-planning write records an auto-repairable violation, while repeated or serious writes escalate to quarantine and block later promotion.
  </action>
  <verify>node --test tests/workspace-ownership.test.cjs tests/workflow.test.cjs</verify>
  <done>The new ownership tests fail on current code because reconcile output lacks a normalized workspace result manifest and the workflow contract does not yet encode the full pre-finalize inspection and quarantine rules.</done>
</task>

<task type="auto">
  <name>Task 2: Implement preview-only workspace result metadata and boundary triage</name>
  <files>
    src/lib/jj-workspace.js
    src/commands/workspace.js
    workflows/execute-phase.md
    workflows/execute-plan.md
    tests/workspace-ownership.test.cjs
    tests/workflow.test.cjs
    tests/workspace.test.cjs
  </files>
  <action>
  Extend the JJ workspace helpers and command surface so each completed workspace exposes one normalized result-manifest shape that finalize can consume later without guessing from prose.

  Keep `workspace reconcile` preview-only, but enrich it with summary/proof/result-manifest fields such as local summary path, proof bucket status, inspection level, and shared-planning violation classification. Encode the locked inspection heuristic in one place: summary-first by default, direct proof review when verification risk is high or the result claims major completion. Update `workflows/execute-phase.md` and `workflows/execute-plan.md` so workspace-mode execution explicitly forbids shared planning mutations before finalize and records repair-versus-quarantine behavior for boundary violations.
  </action>
  <verify>npm run build</verify>
  <done>Workspace execution emits one preview-only result manifest with inspection and violation metadata, and the workflow contract states that shared planning files remain untouched until finalize promotes the workspace atomically.</done>
</task>

</tasks>

<feature>
  <name>Workspace-local result ownership and preview contract</name>
  <files>
    src/lib/jj-workspace.js
    src/commands/workspace.js
    workflows/execute-phase.md
    workflows/execute-plan.md
    tests/workspace-ownership.test.cjs
    tests/workflow.test.cjs
    tests/workspace.test.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/workspace-ownership.test.cjs tests/workflow.test.cjs</red>
    <green>npm run build && node --test tests/workspace-ownership.test.cjs tests/workflow.test.cjs tests/workspace.test.cjs</green>
    <refactor>node --test tests/workspace-ownership.test.cjs tests/workflow.test.cjs tests/workspace.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    Workspace execution remains plan-local until finalize.

    Cases:
      - completed workspaces expose local summary/proof/result-manifest metadata without mutating shared planning files
      - reconcile stays preview-only and reports inspection guidance plus shared-planning violation classification from normalized metadata
      - summary-first inspection remains the default, but direct proof review is required for major completion claims or risky runtime/shared-state work
      - repeated or serious direct shared-planning writes are quarantined before finalize rather than silently tolerated
  </behavior>
  <implementation>
    Reuse `src/lib/jj-workspace.js`, `src/commands/workspace.js`, and the existing execution workflows instead of introducing a second ownership system. This phase should define the local-result contract and boundary policy, not solve cross-workspace ordering or recovery storage from Phase 184.
  </implementation>
</feature>

<verification>
```bash
node --test tests/workspace-ownership.test.cjs tests/workflow.test.cjs tests/workspace.test.cjs
```
</verification>

<success_criteria>
- [ ] Workspace-mode execution records summaries, proof, and result metadata locally and does not promote shared `.planning` truth before finalize
- [ ] `workspace reconcile` stays preview-only while exposing normalized inspection and violation metadata for one workspace at a time
- [ ] The workflow contract makes summary-first inspection, proof escalation, and repair-versus-quarantine handling explicit for workspace-mode execution
</success_criteria>

<output>
After completion, create `.planning/phases/183-plan-local-workspace-ownership/183-01-SUMMARY.md`
</output>
