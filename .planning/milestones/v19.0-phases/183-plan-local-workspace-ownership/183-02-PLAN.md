---
phase: 183-plan-local-workspace-ownership
plan: 02
type: tdd
wave: 2
depends_on:
  - 01
files_modified:
  - src/commands/misc/finalize.js
  - src/commands/misc/index.js
  - src/router.js
  - workflows/execute-phase.md
  - workflows/execute-plan.md
  - tests/finalize.test.cjs
  - tests/workflow.test.cjs
autonomous: true
requirements:
  - JJ-02
  - FIN-01
verification_route: full
verification_route_reason: "Adds the single-writer finalize coordinator plus the default healthy auto-finalize path, while keeping human review limited to the explicit exception cases locked in Phase 183."
outcome_ids: []
must_haves:
  truths:
    - "A healthy workspace that passes reconcile eligibility auto-invokes finalize from trusted main-checkout state by default, so routine well-evidenced completion does not add a new manual approval step."
    - "Shared `.planning/STATE.md`, `ROADMAP.md`, and `REQUIREMENTS.md` change only through the explicit finalize coordinator, whether that coordinator is auto-invoked for the healthy default path or run manually for retry/inspection follow-up."
    - "If finalize sees ambiguity, intent conflict, missing proof, or quarantined/policy-violating boundary behavior, it stops before any shared planning write and leaves the workspace inspectable for human review."
  artifacts:
    - path: "src/commands/misc/finalize.js"
      provides: "Single-writer workspace finalize coordinator with healthy auto-finalize eligibility and exception gating"
      contains: "cmdExecuteFinalizePlan"
    - path: "src/router.js"
      provides: "Minimal routed execute finalize command surface"
      contains: "finalize-plan"
    - path: "workflows/execute-phase.md"
      provides: "Execution workflow that auto-finalizes healthy workspaces by default and reserves human review for explicit exception cases"
      contains: "execute:finalize-plan"
  key_links:
    - from: "workflows/execute-phase.md"
      to: "src/commands/misc/finalize.js"
      via: "Healthy reconcile-ready workspaces auto-call the finalize coordinator from trusted main-checkout state instead of waiting for routine manual approval"
      pattern: "trusted main-checkout|auto-finalize|execute:finalize-plan"
    - from: "src/commands/misc/finalize.js"
      to: "src/commands/state.js"
      via: "Finalize reuses the canonical state completion mutator instead of patching STATE.md directly"
      pattern: "cmdStateCompletePlan|complete-plan"
    - from: "src/commands/misc/finalize.js"
      to: "src/commands/roadmap.js"
      via: "Finalize updates shared roadmap progress only through the canonical roadmap mutation command"
      pattern: "cmdRoadmapUpdatePlanProgress|update-plan-progress"
    - from: "src/commands/misc/finalize.js"
      to: "src/commands/phase.js"
      via: "Finalize marks requirement completion through the existing requirements mutator"
      pattern: "cmdRequirementsMarkComplete|mark-complete"
---

<objective>
Add the single-writer finalize coordinator and make the healthy routine path auto-run by default once reconcile and proof checks succeed from trusted main-checkout state.

Purpose: Phase 183 is incomplete unless shared planning files have exactly one promotion path and routine well-evidenced work flows through it automatically. This slice makes finalize the only shared-state writer, preserves an explicit command entrypoint for retries/manual follow-up, and limits human review to the locked exception cases instead of introducing a default approval stop.
Output: a routed `execute:finalize-plan` coordinator, workflow guidance for default auto-finalize versus human-review exceptions, and focused regressions that prove canonical planning state only changes through finalize.
</objective>

> **TDD Decision:** Selected — finalize eligibility, healthy auto-promotion, and exception gating are deterministic orchestration behavior with clear observable outcomes, so RED tests should lock the default and blocked paths before the coordinator is wired in.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
@/Users/cam/.config/opencode/skills/tdd-execution/SKILL.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/183-plan-local-workspace-ownership/183-CONTEXT.md
@.planning/phases/183-plan-local-workspace-ownership/183-RESEARCH.md
@.planning/phases/183-plan-local-workspace-ownership/183-01-PLAN.md
@src/router.js
@src/commands/state.js
@src/commands/roadmap.js
@src/commands/phase.js
@workflows/execute-phase.md
@workflows/execute-plan.md
@tests/workflow.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED coverage for default healthy auto-finalize and human-review exception gates</name>
  <files>
    tests/finalize.test.cjs
    tests/workflow.test.cjs
  </files>
  <action>
  Add failing tests that make the locked Phase 183 finalize behavior executable before wiring the coordinator.

  Cover both the default and exception paths: a healthy workspace with complete proof and no ambiguity should auto-invoke finalize from trusted main-checkout state after reconcile eligibility passes, while ambiguity, intent conflict, missing proof, or quarantined/policy-violating boundary behavior must stop before any shared `.planning` write and route to human review with the workspace still inspectable. Lock the single-writer guarantee too: even the auto-run path must promote shared state only through the canonical state, roadmap, and requirements mutators.
  </action>
  <verify>node --test tests/finalize.test.cjs tests/workflow.test.cjs</verify>
  <done>The finalize tests fail on current code because no explicit finalize coordinator exists, healthy workspaces cannot auto-finalize by default, and the workflow guidance does not yet encode the exception-only human-review contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the finalize coordinator and wire healthy reconcile-ready workspaces into the default auto-finalize path</name>
  <files>
    src/commands/misc/finalize.js
    src/commands/misc/index.js
    src/router.js
    workflows/execute-phase.md
    workflows/execute-plan.md
    tests/finalize.test.cjs
    tests/workflow.test.cjs
  </files>
  <action>
  Add a dedicated finalize coordinator under the existing execute surface rather than letting workspace or executor code mutate shared planning state directly.

  Implement `execute:finalize-plan <plan-id|workspace-name>` so it runs from trusted main-checkout state, consumes the workspace-local result manifest from Plan 01, validates proof and boundary compliance, and promotes exactly one workspace atomically by calling the canonical shared-state mutators in order: `verify:state complete-plan`, `plan:roadmap update-plan-progress`, and `plan:requirements mark-complete`. Then update the execution workflows so healthy workspaces auto-call this coordinator immediately after reconcile eligibility passes, while the explicit command remains available for retry or operator-directed follow-up after exceptions are resolved. Keep human review limited to ambiguity, intent conflict, missing proof, or quarantined/policy-violating workspaces; those cases must remain blocked and inspectable instead of auto-promoted.
  </action>
  <verify>npm run build</verify>
  <done>The repo exposes one finalize coordinator, healthy reconcile-ready workspaces auto-finalize by default through it, and shared planning files stay untouched whenever the exception gates require human review.</done>
</task>

</tasks>

<feature>
  <name>Single-writer workspace finalize promotion with default healthy auto-run</name>
  <files>
    src/commands/misc/finalize.js
    src/commands/misc/index.js
    src/router.js
    workflows/execute-phase.md
    workflows/execute-plan.md
    tests/finalize.test.cjs
    tests/workflow.test.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/finalize.test.cjs tests/workflow.test.cjs</red>
    <green>npm run build && node --test tests/finalize.test.cjs tests/workflow.test.cjs</green>
    <refactor>node --test tests/finalize.test.cjs tests/workflow.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    Finalize is the only shared-state writer for workspace execution, and healthy workspaces use it automatically by default.

    Cases:
      - a healthy workspace with complete proof auto-invokes finalize from trusted main-checkout state after reconcile eligibility passes
      - the explicit `execute:finalize-plan` command remains available as the same single-writer coordinator for retry/manual follow-up, not as a separate mutation path
      - finalize refuses to mutate shared planning files when proof is missing, intent is ambiguous or conflicting, or the workspace is quarantined for boundary violations
      - shared state promotion reuses canonical state, roadmap, and requirement mutators instead of hand-editing markdown
      - shared milestone and phase progress stay incomplete until finalize succeeds
  </behavior>
  <implementation>
    Keep this slice focused on finalize behavior, minimal routing, workflow contract, and tests. Do not expand into command-discovery/help-surface polish, sibling ordering, partial-wave determinism, or durable recovery stores that belong outside this phase.
  </implementation>
</feature>

<verification>
```bash
npm run build && node --test tests/finalize.test.cjs tests/workflow.test.cjs
```
</verification>

<success_criteria>
- [ ] Healthy reconcile-ready workspaces auto-finalize by default from trusted main-checkout state instead of introducing a routine manual approval step
- [ ] Shared `.planning/STATE.md`, `ROADMAP.md`, and `REQUIREMENTS.md` are promoted only through one finalize coordinator, whether it is auto-invoked or run explicitly for retry/follow-up
- [ ] Finalize reuses canonical state, roadmap, and requirement mutators and blocks before any shared write when ambiguity, conflict, missing proof, or policy-violation exceptions require human review
- [ ] Execution guidance keeps reconcile preview-only and treats canonical phase or milestone completion as a post-finalize outcome rather than a workspace-execution side effect
</success_criteria>

<output>
After completion, create `.planning/phases/183-plan-local-workspace-ownership/183-02-SUMMARY.md`
</output>
