---
phase: 186-cmux-truthful-lifecycle-signals
plan: 01
type: tdd
verification_route: full
verification_route_reason: "Introduces the shared lifecycle classifier that drives runtime cmux status, progress, and hint truth across workspace states, so focused behavior proof plus build validation are required."
wave: 1
depends_on: []
files_modified:
  - src/plugin/cmux-lifecycle-signal.js
  - src/plugin/cmux-sidebar-snapshot.js
  - tests/plugin-cmux-lifecycle-signal.test.cjs
  - tests/plugin-cmux-sidebar-snapshot.test.cjs
autonomous: true
requirements:
  - CMUX-02
user_setup: []
outcome_ids: []
must_haves:
  truths:
    - "User can see one truthful workspace lifecycle state for running, blocked, waiting, stale, reconciling, finalize-failed, idle, and complete without different cmux surfaces inventing their own meanings."
    - "Compact workspace-scoped overview text includes a plain-English status plus a lightweight progress or log hint so operators can understand activity without pane-hopping."
    - "Intervention-required states stop showing misleading advancing progress, while quiet states keep plain-English labels and trustworthy progress treatment."
  artifacts:
    - path: "src/plugin/cmux-lifecycle-signal.js"
      provides: "Shared workspace lifecycle classifier with ordered state precedence, hint text, severity, and progress treatment"
      contains: "deriveWorkspaceLifecycleSignal"
    - path: "src/plugin/cmux-sidebar-snapshot.js"
      provides: "Sidebar snapshot projection that reuses the shared lifecycle signal for status, context, and activity output"
      contains: "deriveCmuxSidebarSnapshot"
    - path: "tests/plugin-cmux-lifecycle-signal.test.cjs"
      provides: "Focused lifecycle contract proof for precedence, hint generation, and progress suppression"
      contains: "deriveWorkspaceLifecycleSignal"
  key_links:
    - from: "src/plugin/cmux-sidebar-snapshot.js"
      to: "src/plugin/cmux-lifecycle-signal.js"
      via: "sidebar snapshot deriving status, activity hint, and progress behavior from one shared semantic signal"
      pattern: "deriveWorkspaceLifecycleSignal"
    - from: "src/plugin/cmux-lifecycle-signal.js"
      to: "src/lib/jj-workspace.js"
      via: "reuse of canonical stale and finalize-failed workspace meanings when recovery metadata is present"
      pattern: "stale|finalize_failed|blocking_reason|recovery_summary"
    - from: "tests/plugin-cmux-lifecycle-signal.test.cjs"
      to: "src/plugin/cmux-lifecycle-signal.js"
      via: "scenario tests proving ordered lifecycle precedence and intervention-state progress suppression"
      pattern: "finalize-failed|waiting|stale|blocked|reconciling|running|complete|idle"
---

<objective>
Create the shared lifecycle truth layer that turns current runtime metadata into one workspace-scoped cmux status, hint, and progress contract.

Purpose: Phase 186 only works if sidebar and later attention logic consume the same semantic state instead of separate regex guesses. This plan establishes the ordered lifecycle classifier and updates the sidebar snapshot to project compact, plain-English, workspace-scoped signals for roadmap criterion 1.
Output: a dedicated lifecycle derivation module, updated sidebar projection, and focused tests proving truthful precedence and progress handling.
</objective>

> **TDD Decision:** Selected — lifecycle precedence, hint selection, and progress suppression are deterministic rules with clear scenario-based outputs, so RED tests should lock the contract before implementation.

<feature>
  <name>Shared workspace lifecycle classifier for cmux sidebar truth</name>
  <files>
    src/plugin/cmux-lifecycle-signal.js
    src/plugin/cmux-sidebar-snapshot.js
    tests/plugin-cmux-lifecycle-signal.test.cjs
    tests/plugin-cmux-sidebar-snapshot.test.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/plugin-cmux-lifecycle-signal.test.cjs tests/plugin-cmux-sidebar-snapshot.test.cjs</red>
    <green>node --test tests/plugin-cmux-lifecycle-signal.test.cjs tests/plugin-cmux-sidebar-snapshot.test.cjs</green>
    <refactor>node --test tests/plugin-cmux-lifecycle-signal.test.cjs tests/plugin-cmux-sidebar-snapshot.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    One ordered classifier derives the visible workspace lifecycle contract before any cmux surface projects it.
    Cases: `finalize-failed`, `waiting`, `stale`, and actionable `blocked` outrank quiet active states; `reconciling`, `running`, `complete`, and `idle` stay plain-English and visually quiet; intervention-required states clear misleading exact progress while still surfacing a compact status or activity hint.
  </behavior>
  <implementation>
    Add a focused `src/plugin/cmux-lifecycle-signal.js` module that derives state, label, hint, severity, context, and progress mode from current runtime metadata plus recent notification history. Update `src/plugin/cmux-sidebar-snapshot.js` to delegate to that module instead of independently inferring `Input needed`, `Blocked`, `Warning`, or `Working` from loose status text.

    Reuse Phase 184 recovery/finalize taxonomy when structured metadata is present, keep the overview compact, and do not add new lifecycle states, a second refresh path, or broad cmux UX expansion.
  </implementation>
</feature>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
@/Users/cam/.config/opencode/skills/tdd-execution/SKILL.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/186-cmux-truthful-lifecycle-signals/186-CONTEXT.md
@.planning/phases/186-cmux-truthful-lifecycle-signals/186-RESEARCH.md
@src/plugin/cmux-sidebar-snapshot.js
@src/plugin/cmux-sidebar-sync.js
@src/plugin/cmux-attention-sync.js
@src/lib/jj-workspace.js
@tests/plugin-cmux-sidebar-snapshot.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Add RED coverage for lifecycle precedence, compact hints, and progress suppression</name>
  <files>
    tests/plugin-cmux-lifecycle-signal.test.cjs
    tests/plugin-cmux-sidebar-snapshot.test.cjs
  </files>
  <action>
    Add failing module-level tests that lock the Phase 186 sidebar truth contract before implementation. Cover the roadmap state set explicitly with ordered precedence: `finalize-failed` over `waiting`, `waiting` over `stale`, `stale` over `blocked`, and quiet active states for `reconciling`, `running`, `complete`, and `idle`.

    Also pin the compact overview rules: every scenario must emit a plain-English status plus a lightweight inline hint, and intervention-required states must clear exact progress instead of implying work is still advancing.
  </action>
  <verify>rg -n "finalize-failed|waiting|stale|blocked|reconciling|running|complete|idle|lightweight inline hint" tests/plugin-cmux-lifecycle-signal.test.cjs tests/plugin-cmux-sidebar-snapshot.test.cjs</verify>
  <done>The new tests fail on current code because Phase 185 snapshot heuristics do not yet expose the roadmap-aligned lifecycle states, shared hints, or intervention-state progress suppression.</done>
</task>

<task type="auto">
  <name>Implement the shared lifecycle classifier and route sidebar projection through it</name>
  <files>
    src/plugin/cmux-lifecycle-signal.js
    src/plugin/cmux-sidebar-snapshot.js
    tests/plugin-cmux-lifecycle-signal.test.cjs
    tests/plugin-cmux-sidebar-snapshot.test.cjs
  </files>
  <action>
    Implement `deriveWorkspaceLifecycleSignal()` as the single semantic source for Phase 186 state truth. It should derive the visible lifecycle state, plain-English label, compact hint, severity, and progress mode from trusted runtime metadata and recent warning/error history, while honoring the locked high-impact decisions from `186-CONTEXT.md`.

    Update `deriveCmuxSidebarSnapshot()` to project status, context, activity hint, and progress directly from that signal. Keep the module phase-scoped: no new dependencies, no persistent event store, no dashboard behavior, and no separate attention-only classifier.
  </action>
  <verify>node --test tests/plugin-cmux-lifecycle-signal.test.cjs tests/plugin-cmux-sidebar-snapshot.test.cjs</verify>
  <done>The sidebar snapshot now reflects one shared, roadmap-aligned lifecycle contract with truthful state precedence, compact hints, and trustworthy progress handling.</done>
</task>

</tasks>

<verification>
```bash
npm run build && node --test tests/plugin-cmux-lifecycle-signal.test.cjs tests/plugin-cmux-sidebar-snapshot.test.cjs
```
</verification>

<success_criteria>
- [ ] Roadmap criterion 1 is mapped to one ordered lifecycle classifier that covers running, blocked, waiting, stale, reconciling, finalize-failed, idle, and complete states
- [ ] Sidebar output shows a compact plain-English status plus lightweight activity or progress context without forcing users to inspect raw logs
- [ ] Waiting, stale, finalize-failed, and actionable blocked states suppress misleading exact progress while quiet states keep truthful non-alarm treatment
- [ ] The implementation stays phase-scoped: no new dependencies, no extra lifecycle states, and no second interpretation path for sidebar truth
</success_criteria>

<output>
After completion, create `.planning/phases/186-cmux-truthful-lifecycle-signals/186-01-SUMMARY.md`
</output>
