---
phase: 186-cmux-truthful-lifecycle-signals
plan: 02
type: tdd
verification_route: full
verification_route_reason: "Extends runtime cmux attention, log, and sidebar projection behavior for intervention-required lifecycle states, so integration-focused proof plus build validation are required."
wave: 2
depends_on:
  - 186-01
files_modified:
  - src/plugin/cmux-sidebar-sync.js
  - src/plugin/cmux-attention-sync.js
  - src/plugin/cmux-attention-policy.js
  - tests/plugin-cmux-attention-sync.test.cjs
  - tests/plugin-cmux-attention-policy.test.cjs
autonomous: true
requirements:
  - CMUX-02
  - CMUX-03
user_setup: []
outcome_ids: []
must_haves:
  truths:
    - "User receives one truthful cmux attention signal only when a workspace needs human input, stale-workspace recovery, or finalize intervention."
    - "Required-intervention states stand out from normal progress in status, logs, and notifications so operators can tell where to act without polling raw panes."
    - "Sidebar, logs, and notifications reuse the same lifecycle snapshot, so state text, hint text, and attention decisions do not contradict each other."
  artifacts:
    - path: "src/plugin/cmux-attention-sync.js"
      provides: "Transition-aware attention sync that derives log and notification events from the shared lifecycle signal"
      contains: "syncCmuxAttention"
    - path: "src/plugin/cmux-attention-policy.js"
      provides: "Policy mapping for intervention-required vs quiet lifecycle events, including notify and cooldown behavior"
      contains: "classifyAttentionEvent"
    - path: "tests/plugin-cmux-attention-sync.test.cjs"
      provides: "Focused proof that sidebar and attention consume the same lifecycle meaning and only alert for required intervention"
      contains: "syncCmuxAttention"
  key_links:
    - from: "src/plugin/cmux-attention-sync.js"
      to: "src/plugin/cmux-lifecycle-signal.js"
      via: "attention candidate derivation using the same lifecycle state, hint, and severity projected into the sidebar"
      pattern: "deriveWorkspaceLifecycleSignal"
    - from: "src/plugin/cmux-sidebar-sync.js"
      to: "src/plugin/cmux-lifecycle-signal.js"
      via: "sidebar status, activity hint, and progress decisions reusing the shared lifecycle snapshot fields"
      pattern: "deriveWorkspaceLifecycleSignal|deriveCmuxSidebarSnapshot"
    - from: "src/plugin/cmux-attention-policy.js"
      to: "tests/plugin-cmux-attention-policy.test.cjs"
      via: "cooldown and notify rules for waiting, stale, finalize-failed, and quiet lifecycle transitions"
      pattern: "waiting|stale|finalize-failed|blocked|reconciling|running"
---

<objective>
Project the shared lifecycle truth into cmux attention, logs, and final sidebar behavior so intervention-required states stand out without noisy false alarms.

Purpose: Phase 186 is only approval-ready when the same semantic state powers sidebar pills, progress clearing, transition logs, and notifications. This plan maps roadmap criteria 2 and 3 by wiring shared lifecycle truth into attention/log policy and proving loud attention is reserved for actual operator intervention.
Output: unified attention integration, updated sidebar projection behavior, and focused tests for truthful transition logging and intervention-only notifications.
</objective>

> **TDD Decision:** Selected — transition logging, notification eligibility, and shared-signal reuse across sidebar and attention are deterministic runtime behaviors with clear scenario outcomes.

<feature>
  <name>Unified lifecycle attention and intervention signaling</name>
  <files>
    src/plugin/cmux-sidebar-sync.js
    src/plugin/cmux-attention-sync.js
    src/plugin/cmux-attention-policy.js
    tests/plugin-cmux-attention-sync.test.cjs
    tests/plugin-cmux-attention-policy.test.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/plugin-cmux-attention-sync.test.cjs tests/plugin-cmux-attention-policy.test.cjs</red>
    <green>node --test tests/plugin-cmux-attention-sync.test.cjs tests/plugin-cmux-attention-policy.test.cjs</green>
    <refactor>node --test tests/plugin-cmux-attention-sync.test.cjs tests/plugin-cmux-attention-policy.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    Attention and sidebar projection consume the same lifecycle snapshot and emit logs or notifications only on semantic transitions that truly require action.
    Cases: `waiting`, `stale`, and `finalize-failed` notify once per semantic occurrence; actionable `blocked` can log loudly without treating `reconciling`, `running`, `idle`, or `complete` as alerts; transition logs explain recovery or completion after an intervention without leaving active warning UI stuck on the resolved state.
  </behavior>
  <implementation>
    Update `src/plugin/cmux-attention-sync.js` and `src/plugin/cmux-attention-policy.js` to derive events from the Plan 01 lifecycle signal rather than a second set of regex heuristics. Adjust `src/plugin/cmux-sidebar-sync.js` as needed so the sidebar uses the shared hint and progress treatment consistently with attention outcomes.

    Keep transition logging bounded, keep loud attention reserved for states requiring human action, and preserve Phase 185's shared refresh backbone and quiet fail-open behavior. Do not add new lifecycle states, raw-log browsing, or a dashboard surface.
  </implementation>
</feature>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
@/Users/cam/.config/opencode/skills/tdd-execution/SKILL.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/186-cmux-truthful-lifecycle-signals/186-CONTEXT.md
@.planning/phases/186-cmux-truthful-lifecycle-signals/186-RESEARCH.md
@.planning/phases/186-cmux-truthful-lifecycle-signals/186-01-PLAN.md
@src/plugin/cmux-lifecycle-signal.js
@src/plugin/cmux-sidebar-sync.js
@src/plugin/cmux-attention-sync.js
@src/plugin/cmux-attention-policy.js
@tests/plugin-cmux-attention-policy.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Add RED coverage for intervention-only notifications and shared-signal transition logs</name>
  <files>
    tests/plugin-cmux-attention-sync.test.cjs
    tests/plugin-cmux-attention-policy.test.cjs
  </files>
  <action>
    Add failing tests that prove the Phase 186 attention contract before changing runtime code. Cover semantic transitions for `waiting`, `stale`, `finalize-failed`, and actionable `blocked`, and assert that only the human-action states notify while quiet states remain log-only or silent.

    Also lock cross-surface truthfulness: attention events must reuse the same lifecycle state and hint generated for the sidebar rather than recomputing separate meanings from raw status text.
  </action>
  <verify>rg -n "waiting|stale|finalize-failed|blocked|shared-signal|reuse the same lifecycle state|notify" tests/plugin-cmux-attention-sync.test.cjs tests/plugin-cmux-attention-policy.test.cjs</verify>
  <done>The new tests fail on current code because attention and sidebar still infer meaning separately and warning/blocker notifications are not yet aligned to the roadmap-defined intervention states.</done>
</task>

<task type="auto">
  <name>Wire attention policy and sidebar sync to the shared lifecycle signal</name>
  <files>
    src/plugin/cmux-sidebar-sync.js
    src/plugin/cmux-attention-sync.js
    src/plugin/cmux-attention-policy.js
    tests/plugin-cmux-attention-sync.test.cjs
    tests/plugin-cmux-attention-policy.test.cjs
  </files>
  <action>
    Refactor the attention candidate builder and policy mapping so they consume Plan 01 lifecycle output for state, hint, severity, and progress treatment. Emit logs only on semantic transitions, notify only for waiting-input, stale-recovery, and finalize-intervention cases, and keep any `blocked` escalation limited to truly actionable hard stops rather than routine degraded flow.

    Update sidebar sync where needed so the same shared hint and progress-clearing behavior appears in the visible workspace overview. Preserve cooldown and dedupe behavior, preserve quiet fail-open cmux handling, and avoid introducing any second classifier or noisy refresh-time log spam.
  </action>
  <verify>node --test tests/plugin-cmux-attention-sync.test.cjs tests/plugin-cmux-attention-policy.test.cjs</verify>
  <done>cmux sidebar, logs, and notifications now reuse one lifecycle truth source, with intervention-required states clearly surfacing where operator action is needed and quiet states staying non-alarming.</done>
</task>

</tasks>

<verification>
```bash
npm run build && node --test tests/plugin-cmux-attention-sync.test.cjs tests/plugin-cmux-attention-policy.test.cjs
```
</verification>

<success_criteria>
- [ ] Roadmap criterion 2 is mapped to explicit notification and logging behavior for waiting, stale-workspace recovery, and finalize intervention states
- [ ] Roadmap criterion 3 is mapped to shared sidebar and attention treatment that makes required-intervention states stand out from normal progress without alerting on quiet states
- [ ] Attention and sidebar reuse one lifecycle signal so status, hint, log, and notification semantics do not contradict each other
- [ ] Transition logging remains bounded and Phase 185 refresh/backoff behavior stays intact without event storms or stale active warnings after resolution
</success_criteria>

<output>
After completion, create `.planning/phases/186-cmux-truthful-lifecycle-signals/186-02-SUMMARY.md`
</output>
