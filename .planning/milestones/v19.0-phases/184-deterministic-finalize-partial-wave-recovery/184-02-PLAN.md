---
phase: 184-deterministic-finalize-partial-wave-recovery
plan: 02
type: tdd
wave: 2
depends_on:
  - 01
files_modified:
  - src/commands/misc/finalize.js
  - src/commands/misc/index.js
  - src/router.js
  - tests/finalize.test.cjs
autonomous: true
requirements:
  - FIN-02
  - FIN-03
  - FIN-04
verification_route: full
verification_route_reason: "Implements deterministic shared-state promotion and rerun behavior for sibling workspaces, directly affecting finalize ordering and single-writer planning truth."
outcome_ids: []
must_haves:
  truths:
    - "Final shared planning state is computed from canonical planned wave order rather than the order healthy siblings happen to finish."
    - "The wave finalizer promotes only the longest healthy prefix and leaves later healthy siblings staged-ready behind the first earlier blocker."
    - "Rerunning finalize from trusted main-checkout state preserves already-healthy staged work and regenerates canonical recovery metadata without re-executing those siblings."
  artifacts:
    - path: "src/commands/misc/finalize.js"
      provides: "Wave-level deterministic finalize coordinator"
      contains: "cmdExecuteFinalizeWave"
    - path: "src/router.js"
      provides: "Execute-surface routing for wave finalize"
      contains: "finalize-wave"
    - path: "tests/finalize.test.cjs"
      provides: "Regression coverage for ordered-prefix promotion and rerun determinism"
      contains: "gating_sibling"
  key_links:
    - from: "src/commands/misc/finalize.js"
      to: "src/commands/state.js"
      via: "Wave finalize reuses canonical complete-plan mutation for each promotable sibling instead of patching shared state directly"
      pattern: "complete-plan|update-plan-progress|mark-complete"
    - from: "src/commands/misc/finalize.js"
      to: "src/lib/jj-workspace.js"
      via: "Ordered-prefix promotion consumes staged manifests and recovery metadata generated by the workspace health layer"
      pattern: "staged_ready|gating_sibling|createWaveRecoverySummary"
---

<objective>
Add the deterministic wave finalizer that reconciles healthy sibling workspaces into one canonical shared state without letting completion timing leak into milestone truth.

Purpose: Phase 184 is not complete until shared `.planning` updates are computed from planned wave order, not sibling finish order. This slice introduces one trusted-main wave coordinator that promotes only the healthy prefix, preserves later healthy work as staged-ready, and regenerates recovery metadata on rerun.
Output: a routed wave-finalize command, deterministic ordered-prefix promotion logic, and focused regressions that prove shared-state determinism across finish-order permutations.
</objective>

> **TDD Decision:** Selected — ordered-prefix promotion, rerun idempotence, and blocker handling are deterministic orchestration behavior with clear pass/fail outcomes, so RED tests should lock the finish-order permutations before implementation.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
@/Users/cam/.config/opencode/skills/tdd-execution/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/184-deterministic-finalize-partial-wave-recovery/184-CONTEXT.md
@.planning/phases/184-deterministic-finalize-partial-wave-recovery/184-RESEARCH.md
@.planning/phases/183-plan-local-workspace-ownership/183-02-SUMMARY.md
@src/commands/misc/finalize.js
@src/router.js
@tests/finalize.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED coverage for ordered-prefix promotion and trusted-main reruns</name>
  <files>
    tests/finalize.test.cjs
  </files>
  <action>
  Add failing finalize tests that lock the Phase 184 deterministic-promotion rules before changing the coordinator.

  Cover finish-order permutations for siblings in the same wave, proving the final shared state is identical when healthy siblings finish in different orders. Lock the blocker rule too: once the first earlier unhealthy sibling appears, later healthy siblings must remain `staged_ready`, shared state must stay `recovery-needed`, and rerunning finalize from trusted main checkout after recovery must reuse already-healthy staged work instead of requiring re-execution.
  </action>
  <verify>node --test tests/finalize.test.cjs</verify>
  <done>The new finalize tests fail on current code because there is no wave-level coordinator, no ordered-prefix promotion, and no rerun path that preserves healthy staged siblings behind a blocker.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the deterministic wave finalizer and ordered-prefix promotion</name>
  <files>
    src/commands/misc/finalize.js
    src/commands/misc/index.js
    src/router.js
    tests/finalize.test.cjs
  </files>
  <action>
  Extend the existing finalize coordinator into a wave-aware trusted-main entrypoint instead of finalizing siblings independently.

  Add `execute:finalize-wave` so it loads staged sibling manifests, sorts them by canonical planned order, promotes only the longest healthy prefix through the existing canonical mutators, and stops at the first earlier unhealthy sibling. Later healthy siblings must stay `staged_ready`, shared state must remain explicitly `recovery-needed`, and the canonical recovery summary must be regenerated from current disk truth on every rerun. Keep reruns anchored to trusted main-checkout state and preserve already-finalized or staged-ready healthy work rather than requiring fresh execution.
  </action>
  <verify>npm run build</verify>
  <done>The repo exposes one wave-aware finalize command that produces the same shared state regardless of healthy sibling finish order, stops cleanly at the first blocker, and preserves healthy staged work for trusted-main reruns.</done>
</task>

</tasks>

<feature>
  <name>Deterministic wave finalize with ordered-prefix promotion</name>
  <files>
    src/commands/misc/finalize.js
    src/commands/misc/index.js
    src/router.js
    tests/finalize.test.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/finalize.test.cjs</red>
    <green>npm run build && node --test tests/finalize.test.cjs</green>
    <refactor>node --test tests/finalize.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    Shared planning truth is promoted in one deterministic order no matter which healthy sibling finishes first.

    Cases:
      - healthy siblings finalize only in canonical planned wave order
      - the first earlier unhealthy sibling becomes the gating blocker for wave promotion
      - later healthy siblings stay `staged_ready` behind that blocker instead of being promoted or discarded
      - rerunning finalize from trusted main checkout recomputes the same result from staged manifests and refreshed recovery metadata
  </behavior>
  <implementation>
    Build on the existing `execute:finalize-plan` single-writer pattern and canonical mutators. Do not introduce new markdown patchers, timestamp-based ordering, or automatic sibling-plan routing heuristics.
  </implementation>
</feature>

<verification>
```bash
npm run build && node --test tests/finalize.test.cjs
```
</verification>

<success_criteria>
- [ ] Final shared planning state is identical across healthy sibling finish-order permutations
- [ ] Later healthy siblings behind the first earlier blocker remain staged-ready and inspectable instead of promoting shared state out of order
- [ ] Trusted-main finalize reruns preserve already-healthy staged work and regenerate canonical recovery metadata from current disk truth
</success_criteria>

<output>
After completion, create `.planning/phases/184-deterministic-finalize-partial-wave-recovery/184-02-SUMMARY.md`
</output>
