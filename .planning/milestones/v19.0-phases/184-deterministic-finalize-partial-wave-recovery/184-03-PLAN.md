---
phase: 184-deterministic-finalize-partial-wave-recovery
plan: 03
type: tdd
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - src/commands/init.js
  - src/commands/workspace.js
  - workflows/execute-phase.md
  - tests/workflow.test.cjs
  - tests/workspace.test.cjs
autonomous: true
requirements:
  - FIN-02
  - FIN-04
verification_route: full
verification_route_reason: "Wires partial-wave recovery status into execution and inventory surfaces that operators depend on for truthful recovery and finalize decisions."
outcome_ids: []
must_haves:
  truths:
    - "Execution surfaces report partial-wave outcomes honestly instead of collapsing healthy and unhealthy siblings into one opaque success/failure bit."
    - "Operators can inspect one canonical recovery summary first, see the exact gating sibling, and rerun recovery/finalize from trusted main-checkout state."
    - "Active workspace inventory exposes staged-ready, recovery-needed, and finalize-failed status so downstream cmux work can project truthful runtime state without inventing it later."
  artifacts:
    - path: "workflows/execute-phase.md"
      provides: "Wave execution contract for trusted-main finalize/recovery reruns"
      contains: "execute:finalize-wave"
    - path: "src/commands/init.js"
      provides: "Execution init payload with wave-aware workspace status for active siblings"
      contains: "workspace_active"
    - path: "src/commands/workspace.js"
      provides: "Inventory/reconcile output that surfaces canonical recovery-summary references"
      contains: "recovery_summary"
  key_links:
    - from: "workflows/execute-phase.md"
      to: "src/commands/misc/finalize.js"
      via: "Wave execution reruns finalize and recovery from trusted main-checkout state instead of from inside a sibling workspace"
      pattern: "trusted main-checkout state|execute:finalize-wave|workspace reconcile"
    - from: "src/commands/init.js"
      to: "src/commands/workspace.js"
      via: "Active workspace inventory reuses the wave-aware reconcile/status contract so downstream status projections stay truthful"
      pattern: "workspace_active|staged_ready|recovery_summary"
---

<objective>
Wire the deterministic finalize and recovery contract into execution-facing status surfaces so operators can inspect, recover, and rerun without guessing.

Purpose: Phase 184 needs truthful operator surfaces, not just internal finalize logic. This slice makes partial-wave outcomes explicit in execution and inventory outputs, teaches trusted-main reruns, and ensures one canonical recovery summary is the default inspection entrypoint.
Output: updated execution workflow guidance, wave-aware active-workspace status output, and focused regressions that prove partial-wave recovery stays inspectable.
</objective>

> **TDD Decision:** Selected — partial-wave status reporting, trusted-main rerun guidance, and recovery-summary wiring are deterministic runtime/workflow behavior with explicit expected outputs, so RED tests should lock those surfaces before implementation.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
@/Users/cam/.config/opencode/skills/tdd-execution/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/184-deterministic-finalize-partial-wave-recovery/184-CONTEXT.md
@.planning/phases/184-deterministic-finalize-partial-wave-recovery/184-RESEARCH.md
@workflows/execute-phase.md
@src/commands/init.js
@src/commands/workspace.js
@tests/workflow.test.cjs
@tests/workspace.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED coverage for truthful partial-wave reporting and trusted-main recovery guidance</name>
  <files>
    tests/workflow.test.cjs
    tests/workspace.test.cjs
  </files>
  <action>
  Add failing coverage that locks the Phase 184 operator contract before changing execution surfaces.

  Require execution guidance to distinguish healthy finalized siblings, healthy-but-blocked staged-ready siblings, and recovery-needed blockers explicitly. Lock the default inspection path too: one canonical recovery summary must be the first surface shown, it must name the exact gating sibling and next command, and recovery/finalize reruns must be described as trusted main-checkout actions rather than workspace-local retries.
  </action>
  <verify>node --test tests/workflow.test.cjs tests/workspace.test.cjs</verify>
  <done>The new workflow and workspace tests fail on current code because partial-wave outcomes, recovery-summary references, and trusted-main rerun guidance are not yet fully wired into execution-facing surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Implement wave-aware inventory output and execution guidance</name>
  <files>
    src/commands/init.js
    src/commands/workspace.js
    workflows/execute-phase.md
    tests/workflow.test.cjs
    tests/workspace.test.cjs
  </files>
  <action>
  Update execution-facing surfaces so the new finalize/recovery behavior is visible without inspecting raw manifests first.

  Extend active workspace inventory and reconcile/list output to surface staged-ready, recovery-needed, finalize-failed, gating-sibling, and canonical recovery-summary references. Then update `workflows/execute-phase.md` so wave execution reports partial-wave outcomes honestly, directs operators to the recovery summary first, and reruns recovery/finalize from trusted main-checkout state via the wave-aware finalize path once blockers are resolved.
  </action>
  <verify>npm run build</verify>
  <done>Execution and inventory surfaces now expose truthful partial-wave states, point to one canonical recovery summary, and teach trusted-main reruns for finalize/recovery follow-up.</done>
</task>

</tasks>

<feature>
  <name>Truthful partial-wave execution and recovery surfaces</name>
  <files>
    src/commands/init.js
    src/commands/workspace.js
    workflows/execute-phase.md
    tests/workflow.test.cjs
    tests/workspace.test.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/workflow.test.cjs tests/workspace.test.cjs</red>
    <green>npm run build && node --test tests/workflow.test.cjs tests/workspace.test.cjs</green>
    <refactor>node --test tests/workflow.test.cjs tests/workspace.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    Operators can inspect and rerun partial-wave recovery from truthful status surfaces instead of reconstructing state from raw artifacts.

    Cases:
      - execution reports healthy finalized siblings, staged-ready healthy siblings, and recovery-needed blockers separately
      - one canonical recovery summary is the first inspection surface and names the exact gating sibling plus next command
      - recovery/finalize reruns happen from trusted main-checkout state after blockers are resolved
  </behavior>
  <implementation>
    Keep this slice focused on execution/inventory truthfulness and recovery summary discoverability. Do not expand into new cmux UX, generalized task management, or broader workspace orchestration beyond the locked Phase 184 contract.
  </implementation>
</feature>

<verification>
```bash
npm run build && node --test tests/workflow.test.cjs tests/workspace.test.cjs
```
</verification>

<success_criteria>
- [ ] Execution and inventory outputs separate finalized, staged-ready, and recovery-needed sibling states clearly
- [ ] Operators can inspect one canonical recovery summary that directly names the gating sibling, next command, and deeper proof links
- [ ] Workflow guidance teaches trusted-main finalize/recovery reruns and avoids implying that blocked siblings require healthy work to be re-executed
</success_criteria>

<output>
After completion, create `.planning/phases/184-deterministic-finalize-partial-wave-recovery/184-03-SUMMARY.md`
</output>
