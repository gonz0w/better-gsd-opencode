---
phase: 184-deterministic-finalize-partial-wave-recovery
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/jj-workspace.js
  - src/commands/workspace.js
  - tests/workspace-ownership.test.cjs
  - tests/workspace.test.cjs
autonomous: true
requirements:
  - FIN-02
  - FIN-04
verification_route: full
verification_route_reason: "Defines sibling health classification, staged-ready status, and canonical recovery-summary metadata for JJ workspace finalize/recovery behavior."
outcome_ids: []
must_haves:
  truths:
    - "Healthy sibling workspaces still report usable reconcile status when an earlier sibling is stale, divergent, proof-missing, or finalize-failed."
    - "Later healthy siblings stop at a staged-ready state behind the first earlier unhealthy sibling instead of promoting shared state out of order."
    - "One canonical recovery summary names the current status, gating sibling, blocking reason, next command, and deeper proof links for recovery-first inspection."
  artifacts:
    - path: "src/lib/jj-workspace.js"
      provides: "Wave-aware staged-ready and recovery-summary metadata helpers"
      contains: "createWaveRecoverySummary"
    - path: "src/commands/workspace.js"
      provides: "Workspace reconcile/status output with staged-ready and gating-sibling fields"
      contains: "gating_sibling"
    - path: "tests/workspace.test.cjs"
      provides: "Regression coverage for stale/divergent sibling recovery reporting"
      contains: "staged_ready"
  key_links:
    - from: "src/commands/workspace.js"
      to: "src/lib/jj-workspace.js"
      via: "Reconcile output reuses one wave-aware manifest and recovery-summary generator instead of hand-built status prose"
      pattern: "createWaveRecoverySummary|gating_sibling|staged_ready"
    - from: "src/lib/jj-workspace.js"
      to: "tests/workspace.test.cjs"
      via: "JJ stale/divergent diagnostics map to explicit recovery commands and proof links in the canonical summary"
      pattern: "workspace update-stale|op log|proof_artifacts"
---

<objective>
Define the wave-aware sibling status and recovery-summary contract that Phase 184 needs before any deterministic shared-state promotion can happen.

Purpose: This phase only works if healthy siblings remain inspectable and reusable when another sibling blocks promotion. The repo needs one normalized staged-ready and recovery-needed model that names the gating sibling, preserves already-healthy work, and points operators to trusted JJ-backed recovery evidence.
Output: wave-aware reconcile metadata, a canonical recovery-summary contract, and focused regressions that lock the sibling-status taxonomy.
</objective>

> **TDD Decision:** Selected — sibling status classification, staged-ready gating, and recovery-summary contents are deterministic behavior with clear observable outcomes, so RED coverage should lock the contract before implementation.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
@/Users/cam/.config/opencode/skills/tdd-execution/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/184-deterministic-finalize-partial-wave-recovery/184-CONTEXT.md
@.planning/phases/184-deterministic-finalize-partial-wave-recovery/184-RESEARCH.md
@.planning/phases/183-plan-local-workspace-ownership/183-02-SUMMARY.md
@src/lib/jj-workspace.js
@src/commands/workspace.js
@tests/workspace-ownership.test.cjs
@tests/workspace.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED coverage for staged-ready sibling states and the canonical recovery summary</name>
  <files>
    tests/workspace-ownership.test.cjs
    tests/workspace.test.cjs
  </files>
  <action>
  Add failing tests that make the locked Phase 184 status contract executable before changing runtime code.

  Cover the healthy-but-blocked case explicitly: when an earlier sibling is stale, divergent, proof-missing, or finalize-failed, a later healthy sibling must remain inspectable as `staged_ready` and must surface the exact earlier gating sibling instead of promoting shared state. Lock the canonical recovery summary too: it must include current status, blocking reason, next command, deeper proof links, and the gating sibling, with trusted-main rerun framing rather than workspace-local mutation guidance.
  </action>
  <verify>node --test tests/workspace-ownership.test.cjs tests/workspace.test.cjs</verify>
  <done>The new tests fail on current code because sibling-aware staged-ready status and canonical recovery-summary fields are not yet emitted by the workspace inspection/reconcile path.</done>
</task>

<task type="auto">
  <name>Task 2: Implement wave-aware reconcile metadata and recovery-summary generation</name>
  <files>
    src/lib/jj-workspace.js
    src/commands/workspace.js
    tests/workspace-ownership.test.cjs
    tests/workspace.test.cjs
  </files>
  <action>
  Extend the JJ workspace helpers so sibling health is normalized into one reusable contract for later deterministic finalize work.

  Add explicit blocker taxonomy fields for stale, divergent, proof-missing, quarantine, and finalize-failed states; represent later healthy siblings behind the first blocker as `staged_ready`; and generate one canonical recovery summary payload that carries `status`, `gating_sibling`, `blocking_reason`, `next_command`, and proof-artifact links. Keep the recovery/finalize rerun story anchored to trusted main-checkout state, and keep workspace reconcile preview-only while returning the enriched metadata for inspection.
  </action>
  <verify>npm run build</verify>
  <done>Workspace inspection and reconcile now emit wave-aware staged-ready/recovery-needed metadata plus one canonical recovery summary that directly names the gating sibling and next recovery action.</done>
</task>

</tasks>

<feature>
  <name>Wave-aware sibling recovery contract</name>
  <files>
    src/lib/jj-workspace.js
    src/commands/workspace.js
    tests/workspace-ownership.test.cjs
    tests/workspace.test.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/workspace-ownership.test.cjs tests/workspace.test.cjs</red>
    <green>npm run build && node --test tests/workspace-ownership.test.cjs tests/workspace.test.cjs</green>
    <refactor>node --test tests/workspace-ownership.test.cjs tests/workspace.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    Healthy sibling work stays inspectable and reusable even when another sibling blocks wave promotion.

    Cases:
      - later healthy siblings behind the first earlier blocker report `staged_ready` instead of looking finalized
      - stale, divergent, proof-missing, quarantine, and finalize-failed blockers map to explicit blocking reasons and next commands
      - one canonical recovery summary surfaces the gating sibling, current status, deeper proof links, and trusted-main rerun guidance
  </behavior>
  <implementation>
    Reuse `src/lib/jj-workspace.js` and `src/commands/workspace.js` as the single source of JJ-backed workspace health truth. Do not invent a new recovery engine, task manager, or cmux-specific status layer in this slice.
  </implementation>
</feature>

<verification>
```bash
npm run build && node --test tests/workspace-ownership.test.cjs tests/workspace.test.cjs
```
</verification>

<success_criteria>
- [ ] Healthy siblings remain inspectable as `staged_ready` when an earlier sibling blocks shared-state promotion
- [ ] The first blocking sibling is surfaced explicitly in reconcile/recovery metadata instead of inferred from plan order
- [ ] One canonical recovery summary includes current status, blocking reason, next command, proof links, and trusted-main rerun context
</success_criteria>

<output>
After completion, create `.planning/phases/184-deterministic-finalize-partial-wave-recovery/184-01-SUMMARY.md`
</output>
