# Phase 173 Milestone Audit

## Method and Scope

This is the canonical six-pass milestone audit for Phase 173. It is the only findings ledger for this phase and is intended to stay execution-first: later cleanup plans should lift finding IDs from this file instead of rebuilding separate review notes.

### Locked review method

- **six-pass coverage:** dead-code, duplication, simplification, concurrency, error-handling, hygiene.
- **evidence policy:** prefer repo-local audit artifacts (`audit:dead-code`, `audit:exports`, `audit:circular`, `audit:commands`) plus direct source reads and hotspot counts; tool output can nominate candidates but does not become truth without file-backed notes.
- **hybrid classification model:** each finding records `action_bucket` plus explicit `confidence` so proven delete candidates stay distinct from proof-needed suspects.
- **pass-tag dedupe rule:** each cleanup unit appears once in the ledger with merged `pass_tags`; repeated per-pass hotspot sections are intentionally forbidden.
- **stable `finding_id` convention:** use `AUD-###` identifiers in milestone order and keep the ID stable even if confidence, notes, or sequencing guidance changes later.
- **stage-gate handling:** `recommended_stage_gate` is reserved for plan 02 sequencing work; task 01 only locks the column and placeholder usage.

### Evidence inputs used by this audit

- `package.json` audit scripts: `audit:dead-code`, `audit:exports`, `audit:circular`, `audit:commands`
- Locked phase context: `173-CONTEXT.md`, `173-RESEARCH.md`, roadmap, requirements, intent docs
- Direct source inspection of router, command, output, and plugin hotspot files named in the plan

## Canonical Findings Ledger

| finding_id | hotspot | files | pass_tags | action_bucket | confidence | blast_radius | evidence_strength | sequencing_dependency | recommended_stage_gate | notes |
|------------|---------|-------|-----------|---------------|------------|--------------|-------------------|-----------------------|------------------------|-------|
| AUD-001 | Plugin source tree looks unused beside shipped bundle, but current host-loading contract is not yet proven | `plugin.js`, `src/plugin/command-enricher.js`, `src/plugin/tool-availability.js`, `src/plugin/index.js`, `src/plugin/node-runtime.js` | dead-code, duplication, hygiene | validate_before_delete | MEDIUM | high | `audit:dead-code` lists multiple `src/plugin/*` files as unused while `plugin.js` exports the same concepts and install/deploy flows still ship bundle artifacts | Needs explicit build, bundle-entry, and host-loader proof before any plugin-surface deletion can be promoted beyond suspect status | Stage 2: Validation-required cleanup | Stage as `validate_before_delete`: static signals are strong, but packaging proof must land before maintainers touch bundled plugin inputs. |
| AUD-002 | One truly dead lifecycle export is still retained | `src/lib/lifecycle.js` | dead-code, hygiene | safe_delete | HIGH | low | `audit:exports` classifies exactly one `truly_dead` export: default export in `src/lib/lifecycle.js` | Independent low-blast-radius delete once import parity is rechecked on the touched file; no later cleanup gate must move first | Stage 1: Proven dead or legacy-only cleanup | Stage as `safe_delete`: this is the cleanest first-pass removal because evidence is specific, isolated, and not tied to router or shared-state behavior. |
| AUD-003 | Command surface documentation and workflow references have drifted far below registered CLI surface | `.planning/baselines/audit/command-reference-map.json`, `commands/`, `workflows/`, `src/router.js`, `src/lib/commandDiscovery.js` | dead-code, duplication, simplification, hygiene | simplify_consolidate | MEDIUM | medium | `audit:commands` reports 281 CLI commands but only 40 markdown-referenced commands, leaving 241 orphaned surfaces to reconcile before removals | Wait until proven-dead removals and proof-required delete decisions reduce ambiguity, then simplify live command docs/help around the supported surface | Stage 3: Live-surface simplification | Stage after low-risk deletions: this is live-surface cleanup, not a blind delete, and it should not outrun the command-definition decisions it depends on. |
| AUD-004 | Router argument parsing is a concentrated duplication and blast-radius hotspot | `src/router.js` | duplication, simplification, error-handling, hygiene | high_risk_refactor_defer | HIGH | high | Direct count from current source: 1658-line router with 127 `indexOf('--...')` scans, 18 `includes('--...')` checks, and direct `process.argv` ownership | Must wait for earlier low-blast-radius cleanup and command-surface consolidation to reduce router responsibilities before any structural edit is attempted | Stage 4: High-risk structural cleanup | Stage 4 on purpose: `src/router.js` is a proven hotspot, but moving it first would front-load the highest CLI blast radius. |
| AUD-005 | Init-time execution context is assembled in parallel paths instead of one canonical source | `src/commands/init.js`, `src/plugin/command-enricher.js`, `src/plugin/project-state.js` | duplication, simplification, hygiene | simplify_consolidate | HIGH | medium | `src/commands/init.js` (2275 lines) and `src/plugin/command-enricher.js` (976 lines) both derive overlapping phase, model, plan, and tool-availability context from `.planning/` state | Safer after Stage 1 and Stage 2 narrow which context consumers still matter, but before Stage 4 structural surgery on router or globals | Stage 3: Live-surface simplification | Stage 3 keeps this consolidation behind lower-risk cleanup while still landing before router/global rewrites that depend on clearer context ownership. |
| AUD-006 | CLI output mode depends on ambient global mutable state shared across startup and command handlers | `src/router.js`, `src/lib/output.js`, `src/commands/features.js`, `src/commands/init.js` | concurrency, error-handling, hygiene, simplification | high_risk_refactor_defer | HIGH | high | Repo scan shows 62 `global._gsd*` references; router sets output globals, `src/lib/output.js` reads them, and `src/commands/features.js` temporarily flips compact-mode globals during helper execution | Wait until earlier cleanup narrows the supported command and output surface, then pair the refactor with focused regression proof on user-visible formatting paths | Stage 4: High-risk structural cleanup | Ambient-global removal is explicitly late-stage work: the risk is real, but the current shared state touches visible CLI behavior and should not precede easier reductions. |
| AUD-007 | State mutation still crosses a circular dependency boundary | `src/commands/state.js`, `src/lib/state-session-mutator.js` | concurrency, simplification, hygiene | high_risk_refactor_defer | HIGH | medium | `audit:circular` reports the only current source cycle: `commands/state.js > lib/state-session-mutator.js`; source read shows `state-session-mutator` requiring `../commands/state` back at line 387 | Best addressed after low-risk cleanup and live-surface simplification reduce adjacent noise around state/session flows | Stage 4: High-risk structural cleanup | Keep late: the cycle is real, but state mutation underpins roadmap and session metadata, so untangling it belongs behind earlier blast-radius reduction. |
| AUD-008 | Silent fallback and swallowed-error patterns obscure when planning context degraded | `src/router.js`, `src/commands/init.js`, `src/plugin/command-enricher.js`, `src/commands/state.js` | error-handling, hygiene, simplification | simplify_consolidate | MEDIUM | medium | Direct grep found many silent `catch {}` or non-fatal fallbacks, especially in `src/plugin/command-enricher.js` and state/init paths that build execution context | Resolve after proof-required deletes and context-surface cleanup clarify which fallbacks are real resilience versus compatibility debt | Stage 3: Live-surface simplification | Stage 3 fits because this is still live behavior cleanup, but it should not be mixed with Stage 1 deletes or Stage 4 structural rewrites. |
| AUD-009 | Oversized bucket modules concentrate unrelated responsibilities and inflate cleanup blast radius | `src/commands/features.js`, `src/commands/init.js`, `src/commands/phase.js`, `src/router.js`, `src/commands/verify.js`, `src/commands/misc.js` | simplification, hygiene, duplication | high_risk_refactor_defer | HIGH | high | Repo-wide count shows 188 JS files with 18 files over 1000 lines; largest hotspots include `src/commands/features.js` (2195), `src/commands/init.js` (2275), `src/commands/misc.js` (2990), `src/commands/verify.js` (3384), and `src/router.js` (1658) | Requires Stage 1 and Stage 2 reductions plus Stage 3 surface simplification before splitting modules with many command-entry dependencies | Stage 4: High-risk structural cleanup | Oversized command-module breakup is intentionally late-stage because maintainability pain alone is not enough reason to start with the widest blast radius. |
| AUD-010 | Direct `process.argv` reads remain outside the router boundary | `src/commands/init.js`, `src/commands/research.js`, `src/lib/debug.js`, `src/lib/error.js`, `src/lib/format.js`, `src/lib/plugin-paths.js`, `src/plugin/node-runtime.js`, `src/router.js` | simplification, concurrency, error-handling, hygiene | simplify_consolidate | MEDIUM | medium | Current repo scan found 20 `process.argv` references, including 6 in `src/commands/init.js`, plus extra direct reads in research/debug/format/plugin runtime files | Wait until command-definition cleanup and earlier surface reduction clarify which flags remain router-owned before collapsing direct argv reads | Stage 4: High-risk structural cleanup | Keep direct-argv cleanup late with router work: it is a real hotspot family, but the contract risk is too high for an early annoyance-driven refactor. |

## Stage Gates

| Stage Gate | Can Start When | Includes | Must Exclude | Why this boundary exists |
|------------|----------------|----------|--------------|--------------------------|
| Stage 1: Proven dead or legacy-only cleanup | Routing, help, import, or packaging evidence agrees the surface is dead, legacy-only, or already superseded | Safe low-blast-radius deletions, stale references, and isolated removals whose contracts are already proven absent | Router rewrites, command-structure breakups, shared mutable state changes, or any suspect that still needs runtime proof | This stage reduces noise and blast radius first so later plans reason about a smaller supported surface. |
| Stage 2: Validation-required cleanup | A targeted proof plan exists for each suspect and the touched contract can be checked without widening into milestone-wide regression reruns | `validate_before_delete` candidates and other proof-required removals that look dead statically but still need regression, routing, help, docs, or packaging confirmation | High-coupling refactors, speculative deletions, or live-surface simplification that depends on unresolved contract questions | This boundary keeps static-analysis suspects from being promoted to safe deletes before maintainers have proof. |
| Stage 3: Live-surface simplification | Stage 1 removed proven dead surfaces and Stage 2 resolved proof-required deletes, leaving a clearer supported command and context surface | Simplify/consolidate work on still-live paths, duplicate metadata, repeated helper logic, and other medium-blast-radius cleanup that preserves behavior | Router parsing surgery, ambient-global removal, oversized bucket decomposition, or redesign disguised as cleanup | This stage is for behavior-preserving cleanup on live code after earlier deletions reduce ambiguity about what still matters. |
| Stage 4: High-risk structural cleanup | Lower-risk stages already landed and reduced the touched surface enough to justify focused regression proof on the remaining hotspots | Router, ambient-global, direct argv, circular-dependency, and oversized command-module work with high coupling or user-visible blast radius | New feature work, speculative redesign, or early-stage deletes that should have landed already | This boundary deliberately pushes the most coupled cleanup late so maintainers do not start with the riskiest CLI surgery out of annoyance. |

- **Can Start When** means the prerequisite that must already be true before planning work from that stage.
- **Must Exclude** is a hard guardrail: if a finding still matches those exclusions, it belongs in a later stage even if it looks tempting to clean up now.
- **Inside-stage order:** keep ordering loose unless one finding explicitly blocks another; these gates are safety boundaries, not fake exact waves.

## Coverage Notes

- **validate_before_delete set:** `AUD-001` and any plugin-source removal candidate still need bundle-entry, host-loader, and deploy/install proof before cleanup. `AUD-003` needs command/help/docs parity checks so unsupported aliases are not confused with merely undocumented supported flows.
- **Contract-sensitive cleanup:** `AUD-010` and `AUD-008` need regression or smoke proof around CLI flags, compact/json output mode, and state/session mutations before converting direct reads or deleting fallback branches.
- **Thin-evidence areas:** static analysis strongly identifies dead surfaces and structural hotspots, but this phase intentionally did not promote dynamic behavior claims beyond repo-local evidence. Anything not already `safe_delete` should be treated as proof-needed, not pre-approved cleanup.
- **Milestone-local boundary:** this audit captures where cleanup pressure exists; it does not authorize command redesign, plugin architecture replacement, or behavior changes during Phase 173 plan 01.

## Deferred Adjacent Work

- Carry these stage gates and per-finding placements into Phase 174-176 cleanup plans instead of re-deriving sequencing from scratch once execution work begins.
- Convert high-confidence findings into execution plans that group low-blast-radius deletes separately from router/global-state refactors instead of batching the whole audit into one cleanup pass.
- Re-run targeted regression proof only for the touched command families when Phase 174-176 cleanup starts; avoid broad milestone-wide revalidation unless a later plan explicitly asks for it.

## Recommended Order of Operations

- Start with the low-blast-radius `safe_delete` work in Stage 1 so clearly dead or legacy-only surfaces can be removed without dragging router or command-structure risk into the first cleanup pass.
- Promote `validate_before_delete` findings only after targeted proof exists; Stage 2 is where packaging, routing, help, docs, and regression checks decide whether a suspect really graduates from evidence-backed candidate to removal-ready work.
- Use Stage 3 for live-surface simplification once the supported surface is clearer, especially duplicate context assembly, command-reference drift, and fallback cleanup that should not be mixed with speculative deletes.
- Leave router, ambient-global, direct-argv, circular-dependency, and oversized command-module work for Stage 4, where focused proof can justify touching the highest-coupling hotspots after earlier cleanup has already reduced blast radius.
