---
phase: 138-workflow-agent-tool-routing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/bgsd-executor.md
  - agents/bgsd-debugger.md
  - agents/bgsd-codebase-mapper.md
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - ROUTE-03
outcome_ids:
  - DO-101
  - DO-100

must_haves:
  truths:
    - "Executor agent system prompt includes a Preferred Commands section that maps tool_availability booleans to concrete CLI commands for search, discovery, and JSON transforms"
    - "Debugger agent system prompt includes search-mode guidance in the investigation section, with concrete ripgrep commands when available and node-based alternatives when not"
    - "Codebase mapper agent system prompt has a Preferred Commands preamble with resolved file discovery (fd) and content search (rg) commands, and the explore_codebase code blocks reference these instead of hardcoded find/grep"
  artifacts:
    - path: "agents/bgsd-executor.md"
      provides: "Tool-aware Preferred Commands section adapting to tool_availability"
      contains: "tool_routing, Preferred Commands, tool_availability"
    - path: "agents/bgsd-debugger.md"
      provides: "Search-mode guidance for investigation with tool-aware commands"
      contains: "tool_routing, search-mode, ripgrep"
    - path: "agents/bgsd-codebase-mapper.md"
      provides: "Preferred Commands preamble with resolved fd/rg commands and updated explore_codebase blocks"
      contains: "Preferred Commands, fd, rg, tool_availability"
  key_links:
    - from: "agents/bgsd-executor.md"
      to: "bgsd-context tool_availability"
      via: "Reads tool_availability from bgsd-context to select concrete commands"
      pattern: "tool_availability|Preferred Commands"
    - from: "agents/bgsd-debugger.md"
      to: "bgsd-context tool_availability"
      via: "Reads search-mode to select ripgrep vs node-based search in investigation"
      pattern: "tool_availability|search-mode|ripgrep"
    - from: "agents/bgsd-codebase-mapper.md"
      to: "bgsd-context tool_availability"
      via: "Reads tool_availability for fd/rg availability, explore_codebase blocks use resolved commands"
      pattern: "tool_availability|fd|rg|Preferred Commands"
---

<objective>
Add tool-aware Preferred Commands sections to 3 agent system prompts so they emit concrete CLI commands adapted to available tools.

Purpose: Agent system prompts currently have no awareness of which CLI tools are available. After this plan, the executor, debugger, and mapper agents read tool_availability from bgsd-context and use resolved concrete commands (fd, rg, jq) when available, falling back to MCP tools and node-based alternatives when not.
Output: 3 agent files updated with tool_routing sections using the resolved-command-table pattern from the context decisions.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/138-workflow-agent-tool-routing/138-CONTEXT.md
@agents/bgsd-executor.md
@agents/bgsd-debugger.md
@agents/bgsd-codebase-mapper.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Preferred Commands section to bgsd-executor.md</name>
  <files>agents/bgsd-executor.md</files>
  <action>
Add a `<tool_routing>` section to the executor agent system prompt. Insert it after the `<skill:project-context />` tag (around line 45) and before `<execution_flow>`.

The section should:

**1. Read tool_availability from bgsd-context:**
Instruct the agent to check `tool_availability` from the `<bgsd-context>` block for available CLI tools.

**2. Provide a Preferred Commands table with resolved concrete commands:**

```markdown
<tool_routing>
## Preferred Commands

Read `tool_availability` from `<bgsd-context>` to determine available CLI tools. Use the first available option:

| Operation | When tool available | Fallback |
|-----------|-------------------|----------|
| File discovery | `fd -e ts -e tsx -e js` (fd) | Glob MCP tool |
| Content search | `rg "pattern" --type ts` (ripgrep) | Grep MCP tool |
| JSON processing | `jq '.field'` (jq) | `node -e "..."` |
| YAML processing | `yq '.field'` (yq) | `node -e "..."` |
| File viewing | `bat --plain file.ts` (bat) | Read MCP tool |

Use resolved commands from the table — no if/else conditionals in your execution. If a resolved tool unexpectedly fails at runtime, fall back to the alternative through normal reasoning.
</tool_routing>
```

The table follows the context decision: "concrete resolved commands that match training data" with a single fallback column. Body of the agent prompt should not need changes since it already uses abstract instructions ("search codebase", "read files") that the agent resolves using this table.
  </action>
  <verify>
Read agents/bgsd-executor.md and confirm:
1. A `<tool_routing>` section exists after `<skill:project-context />` and before `<execution_flow>`
2. The section contains a Preferred Commands table with operations: file discovery, content search, JSON processing, YAML processing, file viewing
3. Each row has a concrete "when tool available" command and a fallback
4. The section references `tool_availability` from `<bgsd-context>`
  </verify>
  <done>Executor agent system prompt includes a tool usage section that adapts based on tool_availability with specific CLI commands for search, discovery, and JSON transforms</done>
</task>

<task type="auto">
  <name>Task 2: Add search-mode guidance to bgsd-debugger.md investigation section</name>
  <files>agents/bgsd-debugger.md</files>
  <action>
Add a `<tool_routing>` section to the debugger agent system prompt. Insert it after the `<skill:project-context />` tag (around line 53) and before `<philosophy>`.

The section should focus on **search-mode guidance for investigation**, since the debugger's primary tool need is content search during the investigation_loop (lines 285-312).

```markdown
<tool_routing>
## Preferred Commands for Investigation

Read `tool_availability` from `<bgsd-context>` to determine available CLI tools. During investigation:

| Operation | When tool available | Fallback |
|-----------|-------------------|----------|
| Search for error text | `rg "error text" --type ts -C 3` (ripgrep) | Grep MCP tool |
| Find related files | `fd "pattern" -e ts -e tsx` (fd) | Glob MCP tool |
| Search stack traces | `rg "function_name" --type ts -l` (ripgrep) | Grep MCP tool |
| View file context | `bat --plain -r 50:100 file.ts` (bat) | Read MCP tool |

For investigation_loop searches: prefer ripgrep when available — it provides context lines (-C), file-only listing (-l), and type filtering (--type) that accelerate root cause identification.
</tool_routing>
```

The debugger's investigation_loop already uses abstract instructions like "search codebase for error text" and "Identify relevant code area" — the tool_routing section tells it WHICH concrete commands to use for these abstract operations.
  </action>
  <verify>
Read agents/bgsd-debugger.md and confirm:
1. A `<tool_routing>` section exists after `<skill:project-context />` and before `<philosophy>`
2. The section contains investigation-focused Preferred Commands (search for error text, find related files, search stack traces)
3. Each row references ripgrep/fd with concrete flags and a fallback
4. The section mentions `tool_availability` from `<bgsd-context>`
  </verify>
  <done>Debugger agent system prompt includes search-mode guidance for investigation with ripgrep commands when available and node-based search when not</done>
</task>

<task type="auto">
  <name>Task 3: Add Preferred Commands preamble and update explore_codebase in bgsd-codebase-mapper.md</name>
  <files>agents/bgsd-codebase-mapper.md</files>
  <action>
This is the highest-impact change — the mapper agent has 18+ hardcoded CLI commands (8x grep, 3x find, 7x ls) in the explore_codebase step (lines 107-157).

**1. Add a Preferred Commands preamble (after `<skill:project-context />` around line 42, before `<why_this_matters>`):**

```markdown
<tool_routing>
## Preferred Commands

Read `tool_availability` from `<bgsd-context>` to determine available CLI tools.

| Operation | When tool available | Fallback |
|-----------|-------------------|----------|
| File discovery | `fd -e ts -e tsx --type f` (fd) | Glob MCP tool |
| Directory listing | `fd --type d --max-depth 3` (fd) | `ls` or Glob tool |
| Content search | `rg "pattern" --type ts -l` (ripgrep) | Grep MCP tool |
| Import analysis | `rg "^import" --type ts` (ripgrep) | Grep MCP tool |
| Code metrics | `rg "TODO\|FIXME\|HACK" --type ts -c` (ripgrep) | Grep MCP tool |
| File reading | `bat --plain file.ts` (bat) | Read MCP tool |

Use resolved commands from the table in the explore_codebase blocks below. If a tool is not available per bgsd-context, use the fallback.
</tool_routing>
```

**2. Update the explore_codebase code blocks (lines 107-157) to reference the Preferred Commands:**

Replace hardcoded commands with references to the preamble pattern. For each focus area:

- **Tech stack (lines 107-118):** Replace `grep -r "import.*stripe" src/ --include="*.ts"` with "Use content search from Preferred Commands to find third-party imports". The actual command (`rg` or Grep tool) comes from the preamble.
- **Architecture (lines 120-130):** Replace `find . -type d -not -path '*/node_modules/*'` with "Use directory listing from Preferred Commands to discover project structure" and `grep -r "^import" src/` with "Use import analysis from Preferred Commands".
- **Quality (lines 132-144):** Replace `find . -name "*.test.*" -o -name "*.spec.*"` with "Use file discovery from Preferred Commands to find test files" and `find src/ -name "*.ts" | xargs wc -l` with "Use file discovery + wc for size metrics".
- **Concerns (lines 146-157):** Replace `grep -rn "TODO\|FIXME\|HACK" src/` with "Use code metrics from Preferred Commands to find TODO/FIXME markers" and `grep -rn "return null" src/` with "Use content search for stub patterns".

Keep the existing `grep -E '(sk-[a-zA-Z0-9]{20,}|...'` at line 80 (secrets scan) unchanged — it's a security check that should always use grep directly.

The pattern is: preamble has concrete commands, body has descriptive references that the agent resolves using the preamble. This follows the context decision: "Single Preferred Commands section at top with resolved commands... body references these commands instead of hardcoding find/grep/cat."
  </action>
  <verify>
Read agents/bgsd-codebase-mapper.md and confirm:
1. A `<tool_routing>` section exists with a Preferred Commands table after `<skill:project-context />`
2. The explore_codebase code blocks reference "Preferred Commands" instead of hardcoding find/grep for general search
3. The secrets scan grep at line 80 is unchanged
4. The Preferred Commands table includes: file discovery, directory listing, content search, import analysis, code metrics, file reading
5. Each row has concrete fd/rg commands and fallbacks
  </verify>
  <done>Codebase mapper agent has Preferred Commands preamble with resolved fd/rg commands and explore_codebase blocks reference these instead of hardcoded find/grep</done>
</task>

</tasks>

<verification>
After all 3 tasks, verify the complete agent routing surface:
1. `bgsd-executor.md` — has tool_routing section with Preferred Commands table (5 operations: discovery, search, JSON, YAML, file viewing)
2. `bgsd-debugger.md` — has tool_routing section focused on investigation (4 operations: error search, file finding, stack traces, file context)
3. `bgsd-codebase-mapper.md` — has Preferred Commands preamble AND updated explore_codebase blocks referencing the preamble
4. All 3 agents read tool_availability from bgsd-context (no hardcoded tool assumptions)
5. All command tables use concrete resolved commands with fallbacks (matching context decision)
6. No if/else conditionals in agent prompt bodies — all routing through the Preferred Commands table
</verification>
