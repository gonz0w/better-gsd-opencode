---
phase: 81-safe-adoption-controls-and-regression-parity
plan: 02
type: execute
wave: 2
depends_on:
  - 81-01
files_modified:
  - src/lib/parsers/plan-parser.js
  - src/lib/parsers/state-parser.js
  - src/lib/parsers/roadmap-parser.js
  - src/lib/parsers/phase-parser.js
autonomous: true
requirements:
  - SAFE-02
intent:
  outcome_ids:
    - DO-32
    - DO-38
  rationale: "Backward compatibility verification ensures existing .planning/ artifacts remain usable after dependency migrations, protecting user data."
must_haves:
  truths:
    - "All .planning/ artifact parsers accept both old and new format variants."
    - "Parser errors gracefully indicate format issues without breaking workflows."
    - "Migration documentation exists for any format changes."
  artifacts:
    - path: src/lib/parsers/plan-parser.js
      provides: "Backward-compatible PLAN.md parser"
      contains: "parse"
    - path: src/lib/parsers/state-parser.js
      provides: "Backward-compatible STATE.md parser"
      contains: "parse"
    - path: src/lib/parsers/roadmap-parser.js
      provides: "Backward-compatible ROADMD.md parser"
      contains: "parse"
    - path: src/lib/parsers/phase-parser.js
      provides: "Backward-compatible phase directory parser"
      contains: "parse"
  key_links:
    - from: src/lib/config.js
      to: src/lib/parsers/
      via: "Config may read .planning/ artifacts"
      pattern: "parse"
    - from: src/commands/init.js
      to: src/lib/parsers/
      via: "Init reads planning artifacts"
      pattern: "parse"
---

<objective>
Verify and ensure backward compatibility for all .planning/ artifact parsers after dependency migrations.

Purpose: Users can rely on existing .planning/ artifacts remaining compatible after Phase 77-80 dependency migrations.
Output: Test coverage for parser compatibility, error handling improvements, and compatibility documentation.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/81-safe-adoption-controls-and-regression-parity/81-RESEARCH.md
@src/lib/parsers/plan-parser.js
@src/lib/parsers/state-parser.js
@src/lib/parsers/roadmap-parser.js
@src/lib/parsers/phase-parser.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory all .planning/ artifact parsers and their format contracts</name>
  <files>src/lib/parsers/plan-parser.js, src/lib/parsers/state-parser.js, src/lib/parsers/roadmap-parser.js, src/lib/parsers/phase-parser.js</files>
  <action>Read each parser file to identify:
    1. What format variants it currently accepts
    2. What fields are required vs optional
    3. What validation is performed
    4. How errors are reported
  Create a compatibility matrix documenting each parser's contract.</action>
  <verify>ls -la src/lib/parsers/</verify>
  <done>All .planning/ artifact parsers identified and their format contracts documented.</done>
</task>

<task type="auto">
  <name>Task 2: Verify existing .planning/ artifacts parse correctly</name>
  <files>.planning/PLAN.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>Test that the current project's .planning/ artifacts (PLAN.md, STATE.md, ROADMAP.md) parse correctly with existing parsers. Verify each parser handles:
    - Missing optional fields
    - Extra unknown fields
    - Type variations (string vs array for single vs multi-value)
    - Legacy format variants from older bGSD versions</action>
  <verify>node bin/bgsd-tools.cjs init:check</verify>
  <done>All existing .planning/ artifacts parse without errors.</done>
</task>

<task type="auto">
  <name>Task 3: Add backward compatibility test coverage</name>
  <files>tests/parsers.test.cjs</files>
  <action>Create or extend a test file that validates parser backward compatibility by:
    1. Testing parsers with minimal/legacy format inputs
    2. Testing parsers with extra unknown fields
    3. Testing parsers with type variations
    4. Verifying error messages are helpful, not breaking</action>
  <verify>npm run test:file -- tests/parsers.test.cjs 2>/dev/null || echo "No dedicated parser tests - verify via npm test"</verify>
  <done>Backward compatibility test coverage exists for all major parsers.</done>
</task>

</tasks>

<verification>
- Run `npm test` to confirm no parser regressions.
- Manually test with a minimal .planning/ structure to verify graceful degradation.
- Verify error messages for malformed artifacts are actionable.
</verification>

<success_criteria>
- All .planning/ artifact parsers accept both old and new format variants.
- Parser errors provide helpful messages without breaking workflows.
- Backward compatibility test coverage exists.
</success_criteria>

<output>
After completion, create `.planning/phases/81-safe-adoption-controls-and-regression-parity/81-02-SUMMARY.md`.
</output>
