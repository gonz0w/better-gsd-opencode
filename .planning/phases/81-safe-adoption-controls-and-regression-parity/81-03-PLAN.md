---
phase: 81-safe-adoption-controls-and-regression-parity
plan: 03
type: execute
wave: 2
depends_on:
  - 81-01
files_modified:
  - src/lib/utils/parity-check.js
  - src/commands/util.js
  - src/lib/adapters/discovery.js
autonomous: true
requirements:
  - SAFE-03
intent:
  outcome_ids:
    - DO-32
    - DO-38
  rationale: "Generalized parity-check utility enables maintainers to validate dependency adoptions without functional regressions in core flows."
must_haves:
  truths:
    - "Parity check can validate all dependency-backed optimizations."
    - "Output format shows match/mismatch status with details."
    - "Command can run on-demand or as part of validation workflow."
  artifacts:
    - path: src/lib/utils/parity-check.js
      provides: "Generalized parity checking utility"
      contains: "checkParity"
    - path: src/commands/util.js
      provides: "bgsd-tools util:parity-check command"
      contains: "parity-check"
  key_links:
    - from: src/commands/util.js
      to: src/lib/utils/parity-check.js
      via: "Parity check command implementation"
      pattern: "checkParity"
    - from: src/lib/utils/parity-check.js
      to: src/lib/adapters/discovery.js
      via: "Reuses diagnoseParity pattern"
      pattern: "diagnoseParity"
---

<objective>
Create a generalized parity-check utility command that validates all dependency-backed optimizations have no functional regressions in core flows.

Purpose: Enable maintainers to run on-demand parity validation for each optimization using the proven diagnoseParity pattern from Phase 78.
Output: New parity-check utility module and bgsd-tools command.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/81-safe-adoption-controls-and-regression-parity/81-RESEARCH.md
@src/lib/adapters/discovery.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create generalized parity-check utility module</name>
  <files>src/lib/utils/parity-check.js</files>
  <action>Create src/lib/utils/parity-check.js that generalizes the diagnoseParity pattern from discovery.js:
    - Export checkParity(optimizationName, options) function
    - Support validation for: valibot, discovery, compile_cache, sqlite_cache
    - Return structured output: { optimization, match: boolean, details: {}, onlyLegacy: [], onlyOptimized: [] }
    - Include capability detection fallback for each optimization
    - Use normalizeFlag from flags.js for consistent boolean parsing</action>
  <verify>node -e "const {checkParity} = require('./src/lib/utils/parity-check.js'); console.log(typeof checkParity)"</verify>
  <done>Parity-check utility module exists with checkParity function.</done>
</task>

<task type="auto">
  <name>Task 2: Add valibot parity check implementation</name>
  <files>src/lib/utils/parity-check.js</files>
  <action>Implement valibot parity check that:
    - Runs validation with valibot engine and zod fallback engine on same inputs
    - Compares output contracts (success/error shape, parsed values)
    - Returns match status with any discrepancies</action>
  <verify>node -e "const {checkParity} = require('./src/lib/utils/parity-check.js'); checkParity('valibot').then(r => console.log(r.match))"</verify>
  <done>Valibot parity check validates both engines produce identical output contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Add discovery, compile_cache, sqlite_cache parity checks</name>
  <files>src/lib/utils/parity-check.js</files>
  <action>Implement remaining optimization parity checks:
    - discovery: Reuse diagnoseParity() from discovery.js
    - compile_cache: Check capability detection and verify fallback works
    - sqlite_cache: Verify statement cache produces same results as non-cached</action>
  <verify>node -e "const {checkParity} = require('./src/lib/utils/parity-check.js'); Promise.all(['discovery', 'compile_cache', 'sqlite_cache'].map(o => checkParity(o))).then(r => console.log(r.every(x => x !== null ? x.match : true)))"</verify>
  <done>All optimization parity checks implemented and functional.</done>
</task>

<task type="auto">
  <name>Task 4: Add bgsd-tools util:parity-check command</name>
  <files>src/commands/util.js</files>
  <action>Add util:parity-check command to src/commands/util.js:
    - Usage: bgsd-tools util:parity-check [--optimization <name>]
    - Without --optimization flag, checks all optimizations
    - Output format: JSON or human-readable summary
    - Exit code: 0 if all match, 1 if any mismatch</action>
  <verify>node bin/bgsd-tools.cjs util:parity-check --help</verify>
  <done>Parity-check command is registered and accessible.</done>
</task>

</tasks>

<verification>
- Run `node bin/bgsd-tools.cjs util:parity-check` to verify all optimizations check successfully.
- Run with specific optimization: `node bin/bgsd-tools.cjs util:parity-check --optimization valibot`
- Verify exit codes: 0 for match, 1 for mismatch
</verification>

<success_criteria>
- Generalized parity-check utility module exists with checkParity function.
- All four optimization types have parity check implementations.
- bgsd-tools util:parity-check command is registered and functional.
- Command returns proper exit codes based on parity match status.
</success_criteria>

<output>
After completion, create `.planning/phases/81-safe-adoption-controls-and-regression-parity/81-03-SUMMARY.md`.
</output>
