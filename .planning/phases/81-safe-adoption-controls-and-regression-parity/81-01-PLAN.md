---
phase: 81-safe-adoption-controls-and-regression-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/constants.js
  - src/lib/config.js
  - src/commands/misc.js
autonomous: true
requirements:
  - SAFE-01
intent:
  outcome_ids:
    - DO-32
    - DO-38
  rationale: "Centralizing optimization flags in CONFIG_SCHEMA enables deterministic control and verification of dependency-backed performance gains."
must_haves:
  truths:
    - "All dependency-backed optimizations are discoverable in a single location."
    - "Each optimization can be toggled via config file or environment variable."
    - "Default values are documented and follow safe-by-default principles."
  artifacts:
    - path: src/lib/constants.js
      provides: "Unified optimization flags registry in CONFIG_SCHEMA"
      contains: "optimization"
    - path: src/lib/config.js
      provides: "Config loading reads optimization flags from schema"
      contains: "optimization"
    - path: src/commands/misc.js
      provides: "Settings command displays optimization flags"
      contains: "optimization"
  key_links:
    - from: src/lib/config.js
      to: src/lib/constants.js
      via: "CONFIG_SCHEMA reference"
      pattern: "CONFIG_SCHEMA"
    - from: src/commands/misc.js
      to: src/lib/constants.js
      via: "CONFIG_SCHEMA reference for settings display"
      pattern: "CONFIG_SCHEMA"
---

<objective>
Create a unified optimization flags registry in CONFIG_SCHEMA that exposes all dependency-backed optimizations as first-class config options with environment variable fallbacks.

Purpose: Enable maintainers to discover and toggle each optimization independently from a single canonical location.
Output: Updated CONFIG_SCHEMA with optimization section, config loading support, and settings display.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/81-safe-adoption-controls-and-regression-parity/81-RESEARCH.md
@src/lib/constants.js
@src/lib/config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optimization flags section to CONFIG_SCHEMA</name>
  <files>src/lib/constants.js</files>
  <action>Add an `optimization` section to CONFIG_SCHEMA in src/lib/constants.js containing:
    - valibot: { type: 'boolean', default: true, env: 'BGSD_DEP_VALIBOT', description: 'Use valibot for schema validation' }
    - valibot_fallback: { type: 'boolean', default: false, env: 'BGSD_DEP_VALIBOT_FALLBACK', description: 'Force zod fallback for validation' }
    - discovery: { type: 'string', default: 'optimized', env: 'BGSD_DISCOVERY_MODE', values: ['optimized', 'legacy'], description: 'File discovery mode' }
    - compile_cache: { type: 'boolean', default: false, env: 'BGSD_COMPILE_CACHE', description: 'Enable Node.js compile-cache' }
    - sqlite_cache: { type: 'boolean', default: true, env: 'BGSD_SQLITE_STATEMENT_CACHE', description: 'SQLite statement caching' }
  Add a comment section: "// ─── Dependency-Backed Optimizations ───"</action>
  <verify>node -e "const {CONFIG_SCHEMA} = require('./src/lib/constants.js'); console.log('optimization' in CONFIG_SCHEMA ? 'PASS' : 'FAIL')"</verify>
  <done>CONFIG_SCHEMA contains optimization section with all four optimization flags.</done>
</task>

<task type="auto">
  <name>Task 2: Verify config loading handles optimization section</name>
  <files>src/lib/config.js</files>
  <action>Verify that loadConfig() in src/lib/config.js correctly reads and handles the nested optimization section from CONFIG_SCHEMA. Ensure optimization flags are accessible via getConfig('optimization.valibot') and similar paths.</action>
  <verify>node -e "const {loadConfig} = require('./src/lib/config.js'); const cfg = loadConfig(); console.log(typeof cfg.optimization)"</verify>
  <done>Config loading correctly parses and exposes optimization flags.</done>
</task>

<task type="auto">
  <name>Task 3: Update settings command to display optimization flags</name>
  <files>src/commands/misc.js</files>
  <action>Update the settings display in misc.js to include the optimization section. Ensure settings list shows all optimization flags with their current values, sources (config vs env var), and descriptions.</action>
  <verify>node bin/bgsd-tools.cjs settings | grep -c optimization</verify>
  <done>Settings command displays optimization flags in output.</done>
</task>

</tasks>

<verification>
- Run `npm test` to confirm no regressions from schema changes.
- Run `node bin/bgsd-tools.cjs settings` and verify all optimization flags appear.
- Test env var override: BGSD_DEP_VALIBOT=0 node bin/bgsd-tools.cjs settings | grep valibot
</verification>

<success_criteria>
- CONFIG_SCHEMA contains unified optimization section with all four dependency-backed optimization flags.
- Config loading correctly exposes optimization flags via getConfig().
- Settings command displays all optimization flags with values and descriptions.
</success_criteria>

<output>
After completion, create `.planning/phases/81-safe-adoption-controls-and-regression-parity/81-01-SUMMARY.md`.
</output>
