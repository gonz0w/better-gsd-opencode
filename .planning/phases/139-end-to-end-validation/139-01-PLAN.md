---
phase: 139-end-to-end-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/cli-tools-integration.test.cjs
  - tests/tool-routing-contract.test.cjs
autonomous: true
requirements:
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "E2E test runs decisions:evaluate for all 4 Chain B rules with tools-present and tools-absent state and asserts .value fields differ"
    - "Contract test dynamically identifies Chain B rules by filtering decisions:list for rules with tool_availability in inputs"
    - "Contract test proves every Chain B rule has at least one consumer in workflows/ or agents/ directories"
    - "All new tests pass via npm test"
  artifacts:
    - tests/cli-tools-integration.test.cjs (extended with E2E decision evaluation tests)
    - tests/tool-routing-contract.test.cjs (new contract test file)
  key_links:
    - "E2E tests call bin/bgsd-tools.cjs decisions:evaluate via execFileSync — validates the built CLI artifact"
    - "Contract test reads decisions:list JSON output and filters for tool_availability inputs to find Chain B rules dynamically"
    - "Contract test scans real repo files (workflows/*.md, agents/*.md) for decisions.{rule-id} or tool_availability patterns"
---

<objective>
Add E2E and contract tests that validate the full tool detection → decision evaluation → workflow consumption chain.

Purpose: Prove that Chain B decision rules (file-discovery-mode, search-mode, json-transform-mode, agent-capability-level) produce different outputs based on tool availability, and that every such rule has at least one workflow or agent consumer — zero orphaned decisions.

Output: Extended E2E tests in cli-tools-integration.test.cjs and new contract test file tool-routing-contract.test.cjs
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-end-to-end-validation/139-CONTEXT.md
@.planning/phases/138-workflow-agent-tool-routing/138-01-SUMMARY.md
@.planning/phases/138-workflow-agent-tool-routing/138-02-SUMMARY.md
@tests/cli-tools-integration.test.cjs
@src/lib/decision-rules.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add E2E decision evaluation tests to cli-tools-integration.test.cjs</name>
  <files>tests/cli-tools-integration.test.cjs</files>
  <action>
Add a new describe block at the end of `tests/cli-tools-integration.test.cjs`:

```
describe('E2E: Chain B decision evaluation — TEST-01', () => { ... })
```

Use the same `execFileSync` pattern from the existing `detect:gh-preflight` tests (line ~846-886).

Create a helper function `evaluateDecision(ruleId, stateObj)` that calls:
```js
execFileSync(process.execPath, [
  path.join(__dirname, '../bin/bgsd-tools.cjs'),
  'decisions:evaluate', ruleId,
  '--state', JSON.stringify(stateObj)
], { encoding: 'utf8', timeout: 30000 })
```
and returns `JSON.parse(output.trim())`.

Write test pairs for each of the 4 Chain B rules — each pair calls the rule with tools-present state and tools-absent state, then asserts the `.value` fields differ:

1. **file-discovery-mode**: tools-present state `{"tool_availability":{"fd":true},"scope":"project-wide"}` → expects `"fd"`. Tools-absent state `{"tool_availability":{"fd":false},"scope":"project-wide"}` → expects `"node"`. Assert values differ.

2. **search-mode**: tools-present state `{"tool_availability":{"ripgrep":true},"needs_gitignore_respect":true}` → expects `"ripgrep"`. Tools-absent state `{"tool_availability":{"ripgrep":false},"needs_gitignore_respect":true}` → expects `"node"`. Assert values differ.

3. **json-transform-mode**: tools-present state `{"tool_availability":{"jq":true},"json_complexity":"complex"}` → expects `"jq"`. Tools-absent state `{"tool_availability":{"jq":false},"json_complexity":"complex"}` → expects `"javascript"`. Assert values differ.

4. **agent-capability-level**: tools-present state `{"tool_availability":{"ripgrep":true,"fd":true,"jq":true,"yq":true,"bat":true,"gh":true}}` → expects `"HIGH"`. Tools-absent state `{"tool_availability":{"ripgrep":false,"fd":false,"jq":false,"yq":false,"bat":false,"gh":false}}` → expects `"LOW"`. Assert values differ.

Each test should also assert the returned object has `value`, `confidence`, and `rule_id` fields (shape validation).

Add one additional test that calls `decisions:list`, filters for rules whose `inputs` array includes `tool_availability`, and asserts it finds exactly the 4 Chain B rules — proving the dynamic filtering approach works.

IMPORTANT: Use key name `ripgrep` (not `rg`) in tool_availability objects — this matches the actual decision rule implementation in decision-rules.js.
  </action>
  <verify>npm test 2>&1 | grep -E '(Chain B|TEST-01|fail|pass)' — all E2E tests pass</verify>
  <done>E2E describe block with 5+ tests exists in cli-tools-integration.test.cjs, each Chain B rule evaluated with tools-present vs tools-absent state, .value fields asserted to differ, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create contract test for Chain B consumer coverage</name>
  <files>tests/tool-routing-contract.test.cjs</files>
  <action>
Create new file `tests/tool-routing-contract.test.cjs` with a single describe block:

```
describe('Contract: Chain B decision consumer coverage — TEST-02', () => { ... })
```

**Step 1 — Dynamically identify Chain B rules:**

Call `decisions:list` via `execFileSync` (same pattern as Task 1), parse the JSON, and filter `rules` for entries whose `inputs` array includes `'tool_availability'`. This should yield at least 4 rules: `file-discovery-mode`, `search-mode`, `json-transform-mode`, `agent-capability-level`. (May also include `phase-dependencies` which has `tool_availability` in its inputs — that's fine, just assert >= 4.)

**Step 2 — Scan workflow and agent files for consumers:**

Read all `.md` files from `workflows/` and `agents/` directories using `fs.readdirSync` + `fs.readFileSync`. For each Chain B rule ID, check if any file contains either:
- A pattern matching `decisions.{rule-id}` (regex: `decisions\\.` + escaped rule-id)
- A pattern matching `tool_availability` (direct consumption pattern, per stress-tested decision)

A rule is considered "consumed" if at least one file matches either pattern.

**Step 3 — Write tests:**

1. **Dynamic rule identification test:** Assert that filtering `decisions:list` by `tool_availability` in inputs yields >= 4 rules (proves dynamic filtering works and catches future additions).

2. **Per-rule consumer test (loop):** For each identified Chain B rule, assert it has at least one consumer file. Use `test()` inside a loop so each rule gets its own named test: `test(\`Chain B rule "${ruleId}" has at least one consumer\`, ...)`.

3. **Zero orphans summary test:** Assert that the count of consumed rules equals the count of Chain B rules — zero orphans.

4. **Consumer location test:** For at least the 4 known rules (file-discovery-mode, search-mode, json-transform-mode, agent-capability-level), assert the specific consumer files exist. For example:
   - `file-discovery-mode` → consumed by `workflows/execute-plan.md` or `workflows/map-codebase.md`
   - `search-mode` → consumed by `workflows/execute-plan.md`
   - `agent-capability-level` → consumed by `workflows/execute-phase.md`
   - `tool_availability` → consumed by `agents/bgsd-executor.md`, `agents/bgsd-debugger.md`, `agents/bgsd-codebase-mapper.md`

Read from repo source (`workflows/` and `agents/` relative to `__dirname + '/..'), not from deploy path — works in CI with no deployment dependency.
  </action>
  <verify>npm test 2>&1 | grep -E '(Contract|TEST-02|consumer|orphan|fail|pass)' — all contract tests pass</verify>
  <done>tests/tool-routing-contract.test.cjs exists with dynamic Chain B rule identification, per-rule consumer scanning of workflows/ and agents/, zero orphaned decisions asserted, all tests pass</done>
</task>

</tasks>

<verification>
1. `npm test` — all existing tests still pass (762+), new E2E and contract tests pass
2. E2E tests confirm each Chain B rule returns different .value for tools-present vs tools-absent
3. Contract test confirms every Chain B rule has at least one workflow or agent consumer
4. No mocking used — CLI --state flag for E2E, static file analysis for contract
</verification>

<success_criteria>
- E2E: All 4 Chain B decision rules produce different outputs when tool_availability changes (tools-present vs tools-absent)
- Contract: Every Chain B rule (dynamically identified from decisions:list) has at least one consumer in workflows/ or agents/
- Zero orphaned Chain B decision rules
- All tests pass via npm test
</success_criteria>

<output>
After completion, create `.planning/phases/139-end-to-end-validation/139-01-SUMMARY.md`
</output>
