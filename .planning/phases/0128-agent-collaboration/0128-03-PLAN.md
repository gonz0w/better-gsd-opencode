---
phase: 128-agent-collaboration
plan: 03
type: execute
wave: 2
depends_on:
  - 0128-01
  - 0128-02
files_modified:
  - tests/decisions.test.cjs
  - tests/enricher-decisions.test.cjs
autonomous: true
requirements:
  - AGENT-02
  - AGENT-03
outcome_ids:
  - DO-88
  - DO-89
  - DO-90

must_haves:
  truths:
    - "Contract tests verify resolveAgentCapabilityLevel returns HIGH for 5-6, MEDIUM for 2-4, LOW for 0-1 tools"
    - "Contract tests verify resolveAgentCapabilityLevel includes warning metadata only on LOW"
    - "Contract tests verify resolvePhaseDependencies honors declared dependencies over tool availability"
    - "Contract tests verify resolvePhaseDependencies discovery-before-analysis heuristic"
    - "Handoff tests verify all 9 agent pairs have tool context in handoff contracts"
    - "Context filtering tests verify tool_availability stripped for low-dependency agents and retained for high-dependency"
    - "Enricher tests verify handoff_tool_context shape (available_tools, tool_count, capability_level)"
  artifacts:
    - path: "tests/decisions.test.cjs"
      provides: "Contract tests for resolveAgentCapabilityLevel and resolvePhaseDependencies"
      contains: "resolveAgentCapabilityLevel, resolvePhaseDependencies"
    - path: "tests/enricher-decisions.test.cjs"
      provides: "Enricher integration tests for handoff_tool_context and context filtering"
      contains: "handoff_tool_context, scopeContextForAgent"
  key_links:
    - from: "tests/decisions.test.cjs"
      to: "src/lib/decision-rules.js"
      via: "Import and test both new decision functions"
      pattern: "resolveAgentCapabilityLevel|resolvePhaseDependencies"
    - from: "tests/enricher-decisions.test.cjs"
      to: "src/lib/context.js"
      via: "Import and test scopeContextForAgent with tool filtering"
      pattern: "scopeContextForAgent|handoff_tool_context"
---

<objective>
Add comprehensive contract tests for the Phase 128 decision functions, handoff contracts, and capability-aware context filtering.

Purpose: Validate all Phase 128 success criteria with tests covering decision function contracts, handoff pair completeness, and context filtering reduction. Target ~60 new tests across two test files.
Output: Extended tests/decisions.test.cjs and tests/enricher-decisions.test.cjs with Phase 128 test suites.
</objective>

<execution_context>
@/home/cam/.config/oc/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/oc/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0128-agent-collaboration/0128-CONTEXT.md
@.planning/phases/0128-agent-collaboration/0128-01-PLAN.md
@.planning/phases/0128-agent-collaboration/0128-02-PLAN.md
@tests/decisions.test.cjs
@tests/enricher-decisions.test.cjs
@tests/helpers.cjs
@src/lib/decision-rules.js
@src/lib/context.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contract tests for resolveAgentCapabilityLevel and resolvePhaseDependencies</name>
  <files>tests/decisions.test.cjs</files>
  <action>
Add a new `Phase 128: Agent collaboration decision functions` describe block at the end of tests/decisions.test.cjs. Follow existing test patterns (use contractCheck helper, DECISION_CONTRACT shape).

**Import the two new functions** at the top alongside existing imports:
```javascript
const { resolveAgentCapabilityLevel, resolvePhaseDependencies } = require('../src/lib/decision-rules');
```

**resolveAgentCapabilityLevel tests (~15 tests):**

1. `returns HIGH when all 6 tools available` — all true → value 'HIGH'
2. `returns HIGH when 5 tools available` — one false → value 'HIGH'
3. `returns MEDIUM when 4 tools available` — two false → value 'MEDIUM'
4. `returns MEDIUM when 3 tools available` — three false → value 'MEDIUM'
5. `returns MEDIUM when 2 tools available` — four false → value 'MEDIUM'
6. `returns LOW when 1 tool available` — five false → value 'LOW'
7. `returns LOW when 0 tools available` — all false → value 'LOW'
8. `LOW includes warning metadata` — 0 tools → metadata.warning is a string
9. `MEDIUM has no warning metadata` — 3 tools → no metadata or no metadata.warning
10. `HIGH has no warning metadata` — 6 tools → no metadata or no metadata.warning
11. `satisfies decision contract` — contractCheck with DECISION_CONTRACT
12. `confidence is always HIGH` — all levels return 'HIGH' confidence
13. `rule_id is agent-capability-level` — returns correct rule_id
14. `handles missing tool_availability gracefully` — null/undefined → LOW
15. `handles empty object` — {} → LOW (0 tools)

**resolvePhaseDependencies tests (~18 tests):**

1. `returns ordered phases for linear dependency chain` — A→B→C
2. `returns parallel roots at same level` — A and B no deps → both in first wave
3. `honors declared dependencies over tool availability` — phase with tools but depends on another goes after
4. `uses tool availability as tiebreaker for independent phases` — same level, one has tools → that one first
5. `discovery-before-analysis heuristic` — phase named 'discovery' before 'analysis' when independent
6. `detection-before-transform heuristic` — 'detect' before 'transform' when independent
7. `emits warning for phase with missing required tools` — metadata.warning present
8. `confidence HIGH when no warnings` — clean resolution
9. `confidence MEDIUM when warnings present` — tool readiness warning
10. `handles single phase` — one phase → returns it
11. `handles empty phases array` — [] → empty ordered_phases
12. `handles circular dependency` — A→B→A → returns error or breaks cycle
13. `satisfies decision contract` — contractCheck shape
14. `rule_id is phase-dependencies` — correct rule_id
15. `ignores completed phases in ordering` — status: 'complete' excluded from ordering
16. `handles phases with no tool_requirements` — phases without tool needs still ordered
17. `declared deps override discovery heuristic` — if declared dep says analysis before discovery, honor it
18. `handles null/undefined input gracefully` — returns empty result

**DECISION_REGISTRY integration tests (~4 tests):**

1. `registry includes agent-capability-level` — find by id
2. `registry includes phase-dependencies` — find by id
3. `evaluateDecisions fires agent-capability-level when tool_availability in state` — verify it fires
4. `evaluateDecisions fires phase-dependencies when phases in state` — verify it fires
  </action>
  <verify>
Run `npm test 2>&1 | tail -5` to confirm all tests pass. Count new tests:
```bash
npm test 2>&1 | grep -E "(tests|pass|fail)" | tail -3
```
Expected: ~37 new tests above 1501 baseline (total ~1538+).
  </verify>
  <done>~37 new contract tests pass for resolveAgentCapabilityLevel (15 tests covering all thresholds, warning metadata, graceful defaults) and resolvePhaseDependencies (18 tests covering topological sort, tiebreakers, heuristics, edge cases) plus 4 DECISION_REGISTRY integration tests. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Enricher integration tests for handoff context and capability-aware filtering</name>
  <files>tests/enricher-decisions.test.cjs</files>
  <action>
Add a new `Phase 128: handoff tool context and capability-aware filtering` describe block at the end of tests/enricher-decisions.test.cjs.

**Import scopeContextForAgent** alongside existing test infrastructure:
```javascript
const { scopeContextForAgent } = require('../src/lib/context');
```

**handoff_tool_context enricher tests (~8 tests):**

1. `handoff_tool_context is an object` — enrichment output includes it
2. `available_tools is an array of strings` — no objects, no descriptions (per CONTEXT.md)
3. `tool_count matches available_tools length` — consistency check
4. `capability_level is HIGH/MEDIUM/LOW string` — valid enum
5. `capability_level matches tool count thresholds` — 5-6→HIGH, 2-4→MEDIUM, 0-1→LOW
6. `available_tools contains only tool names from the 6 known tools` — no unknowns
7. `tool names are strings, not objects` — per CONTEXT.md: names only, no schemas
8. `handoff_tool_context defaults gracefully when tool_availability absent` — empty array, count 0, LOW

**Capability-aware context filtering tests (~12 tests):**

1. `verifier context excludes tool_availability` — low-dependency → stripped
2. `plan-checker context excludes tool_availability` — low-dependency → stripped
3. `phase-researcher context excludes tool_availability` — low-dependency → stripped
4. `executor context includes tool_availability` — high-dependency → kept
5. `debugger context includes tool_availability` — high-dependency → kept
6. `codebase-mapper context includes tool_availability` — high-dependency → kept
7. `planner context includes tool_availability` — medium-dependency → kept
8. `verifier context strips tool-routing decisions` — decisions without file-discovery-mode
9. `executor context keeps tool-routing decisions` — decisions with file-discovery-mode
10. `low-dependency agent savings > 0` — reduction_pct positive for verifier
11. `high-dependency agent retains full context` — no reduction from tool stripping
12. `filtering is silent — no _filtered_tools metadata` — agent doesn't know what was removed

**Handoff contract completeness test (~5 tests):**

1. `verify:handoff preview returns result for all 9 defined pairs` — iterate pairs, each returns context
2. `critical pairs have rich tool_context_type` — planner→executor, researcher→planner
3. `minimal pairs have minimal tool_context_type` — other 7 pairs
4. `all handoff contexts include tool-related fields` — every pair has some tool context
5. `no undefined handoff pairs in the 9 critical pairs` — all return defined contracts
  </action>
  <verify>
Run `npm test 2>&1 | tail -5` to confirm all tests pass. Count total:
```bash
npm test 2>&1 | grep -E "(tests|pass|fail)" | tail -3
```
Expected: ~25 new tests above previous baseline (total ~1563+).
  </verify>
  <done>~25 new enricher integration tests pass covering handoff_tool_context shape (8 tests), capability-aware filtering for all agent types (12 tests), and handoff contract completeness (5 tests). All tests pass including 1501 baseline. Total test count is 1563+.</done>
</task>

</tasks>

<verification>
1. `npm test` passes with all tests (1501 baseline + ~62 new = ~1563+ total)
2. Contract tests validate both new decision functions against DECISION_CONTRACT shape
3. resolveAgentCapabilityLevel tested at all 7 tool count levels (0-6)
4. resolvePhaseDependencies tested for dependency ordering, tiebreakers, heuristics, edge cases
5. Context filtering tested for all 9+ agent types (low/medium/high dependency)
6. Handoff completeness tested for all 9 agent pairs
7. handoff_tool_context shape validated (names only, no schemas)
</verification>

<success_criteria>
- ~62 new tests pass (37 decision + 25 enricher)
- Total test count 1563+ (above 1501 baseline)
- All success criteria from ROADMAP Phase 128 covered by tests:
  - SC-1: Tool availability in handoff context (handoff tests)
  - SC-2: resolvePhaseDependencies contract tests
  - SC-3: resolveAgentCapabilityLevel contract tests
  - SC-4: All 9 agent pairs tested (handoff completeness tests)
  - SC-5: 25%+ context reduction for tool-independent (filtering tests)
- Zero test failures
</success_criteria>

<output>
After completion, create `.planning/phases/0128-agent-collaboration/0128-03-SUMMARY.md`
</output>
