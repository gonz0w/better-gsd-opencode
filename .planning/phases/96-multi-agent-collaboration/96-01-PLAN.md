---
phase: 96-multi-agent-collaboration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/bgsd-tools.cjs
  - skills/raci/SKILL.md
  - skills/commit-protocol/SKILL.md
  - workflows/execute-phase.md
  - workflows/execute-plan.md
autonomous: true
requirements:
  - AGENT-10
  - AGENT-11
  - AGENT-12

must_haves:
  truths:
    - "Agent handoffs include structured context transfer with preconditions verified"
    - "Shared context patterns enable agents to collaborate on related tasks"
    - "Handoff contracts verified before transfer completes successfully"
  artifacts:
    - path: "bin/bgsd-tools.cjs"
      provides: "Handoff context transfer, shared context registry, contract verification"
      contains: "handoffContext, sharedContext, verifyHandoffContract"
    - path: "skills/raci/SKILL.md"
      provides: "Updated RACI with handoff contract specifications"
      contains: "handoff_contracts, preconditions, context_transfer"
    - path: "workflows/execute-phase.md"
      provides: "Enhanced execution workflow with handoff integration"
      contains: "handoff_context, contract_verification"
  key_links:
    - from: "execute-phase.md"
      to: "raci skill"
      via: "handoff contracts"
      pattern: "agent.*handoff.*contract"
    - from: "bgsd-tools.cjs"
      to: "verify:agents"
      via: "contract verification"
      pattern: "verifyHandoffContract.*preconditions"
    - from: "commit-protocol skill"
      to: "handoff context"
      via: "context preservation"
      pattern: "handoff.*context.*commit"
---

<objective>
Implement multi-agent collaboration with structured context transfer, shared context patterns, and handoff contract verification.

Purpose: Enable seamless handoffs between agents with verified contracts, shared context for collaboration, and automatic precondition checking before any agent-to-agent transfer.

Output: Enhanced bgsd-tools.cjs with handoff infrastructure, updated RACI skill with contract specifications, and modified execution workflows.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-phase.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md

# From prior phases - agent capabilities
@.planning/phases/92-planning-intelligence/92-01-SUMMARY.md
@.planning/phases/93-verification-intelligence/93-01-SUMMARY.md
@.planning/phases/94-execution-intelligence/94-01-SUMMARY.md

# Prior decisions relevant to handoffs
# [86-02]: Handoff contracts documented in RACI skill with inputs, outputs, preconditions
# [86-01]: Created verify:agents command for automated boundary validation
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create structured handoff context transfer system</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
Implement structured context transfer for agent handoffs in bgsd-tools.cjs:

1. Create handoffContext module with:
   - createHandoffContext(fromAgent, toAgent, taskContext) - builds structured context package
   - serializeContext(context) - converts to transferrable format
   - deserializeContext(serialized) - reconstructs on receiving end
   - validateContext(context) - ensures completeness

2. Define handoff context structure:
   - source_agent: originating agent ID
   - target_agent: destination agent ID  
   - task_summary: brief description of work done
   - context_blocks: array of context sections (STATE, DECISIONS, FILES, etc.)
   - preconditions: array of required conditions for handoff
   - artifacts: files created/modified during source agent's work
   - decisions: decisions made that affect downstream work
   - blockers: current blockers if any
   - timestamp: when handoff was created

3. Add CLI command: verify:handoff for testing context transfer:
   - verify:handoff --validate <context> - validates handoff completeness
   - verify:handoff --preview <from> <to> - shows what context would transfer

4. Integrate with execute-phase workflow:
   - Before each agent handoff, create structured context
   - Pass serialized context to receiving agent
   - Log handoff events with context summary

Key: Context should be deterministic (per DO-28) - pre-computed, not search-and-discover.
</action>
  <verify>
- Run: node bin/bgsd-tools.cjs verify:handoff --help to verify command exists
- Check that handoffContext module is properly exported
- Verify context structure includes all required fields
</verify>
  <done>
Handoff context includes all required fields (source, target, task_summary, context_blocks, preconditions, artifacts, decisions, blockers, timestamp)
verify:handoff --validate confirms context completeness
Context transfer is deterministic - same inputs produce same outputs
</done>
</task>

<task type="auto">
  <name>Task 2: Create shared context registry for collaboration</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
Implement shared context patterns enabling agents to collaborate on related tasks:

1. Create sharedContext module with:
   - registerContext(agentId, contextKey, contextValue) - shares context with other agents
   - getSharedContext(agentId, contextKey) - retrieves shared context
   - listAvailableContext(agentId) - shows all shared context available to agent
   - invalidateContext(contextKey) - clears stale shared context

2. Define shared context categories:
   - project_state: current phase, plan, blockers
   - task_dependencies: what other agents are working on
   - common_artifacts: files multiple agents might need
   - coordination_signals: hints about related work

3. Add context persistence:
   - Store shared context in .planning/.cache/shared-context.json
   - TTL-based expiration (default 30 minutes)
   - Automatic cleanup of stale context

4. Add CLI commands:
   - util:shared-context --list - shows all shared context
   - util:shared-context --register <key> <value> - shares context
   - util:shared-context --clear - clears all shared context

5. Integration patterns:
   - When planner creates tasks, share task dependencies
   - When verifier finds issues, share finding context
   - When executor has blockers, share blocker context

Key: Shared context enables DO-11 "Shared context patterns enable agents to collaborate on related tasks"
</action>
  <verify>
- Run: node bin/bgsd-tools.cjs util:shared-context --help to verify command exists
- Check that sharedContext module is properly exported
- Test context sharing between simulated agents
</verify>
  <done>
Agents can register and retrieve shared context
util:shared-context --list shows available shared context
Shared context expires after TTL and is cleaned up
Collaboration patterns documented in skill
</done>
</task>

<task type="auto">
  <name>Task 3: Implement handoff contract verification</name>
  <files>bin/bgsd-tools.cjs, skills/raci/SKILL.md, workflows/execute-phase.md</files>
  <action>
Implement verification that handoff contracts are satisfied before transfer completes:

1. Create verifyHandoffContract function:
   - verifyHandoffContract(contract, currentState) - checks all preconditions
   - listContractPreconditions(agentPair) - shows required conditions
   - generateContractReport(contract, results) - formats verification output

2. Define contract structure per agent pair (from RACI skill):
   - required_inputs: context blocks the receiving agent needs
   - required_artifacts: files that must exist
   - required_state: project state conditions
   - validation_functions: custom validators per precondition

3. Implement precondition types:
   - context_exists: required context blocks present
   - artifacts_exist: required files present and valid
   - state_valid: project in correct state for handoff
   - dependencies_met: prerequisite tasks complete

4. Update RACI skill:
   - Document handoff contracts for each agent pair
   - Add precondition specifications to existing RACI entries
   - Include validation functions in skill

5. Integrate with execute-phase workflow:
   - Before handoff, call verifyHandoffContract
   - If verification fails, block handoff and report missing preconditions
   - Allow forced handoff with warning if critical preconditions missing

6. Add CLI command:
   - verify:agents --contracts - shows all handoff contracts
   - verify:agents --verify <from> <to> - verifies specific contract

Key: This implements AGENT-12 "Handoff contracts verified before transfer completes"
</action>
  <verify>
- Run: node bin/bgsd-tools.cjs verify:agents --contracts to list contracts
- Check that verifyHandoffContract function is exported
- Test contract verification with sample preconditions
</verify>
  <done>
verify:agents --contracts shows all handoff contracts with preconditions
verifyHandoffContract blocks handoffs with unmet preconditions
Contract verification report includes specific missing items
Workflow integration prevents invalid handoffs
</done>
</task>

</tasks>

<verification>
1. All three modules (handoffContext, sharedContext, verifyHandoffContract) are exported from bgsd-tools.cjs
2. verify:handoff command validates context transfer completeness
3. util:shared-context enables cross-agent context sharing
4. verify:agents --verify checks preconditions before handoff
5. execute-phase workflow integrates handoff verification
</verification>

<success_criteria>
- AGENT-10: Structured context transfer implemented with all required fields
- AGENT-11: Shared context registry enables agent collaboration
- AGENT-12: Handoff contracts verified before transfer completes

All three requirements satisfied: context transfer is structured, shared patterns enable collaboration, contract verification prevents invalid handoffs.
</success_criteria>

<output>
After completion, create `.planning/phases/96-multi-agent-collaboration/96-01-SUMMARY.md`
</output>
