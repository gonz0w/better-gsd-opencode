---
phase: 168-adaptive-model-settings-contract
plan: 04
type: execute
wave: 3
depends_on:
  - 168-02
  - 168-03
files_modified:
  - workflows/settings.md
  - workflows/set-profile.md
  - docs/configuration.md
  - docs/agents.md
  - skills/model-profiles/SKILL.md
  - tests/integration.test.cjs
autonomous: true
requirements:
  - MODEL-02
  - MODEL-03
  - MODEL-07
outcome_ids:
  - DO-117
user_setup: []
must_haves:
  truths:
    - "`/bgsd-settings` leads with one selected project profile, editable concrete models for `quality` / `balanced` / `budget`, and optional sparse agent overrides"
    - "Settings, docs, and skill guidance describe model choice in capability/use-case language with GPT-first shipped defaults and no alias or legacy-compatibility promises"
  artifacts:
    - path: "workflows/settings.md"
      provides: "Interactive settings flow for the new contract"
      contains: "selected default profile, profile definitions, sparse overrides"
    - path: "workflows/set-profile.md"
      provides: "Quick-switch flow aligned to the selected project profile"
      contains: "quality, balanced, budget"
    - path: "skills/model-profiles/SKILL.md"
      provides: "Agent guidance aligned to the new contract"
      contains: "provider-agnostic profile language, direct overrides"
  key_links:
    - from: "workflows/settings.md"
      to: "docs/configuration.md"
      via: "same contract language and field names"
      pattern: "quality|balanced|budget|default profile|agent overrides"
    - from: "docs/configuration.md"
      to: "docs/agents.md"
      via: "shared explanation of global profile selection and direct overrides"
      pattern: "quality|balanced|budget|model_overrides"
---

<objective>
Expose the new contract through settings UX and user-facing guidance.

Purpose: Phase 168 is not complete if the contract exists only in schema and runtime code. Users need `/bgsd-settings` to edit the selected profile, concrete profile definitions, and optional exceptions, while docs stop teaching the old Anthropic-shaped surface.
Output: settings workflows and docs teach the same project-default-first contract without adding new abstraction or a broader cleanup project.
</objective>

> **TDD Decision:** Selected - interactive workflow outputs and config-facing guidance have stable expected shapes that should be locked with regression coverage.

<execution_context>
@.planning/phases/168-adaptive-model-settings-contract/168-CONTEXT.md
@.planning/phases/168-adaptive-model-settings-contract/168-RESEARCH.md
@.planning/phases/168-adaptive-model-settings-contract/168-02-PLAN.md
@.planning/phases/168-adaptive-model-settings-contract/168-03-PLAN.md
</execution_context>

<context>
@workflows/settings.md
@workflows/set-profile.md
@skills/model-profiles/SKILL.md
@docs/configuration.md
</context>

<tasks>

<task type="auto">
  <name>Rewrite settings workflows and docs around the project-default-first UX</name>
  <files>
workflows/settings.md
workflows/set-profile.md
docs/configuration.md
docs/agents.md
skills/model-profiles/SKILL.md
tests/integration.test.cjs
  </files>
  <action>
Rewrite the settings-family command surface and related docs to match the new contract.

The interactive flow should:
- lead with the selected project default profile
- let the user edit the concrete models behind `quality`, `balanced`, and `budget`
- keep agent overrides clearly secondary and sparse
- use provider-agnostic language about quality, speed, and use case while seeding GPT-family defaults

Update the settings workflows, configuration docs, agent docs, and `skills/model-profiles/SKILL.md` so every user-facing surface teaches the same contract and no longer references aliases or legacy `opus` / `sonnet` / `haiku` naming as the preferred path.
  </action>
  <verify>npm run test:file -- tests/integration.test.cjs</verify>
  <done>Settings UX and docs consistently teach and expose the new project-default-first contract</done>
</task>

</tasks>

<verification>
- Run: npm run test:file -- tests/integration.test.cjs
- Run: npm run build
- Read: `workflows/settings.md` and confirm the first-run path is selected profile -> profile definitions -> optional overrides
</verification>

<success_criteria>
- [ ] `/bgsd-settings` exposes the selected-profile-first contract instead of legacy tier switching alone
- [ ] Docs and skills use provider-agnostic profile language with GPT-first defaults
- [ ] Regression tests cover the updated settings behavior and guidance
</success_criteria>

<output>
After completion, create `.planning/phases/168-adaptive-model-settings-contract/168-04-SUMMARY.md`
</output>
