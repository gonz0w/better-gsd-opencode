---
phase: 168-adaptive-model-settings-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/MILESTONE-INTENT.md
autonomous: true
requirements:
  - MODEL-01
  - MODEL-06
outcome_ids:
  - DO-117
user_setup: []
must_haves:
  truths:
    - "Phase 168 roadmap text describes a minimal project-default-first profile contract with sparse agent overrides instead of aliases plus migration-safe legacy behavior"
    - "Phase 168 requirement text and milestone intent no longer ask execution to preserve alias or legacy Anthropic compatibility that the locked context explicitly excludes"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 168 goal, requirements, and success criteria aligned to locked context"
      contains: "selected profile, shared profile definitions, sparse agent overrides"
    - path: ".planning/REQUIREMENTS.md"
      provides: "MODEL requirement wording aligned to the break-and-replace contract"
      contains: "profile definitions, direct overrides, provider-agnostic copy"
    - path: ".planning/MILESTONE-INTENT.md"
      provides: "Milestone priorities and notes consistent with the Phase 168 contract"
      contains: "settings-driven model configuration, no migration-safe promise for Phase 168"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Phase 168 requirement list and success criteria"
      pattern: "Phase 168|MODEL-01|MODEL-06|MODEL-07"
    - from: ".planning/MILESTONE-INTENT.md"
      to: ".planning/ROADMAP.md"
      via: "v18.0 priorities and Phase 168 goal wording"
      pattern: "model configuration|migration-safe|provider-agnostic"
---

<objective>
Align stale planning artifacts with the locked minimal Phase 168 context before code execution hardens the wrong contract.

Purpose: the current roadmap, requirements, and milestone intent still promise aliases and legacy compatibility, while `168-CONTEXT.md` and the latest user direction explicitly choose a simpler global-profile-plus-overrides contract. This plan makes the execution target truthful so later plans, verification, and docs all aim at the same user-visible outcome.
Output: updated planning artifacts that describe user-chosen concrete models for `quality` / `balanced` / `budget`, one selected global profile, sparse direct agent overrides, and provider-agnostic copy without alias or migration-safe legacy commitments.
</objective>

> **TDD Decision:** Skipped - this is planning-artifact alignment work, not a behavior-first implementation slice.

<execution_context>
@.planning/phases/168-adaptive-model-settings-contract/168-CONTEXT.md
@.planning/phases/168-adaptive-model-settings-contract/168-RESEARCH.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/MILESTONE-INTENT.md
</context>

<tasks>

<task type="auto">
  <name>Rewrite Phase 168 roadmap and requirement wording to match the locked contract</name>
  <files>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
  </files>
  <action>
Update the Phase 168 roadmap section and stale `MODEL-*` wording so they match the user-approved scope from `168-CONTEXT.md`.

Specifically:
- replace alias-centered wording with simple profile-definition wording built around `quality`, `balanced`, and `budget`
- replace migration-safe legacy wording with explicit break-and-replace scope for this phase
- preserve phase numbering, requirement ids, and traceability table structure so downstream tooling keeps working
- keep the Phase 168 requirement set focused on: configurable built-in profiles, one global active profile, direct agent overrides, and provider-agnostic guidance

Do not add new capabilities or new phase ids. This is truth-alignment, not scope expansion.
  </action>
  <verify>rg -n "Phase 168|MODEL-01|MODEL-06|migration-safe|model aliases" .planning/ROADMAP.md .planning/REQUIREMENTS.md</verify>
  <done>Phase 168 roadmap and requirement text describe the same break-and-replace contract captured in 168-CONTEXT.md</done>
</task>

<task type="auto">
  <name>Align milestone intent with the new model-settings posture</name>
  <files>.planning/MILESTONE-INTENT.md</files>
  <action>
Update the model-configuration bullets in `MILESTONE-INTENT.md` so the milestone no longer promises migration-safe legacy behavior inside Phase 168.

Keep DO-117 unchanged, but rewrite the relevant priority/note language to emphasize:
- settings-driven model configuration with the current global profile selector preserved
- provider-agnostic capability language
- user-chosen concrete models behind `quality`, `balanced`, and `budget`
- later phases owning broader runtime resolution and visibility follow-through

Do not disturb the cmux half of the milestone intent.
  </action>
  <verify>rg -n "model configuration|migration-safe|provider-agnostic|DO-117" .planning/MILESTONE-INTENT.md</verify>
  <done>Milestone intent no longer conflicts with the locked Phase 168 contract</done>
</task>

</tasks>

<verification>
- Run: rg -n "Phase 168|MODEL-01|MODEL-06|migration-safe|model aliases" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/MILESTONE-INTENT.md
- Read the Phase 168 roadmap section and confirm it matches `168-CONTEXT.md` on: configurable built-in profiles, one project default, sparse overrides, and no legacy compatibility promise
</verification>

<success_criteria>
- [ ] `.planning/ROADMAP.md` Phase 168 section matches the locked context
- [ ] `.planning/REQUIREMENTS.md` no longer requires alias or legacy-compat behavior for Phase 168
- [ ] `.planning/MILESTONE-INTENT.md` priorities/notes agree with the new scope
</success_criteria>

<output>
After completion, create `.planning/phases/168-adaptive-model-settings-contract/168-01-SUMMARY.md`
</output>
