---
phase: 97-ux-polish
plan: "02"
type: execute
wave: 2
depends_on: ["97-01"]
files_modified:
  - bin/bgsd-tools.cjs
  - build.cjs
autonomous: true
requirements:
  - PERF-03
  - PERF-04

must_haves:
  truths:
    - "Bundle size reduced beyond v9.3 baseline"
    - "Dead code removal targets unused code paths"
  artifacts:
    - path: "bin/bgsd-tools.cjs"
      provides: "Reduced bundle size through dead code removal"
      contains: "tree-shaken_modules"
    - path: "build.cjs"
      provides: "Build-time dead code elimination"
      contains: "knip_config, tree_shaking_options"
  key_links:
    - from: "build.cjs"
      to: "bin/bgsd-tools.cjs"
      via: "esbuild bundler"
      pattern: "esbuild.*bundle.*tree-shake"
---

<objective>
Reduce bundle size beyond v9.3 baseline and remove dead code paths.

Purpose: Improve CLI performance through smaller bundle size and eliminate unused code paths that bloat the deployment.

Output: Smaller bin/bgsd-tools.cjs with unused code removed.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v10.0-REQUIREMENTS.md
@.planning/benchmarks/v9.3-baseline.json

# Prior performance work - benchmark infrastructure from phase 90
@.planning/phases/90-benchmark/90-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze current bundle and identify reduction opportunities</name>
  <files>bin/bgsd-tools.cjs, build.cjs</files>
<action>
Analyze the current bundle to identify size reduction opportunities:

1. Current baseline check:
   - Measure current bin/bgsd-tools.cjs size: ~1.5MB
   - Review v9.3 baseline metrics in .planning/benchmarks/v9.3-baseline.json
   - Note: v9.3 baseline doesn't have bundle size, so establish current as baseline

2. Use knip for dead code detection:
   - Run knip to find unused exports, dependencies, files
   - Identify functions that are defined but never called
   - Find dependencies that are required but not used

3. Analyze esbuild metafile:
   - Generate esbuild metafile during build
   - Analyze included vs excluded modules
   - Identify large dependencies that could be deferred

4. Categories of reduction opportunities:
   - Unused exports from src/ modules
   - Dependencies only used in debug/benchmark code
   - Legacy code paths no longer exercised
   - Conditional features that could be tree-shaken

5. Document findings:
   - Create .planning/analysis/bundle-size-97.md with analysis
   - List specific reduction targets
   - Estimate potential savings per item

Key: Be systematic - measure before and after each change.
</action>
<verify>
- Run: knip to find unused code
- Check esbuild metafile for module sizes
- Verify bundle size before and after changes
</verify>
<done>
Bundle analysis complete with identified reduction opportunities
knip report shows unused exports/dependencies
Reduction targets documented with estimated savings
</done>
</task>

<task type="auto">
<name>Task 2: Implement bundle size reduction through dead code removal</name>
<files>bin/bgsd-tools.cjs, build.cjs</files>
<action>
Implement bundle size reductions based on analysis findings:

1. Remove identified dead code:
   - Delete unused functions from src/ modules
   - Remove unused export statements
   - Clean up unreachable code paths

2. Configure esbuild tree-shaking:
   - Enable tree-shaking in build.cjs: treeShaking: true
   - Use module sideEffects: false where appropriate
   - Mark pure functions with /*@__PURE__*/ annotation

3. Optimize large dependencies:
   - Identify largest dependencies in bundle
   - Lazy-load non-critical dependencies (e.g., heavy parsers)
   - Consider tree-shaking friendly alternatives

4. Add build-time feature flags:
   - Extend INCLUDE_BENCHMARKS pattern to other conditional features
   - Use esbuild define for build-time constants
   - Example: INCLUDE_DEBUG_HELPERS, INCLUDE_LEGACY_COMMANDS

5. Verify reductions:
   - Measure bundle size after changes
   - Compare to baseline
   - Ensure all tests still pass

Target: Reduce bundle size by at least 5% from current baseline.

Key: Don't break functionality - only remove truly dead code.
</action>
<verify>
- Run: npm run build && ls -la bin/bgsd-tools.cjs
- Compare size to baseline
- Run test suite to verify no regressions
</verify>
<done>
Bundle size reduced by at least 5% from baseline
All tests pass after changes
No functionality regressions
</done>
</task>

</tasks>

<verification>
1. Bundle size measured and reduced beyond baseline
2. Dead code removed from source and bundle
3. Build-time feature flags implemented for conditional code
4. All tests pass after changes
5. Performance benchmarks still within acceptable range
</verification>

<success_criteria>
- PERF-03: Bundle size reduced beyond v9.3 baseline
- PERF-04: Dead code removal targets unused code paths

Both performance requirements satisfied: bundle smaller, dead code removed.
</success_criteria>

<output>
After completion, create `.planning/phases/97-ux-polish/97-02-SUMMARY.md`
</output>
