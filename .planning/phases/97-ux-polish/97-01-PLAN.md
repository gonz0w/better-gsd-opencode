---
phase: 97-ux-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/bgsd-tools.cjs
autonomous: true
requirements:
  - UX-07
  - UX-08
  - UX-09

must_haves:
  truths:
    - "Help includes contextual suggestions based on recent commands"
    - "Command discoverability improved with autocomplete hints"
    - "Examples included in help for common use cases"
  artifacts:
    - path: "bin/bgsd-tools.cjs"
      provides: "Enhanced help system with contextual suggestions"
      contains: "helpWithContext, commandSuggestions, helpExamples"
    - path: "bin/manifest.json"
      provides: "Command metadata for discoverability"
      contains: "descriptions, examples, autocomplete_hints"
  key_links:
    - from: "help command"
      to: "command history"
      via: "recent commands context"
      pattern: "help.*context.*recent"
    - from: "help output"
      to: "autocomplete hints"
      via: "hint generation"
      pattern: "autocomplete.*hint"
---

<objective>
Improve help system with contextual suggestions, command discoverability, and examples.

Purpose: Make the CLI more user-friendly by providing contextual help, better command discovery, and practical examples for common use cases.

Output: Enhanced help system in bgsd-tools.cjs with contextual suggestions, autocomplete hints, and examples.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v10.0-REQUIREMENTS.md

# Prior UX work - TTY foundation from phase 91
@.planning/phases/91-rich-tty-output/91-01-SUMMARY.md

# Prior interactive workflows from phase 95
@.planning/phases/95-interactive-workflows/95-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement contextual help suggestions</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
Implement contextual help system that tracks recent commands and provides relevant suggestions:

1. Create helpContext module with:
   - trackCommand(namespace, command, args) - records command usage with timestamp
   - getRecentCommands(limit) - returns recent command history
   - getContextualSuggestions() - generates suggestions based on history
   - clearHistory() - clears command history

2. Store command history in .planning/.cache/command-history.json:
   - Track last 20 commands
   - Include namespace, command, timestamp, args
   - Auto-cleanup commands older than 7 days

3. Contextual suggestion logic:
   - After "plan:phase", suggest "execute:phase" 
   - After "verify:state", suggest "verify:review"
   - Group by workflow stage (plan -> execute -> verify)

4. Integrate with help output:
   - Add "Recent commands" section to help
   - Add "Suggested next steps" based on context
   - Show "Did you mean?" for typos

5. Add CLI commands:
   - util:history - shows command history
   - util:history --clear - clears history
   - help --context - shows contextual suggestions

Key: Context should enhance help, not replace it - keep standard help prominent.
</action>
  <verify>
- Run: node bin/bgsd-tools.cjs util:history to verify history tracking
- Check that contextual suggestions appear in help output
- Verify history persists across CLI invocations
</verify>
  <done>
Command history tracks last 20 commands with timestamps
util:history shows recent commands with namespace, command, timestamp
Contextual suggestions appear in help based on recent commands
</done>
</task>

<task type="auto">
  <name>Task 2: Improve command discoverability with autocomplete hints</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
Enhance command discoverability with better autocomplete hints and suggestions:

1. Create commandDiscovery module with:
   - getAutocompleteHints(partial) - returns suggestions for partial input
   - getCommandAliases() - maps short aliases to full commands
   - getSimilarCommands(input) - finds similar commands by Levenshtein distance
   - getCommandCategories() - groups commands by category

2. Define command metadata structure:
   - namespace, command, description
   - aliases: [] (shortcuts like "p" for "plan")
   - category: "workflow" | "analysis" | "utility"
   - common_args: [{name, description, required}]
   - related_commands: [] (commands often used together)

3. Add to manifest.json generation:
   - Include command metadata in bin/manifest.json
   - Add autocomplete_hints field per command
   - Group by category for discoverability

4. Implement autocomplete logic:
   - Partial match: "plan" matches "plan:phase", "plan:roadmap"
   - Alias expansion: "p p" expands to "plan:phase"
   - Fuzzy matching: "ver" suggests "verify:state", "verify:review"

5. Add CLI enhancements:
   - Unknown command: show "Did you mean: X?" with similar commands
   - Partial command: show available subcommands
   - Add --suggest flag to show related commands

Key: Discoverability should work even for unknown commands - graceful fallback.
</action>
  <verify>
- Run: node bin/bgsd-tools.cjs plan: to see subcommand suggestions
- Test "Did you mean" for misspellings
- Verify autocomplete hints in help output
</verify>
  <done>
Partial namespace input shows available commands
Misspelled commands show "Did you mean: X?"
Command categories visible in help
Autocomplete hints available via --suggest flag
</done>
</task>

<task type="auto">
  <name>Task 3: Add examples to help for common use cases</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
Add practical examples to help output for common CLI use cases:

1. Create helpExamples module with:
   - getExamples(namespace, command) - returns examples for a command
   - getCommonWorkflows() - returns example workflows
   - formatExample(example) - formats example for display

2. Define example structure per command:
   ```json
   {
     "command": "plan:phase",
     "examples": [
       {
         "description": "Plan a new phase",
         "usage": "plan:phase 97",
         "output": "Creates PLAN.md for phase 97"
       }
     ]
   }
   ```

3. Common workflow examples to include:
   - New project setup: init:project → plan:requirements → plan:roadmap
   - Execute and verify: plan:phase → execute:phase → verify:state
   - Quick iteration: execute:quick → verify:review
   - Debug workflow: debug:start → debug:investigate → debug:fix

4. Integrate with help output:
   - Add "Examples:" section to each command help
   - Add "Common workflows" section to main help
   - Show 2-3 most relevant examples per command

5. Add CLI flags:
   - help <command> --examples - shows only examples
   - help --workflows - shows common workflow examples
   - help --verbose - shows full examples with explanations

Key: Examples should be practical, realistic use cases - not hello world.
</action>
  <verify>
- Run: node bin/bgsd-tools.cjs help plan:phase --examples
- Check help --workflows shows common workflows
- Verify examples are realistic and practical
</verify>
  <done>
help <command> shows Examples section with realistic use cases
help --workflows displays common multi-command workflows
Examples include usage, description, and expected output
</done>
</task>

</tasks>

<verification>
1. All three help enhancements (contextual, discoverability, examples) integrated into CLI
2. Command history persists across invocations
3. "Did you mean" works for misspellings
4. Examples shown in help output for key commands
5. Common workflow examples available via help --workflows
</verification>

<success_criteria>
- UX-07: Help includes contextual suggestions based on recent commands
- UX-08: Command discoverability improved with autocomplete hints
- UX-09: Examples included in help for common use cases

All three UX requirements satisfied: contextual help, discoverability, and examples implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/97-ux-polish/97-01-SUMMARY.md`
</output>
