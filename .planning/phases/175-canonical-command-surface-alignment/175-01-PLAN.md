---
phase: 175-canonical-command-surface-alignment
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - commands/bgsd-plan.md
  - src/lib/planning-command-surface.js
  - src/lib/commandDiscovery.js
  - tests/validate-commands.test.cjs
autonomous: true
requirements:
  - CLI-01
  - CLI-02
must_haves:
  truths:
    - "Maintainers can change the planning-family route grammar from the canonical `commands/bgsd-plan.md` matrix instead of editing separate validator rules and alias checks by hand."
    - "Runnable `/bgsd-plan` guidance without an explicit supported sub-action or required operand fails validation instead of being guessed from shorthand like `/bgsd-plan 175`."
    - "Legacy planning wrappers remain validation metadata with canonical `/bgsd-plan ...` suggestions sourced from the same planning-family definition."
  artifacts:
    - path: "commands/bgsd-plan.md"
      provides: "Canonical planning-family route matrix"
      contains: "Normalize the first argument onto the existing planning-family workflow contract"
    - path: "src/lib/planning-command-surface.js"
      provides: "Shared planning-family parser and route metadata loader"
      contains: "loadPlanningCommandSurface"
    - path: "tests/validate-commands.test.cjs"
      provides: "Regression coverage for canonical planning-family parsing and shorthand rejection"
      contains: "planning-family canonical matrix"
  key_links:
    - from: "src/lib/planning-command-surface.js"
      to: "commands/bgsd-plan.md"
      via: "shared parser reads the canonical planning-family route matrix"
      pattern: "phase <phase-number>|roadmap add <description>|todo check \[area\]"
    - from: "tests/validate-commands.test.cjs"
      to: "src/lib/commandDiscovery.js"
      via: "validation regressions prove shorthand rejection and legacy alias suggestions use shared planning-family metadata"
      pattern: "legacy-command|missing-argument|/bgsd-plan 175"
---

<objective>
Make `commands/bgsd-plan.md` the maintainable planning-family route matrix that command-integrity validation and planning-family shorthand rules read from directly.

Purpose: Phase 175 needs one clearer canonical definition for planning-family routing, alias suggestions, and operand requirements. This slice removes the highest-value parallel drift by extracting shared planning-family metadata from `commands/bgsd-plan.md` instead of hard-coding separate validator expectations.
Output: a shared planning-family metadata loader plus regression coverage for canonical route parsing, shorthand rejection, and legacy wrapper suggestions.
</objective>

> **TDD Decision:** Selected — planning-family parsing and shorthand enforcement are deterministic command behaviors, so RED tests should lock the canonical route matrix and failure cases before the shared parser lands.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/SKILL.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/tdd-reference.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/175-canonical-command-surface-alignment/175-CONTEXT.md
@.planning/phases/175-canonical-command-surface-alignment/175-RESEARCH.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-08-SUMMARY.md
@commands/bgsd-plan.md
@src/lib/commandDiscovery.js
@tests/validate-commands.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED regressions for canonical planning-family parsing and shorthand rejection</name>
  <files>
    tests/validate-commands.test.cjs
  </files>
  <action>
  Add focused failing regressions that make the Phase 175 contract explicit before any parser refactor lands.

  Cover at least these cases: the planning-family route matrix can be read from `commands/bgsd-plan.md`; `/bgsd-plan 175` and other runnable shorthand forms fail as unsupported surfaced guidance; and removed planning wrappers such as the legacy alias `/bgsd-plan-phase [phase]` still produce canonical `/bgsd-plan ...` suggestions without relying on scattered hand-maintained route knowledge.
  </action>
  <verify>node --test tests/validate-commands.test.cjs</verify>
  <done>The targeted regressions fail because planning-family grammar and shorthand enforcement are still hand-maintained or incomplete.</done>
</task>

<task type="auto">
  <name>Task 2: Extract shared planning-family metadata and wire command discovery to it</name>
  <files>
    commands/bgsd-plan.md
    src/lib/planning-command-surface.js
    src/lib/commandDiscovery.js
  </files>
  <action>
  Introduce a focused shared loader that parses the canonical planning-family route matrix from `commands/bgsd-plan.md`, then use that metadata inside `src/lib/commandDiscovery.js` for supported `/bgsd-plan` sub-actions, required operands, and legacy planning-wrapper suggestions.

  Keep the routing contract strict: explicit sub-action first, workflow-owned operand passthrough preserved, and no intent guessing from bare operands. Do not broaden this into unrelated slash-command discovery or a general router rewrite.
  </action>
  <verify>npm run test:file -- tests/validate-commands.test.cjs</verify>
  <done>The planning-family validator rules are sourced from shared canonical metadata, shorthand is rejected deterministically, and the focused regressions pass.</done>
</task>

</tasks>

<feature>
  <name>Canonical planning-family metadata extraction</name>
  <files>
    commands/bgsd-plan.md
    src/lib/planning-command-surface.js
    src/lib/commandDiscovery.js
    tests/validate-commands.test.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/validate-commands.test.cjs</red>
    <green>npm run test:file -- tests/validate-commands.test.cjs</green>
    <refactor>npm run test:file -- tests/validate-commands.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    The canonical planning-family route matrix is parsed from `commands/bgsd-plan.md` and reused for command-integrity enforcement.

    Cases:
      - `commands/bgsd-plan.md` route matrix -> shared metadata for `phase`, `discuss`, `research`, `assumptions`, `roadmap`, `gaps`, and `todo`
      - `/bgsd-plan 175` -> rejected as ambiguous surfaced guidance
      - legacy alias `bgsd-plan-phase [phase]` -> classified as legacy guidance with canonical `/bgsd-plan phase [phase]` suggestion
  </behavior>
  <implementation>
    Add one narrow planning-family parser module and move the planning-specific validation rules in `commandDiscovery` onto that shared metadata instead of preserving another hard-coded route table.
  </implementation>
</feature>

<verification>
node bin/bgsd-tools.cjs util:validate-commands --raw
</verification>

<success_criteria>
- [ ] Planning-family grammar is derived from `commands/bgsd-plan.md` in shared source code
- [ ] Ambiguous `/bgsd-plan` shorthand fails validation instead of being inferred
- [ ] Legacy planning wrappers stay suggestion-only validation metadata with canonical replacements from the shared matrix
</success_criteria>

<output>
After completion, create `.planning/phases/175-canonical-command-surface-alignment/175-01-SUMMARY.md`
</output>
