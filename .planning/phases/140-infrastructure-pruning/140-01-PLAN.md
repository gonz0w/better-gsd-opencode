---
phase: 140-infrastructure-pruning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/plugin/command-enricher.js
  - src/lib/decision-rules.js
  - tests/enricher-decisions.test.cjs
  - tests/decisions.test.cjs
  - tests/tool-routing-contract.test.cjs
  - tests/cli-tools-integration.test.cjs
  - bin/bgsd-tools.cjs
autonomous: true
requirements:
  - PRUNE-01
  - PRUNE-02

must_haves:
  truths:
    - handoff_tool_context enrichment field retains only capability_level (the sole confirmed consumer field) — available_tools and tool_count are gone from the object
    - Three orphaned decision rules (agent-capability-level, json-transform-mode, phase-dependencies) no longer exist in DECISION_REGISTRY or in module.exports
    - npm test passes with zero failures after all removals and test updates
    - No workflow or agent .md file references any of the three removed decision rule IDs
    - Contract test isConsumer() checks for decisions.{rule-id} pattern specifically, not tool_availability as a blanket match
  artifacts:
    - src/plugin/command-enricher.js (handoff_tool_context stripped to capability_level only)
    - src/lib/decision-rules.js (3 registry entries + 3 resolve functions removed)
    - tests/enricher-decisions.test.cjs (available_tools and tool_count shape tests removed)
    - tests/decisions.test.cjs (all tests for 3 removed rules removed, registry count updated)
    - tests/tool-routing-contract.test.cjs (isConsumer fixed, hardcoded rule assertions updated)
    - tests/cli-tools-integration.test.cjs (tests for removed rules removed, chain B count updated)
    - bin/bgsd-tools.cjs (rebuilt from updated src/)
  key_links:
    - execute-phase.md and map-codebase.md still reference handoff_tool_context.capability_level (not broken)
    - execute-plan.md still references decisions.file-discovery-mode and decisions.search-mode (not broken)
    - DECISION_REGISTRY still contains file-discovery-mode, search-mode (2 remaining Chain B tool-routing rules)
---

<objective>
Prune unused tool infrastructure: simplify handoff_tool_context to its only consumed field
(capability_level), remove three orphaned decision rules with zero workflow consumers
(agent-capability-level, json-transform-mode, phase-dependencies), update all affected tests,
rebuild the CLI, and verify the full test suite passes.

Purpose: Satisfy DO-104 — dead-weight infrastructure removed. The pruning completes v14.1 by
confirming every remaining tool infrastructure path has a verified consumer.

Output: Simplified enricher output, slimmer decision registry (19 rules), updated test files,
rebuilt bin/bgsd-tools.cjs.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-infrastructure-pruning/140-CONTEXT.md

# Key source files
@src/plugin/command-enricher.js
@src/lib/decision-rules.js

# Test files being modified
@tests/enricher-decisions.test.cjs
@tests/decisions.test.cjs
@tests/tool-routing-contract.test.cjs
@tests/cli-tools-integration.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify handoff_tool_context to capability_level only (PRUNE-01)</name>
  <files>
    src/plugin/command-enricher.js
    tests/enricher-decisions.test.cjs
  </files>
  <action>
    **In src/plugin/command-enricher.js (lines 529–549):**

    The current enrichment block (Phase 128 comment) builds handoff_tool_context with three
    sub-fields: available_tools, tool_count, capability_level. Only capability_level has real
    consumers (execute-phase.md:31,117,148 and map-codebase.md:14,47). The available_tools
    and tool_count sub-fields have zero runtime consumers.

    Simplify the block to:
    - Remove the availableTools array construction (lines 532–534)
    - Remove the toolCount variable (line 535)
    - Keep only the capabilityLevel computation (lines 537–540, which already avoids the
      circular dep note)
    - Set enrichment.handoff_tool_context = { capability_level: capabilityLevel }
    - Update the fallback catch clause to: { capability_level: 'LOW' }

    The comment on line 543 references available_tools purpose — remove that stale comment.

    **In tests/enricher-decisions.test.cjs (lines 749–832):**

    The 'handoff_tool_context enricher tests' describe block tests the full 3-field shape.
    Remove or update tests that reference the removed sub-fields:
    - Remove the 'available_tools is an array of strings' test (lines 771–778)
    - Remove the 'tool_count matches available_tools length' test (lines 780–787)
    - Remove the 'available_tools contains only tool names from the 6 known tools' test
      (lines 810–816)
    - Remove the 'tool names are strings, not objects' test (lines 818–824)
    - Update 'handoff_tool_context is an object' (line 765) — keep, it still validates
      that the field is a non-null object
    - Update 'handoff_tool_context defaults gracefully when tool_availability absent'
      (lines 826–831) — remove assertions for available_tools and tool_count, keep only
      assert for capability_level === 'LOW'
    - Keep 'capability_level is HIGH/MEDIUM/LOW string' test (lines 789–794)
    - Keep 'capability_level matches tool count thresholds' test (lines 796–808)
    - Update the buildEnrichment helper in the test suite (lines 751–763) to only return
      { handoff_tool_context: { capability_level: level } } — drop available_tools and
      tool_count from both the helper return value and the enrichment object

    Grep confirmation before editing: verify no .md file outside tests/ references
    `available_tools` or `tool_count` as a workflow consumer (expected: none).
  </action>
  <verify>
    Run: node bin/bgsd-tools.cjs decisions:list
    Confirm handoff_tool_context shape in enricher output: capability_level only.

    Run failing tests for the modified file only:
    node --test tests/enricher-decisions.test.cjs 2>&1 | tail -20
    Expected: zero failures in the handoff_tool_context describe block.
  </verify>
  <done>
    - enrichment.handoff_tool_context contains only { capability_level: 'HIGH'|'MEDIUM'|'LOW' }
    - No reference to available_tools or tool_count in the enricher block
    - Tests for available_tools/tool_count sub-fields are gone from enricher-decisions.test.cjs
    - Remaining handoff_tool_context tests pass (capability_level shape, thresholds, defaults)
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove orphaned decision rules and update all affected tests (PRUNE-02)</name>
  <files>
    src/lib/decision-rules.js
    tests/decisions.test.cjs
    tests/tool-routing-contract.test.cjs
    tests/cli-tools-integration.test.cjs
  </files>
  <action>
    **BEFORE EDITING — grep confirmation:**
    Confirm zero workflow/agent .md consumers for each rule ID:
      grep -r "decisions\.agent-capability-level" workflows/ agents/  (expect: 0 matches)
      grep -r "decisions\.json-transform-mode" workflows/ agents/     (expect: 0 matches)
      grep -r "decisions\.phase-dependencies" workflows/ agents/      (expect: 0 matches)

    **In src/lib/decision-rules.js:**

    1. Remove the 'json-transform-mode' registry entry (lines ~671–679, 3rd tool-routing entry)
    2. Remove the 'agent-capability-level' registry entry (lines ~682–691)
    3. Remove the 'phase-dependencies' registry entry (lines ~692–701)
    4. Remove the resolveJsonTransformMode function (~lines 763–778)
    5. Remove the resolveAgentCapabilityLevel function (~lines 790–811)
    6. Remove the resolvePhaseDependencies function and its section comment (~lines 813–961,
       which includes the full topological sort implementation)
    7. Remove from module.exports (~lines 989–992):
       - resolveJsonTransformMode
       - resolveAgentCapabilityLevel
       - resolvePhaseDependencies
    8. Remove the Phase 128 section comment blocks (lines ~681 and ~781) that introduced
       these now-deleted functions

    After removal: DECISION_REGISTRY should have 19 entries (was 22).
    Chain B rules with tool_availability input: only file-discovery-mode and search-mode (2).

    **In tests/decisions.test.cjs:**

    1. Remove the import of resolveJsonTransformMode, resolveAgentCapabilityLevel,
       resolvePhaseDependencies from the require('../src/lib/decision-rules') destructure
       (lines ~28–35)
    2. Update 'registry has >= 17 entries' assertion (line ~51) — no change needed (19 >= 17 passes)
    3. Update 'all three tool-routing rules appear in DECISION_REGISTRY' (lines ~1087–1093):
       - Change to 'two remaining tool-routing rules appear in DECISION_REGISTRY'
       - Change assertion: toolRoutingRules.length === 2 (was 3)
       - Remove the assert for json-transform-mode
    4. Remove the entire 'resolveJsonTransformMode' describe block (~lines 1015–1082)
    5. Remove the 'evaluateDecisions returns results for all three rules' test that asserts
       json-transform-mode fires (~line 1118–1131)
    6. Update the 'evaluateDecisions includes file-discovery-mode' test (~line 1109) — keep as-is
    7. Remove the entire Phase 128 top-level describe block (~lines 1135–1493) which covers:
       - resolveAgentCapabilityLevel describe block
       - resolvePhaseDependencies describe block
       - DECISION_REGISTRY integration (Phase 128) block
       (All tests inside this block test the removed functions)

    **In tests/tool-routing-contract.test.cjs:**

    1. Fix isConsumer() function (lines 61–71) to NOT use tool_availability as a blanket match.
       Replace the current implementation with:
       ```js
       function isConsumer(content, ruleId) {
         const escapedId = ruleId.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
         const decisionPattern = new RegExp('decisions\\.' + escapedId);
         return decisionPattern.test(content);
       }
       ```
       Remove the tool_availability blanket match (lines 67–68) entirely.

    2. Update the 'Dynamic rule identification' test (lines 83–104):
       - Change `>= 4` to `>= 2` (only file-discovery-mode and search-mode remain)
       - Remove the assertions for json-transform-mode (line 100–103) and
         agent-capability-level (lines 102–103)
       - Keep assertions for file-discovery-mode and search-mode

    3. Remove the per-rule tests for json-transform-mode (lines 134–142) and
       agent-capability-level (lines 144–152) from the per-rule describe block.

    4. Update the 'Zero orphans' test (lines 154–171) — no change needed structurally.
       With the fixed isConsumer(), the zero-orphan assertion now correctly checks only
       decisions.{rule-id} pattern, and the remaining 2 rules (file-discovery-mode, search-mode)
       both appear in execute-plan.md via decisions.file-discovery-mode and decisions.search-mode.

    5. Remove the 'Consumer location: agent-capability-level consumed by execute-phase.md'
       test (lines 191–199) — this relied on capability_level pattern which is now
       handoff_tool_context, not a decision rule consumer.

    **In tests/cli-tools-integration.test.cjs:**

    1. Remove the json-transform-mode test block (lines ~1073–1105, 3 tests)
    2. Remove the agent-capability-level test block (lines ~1107–1135, 3 tests)
    3. Update the 'decisions:list filters to exactly 4 Chain B rules' test (lines ~1137–1160):
       - Change title to 'decisions:list filters Chain B rules by tool_availability input'
       - Change `>= 4` to `>= 2`
       - Remove assertions for json-transform-mode (line 1155) and agent-capability-level (line 1156)
       - Keep assertions for file-discovery-mode and search-mode
  </action>
  <verify>
    Grep that removed rule IDs no longer appear in src/:
      grep -r "agent-capability-level\|json-transform-mode\|phase-dependencies" src/lib/decision-rules.js
    Expected: zero matches.

    Verify registry count:
      node -e "const {DECISION_REGISTRY} = require('./src/lib/decision-rules'); console.log(DECISION_REGISTRY.length)"
    Expected: 19

    Run contract test file:
      node --test tests/tool-routing-contract.test.cjs 2>&1 | tail -20
    Expected: all tests pass.

    Run decisions test file:
      node --test tests/decisions.test.cjs 2>&1 | tail -20
    Expected: all tests pass.
  </verify>
  <done>
    - src/lib/decision-rules.js has 19 entries in DECISION_REGISTRY (3 removed)
    - The 3 removed resolve functions are gone from source and exports
    - tests/tool-routing-contract.test.cjs isConsumer() uses decisions.{id} pattern only
    - All four test files compile and have zero failures for their rule-related tests
  </done>
</task>

<task type="auto">
  <name>Task 3: Rebuild CLI and run full test suite</name>
  <files>
    bin/bgsd-tools.cjs
  </files>
  <action>
    Rebuild the CLI bundle from updated source files:
      npm run build

    Verify the built bundle no longer exports the removed rules by checking the bundle:
      grep -c "resolveAgentCapabilityLevel\|resolveJsonTransformMode\|resolvePhaseDependencies" bin/bgsd-tools.cjs
    Expected: 0 matches.

    Run the full test suite:
      npm test

    If any test fails that is NOT one of the directly edited test files, investigate and fix.
    The success bar: ALL tests pass, zero failures.
  </action>
  <verify>
    npm test exits 0 with all tests passing.
    grep confirms removed symbols are absent from bin/bgsd-tools.cjs.
  </verify>
  <done>
    - npm run build succeeds
    - npm test passes with zero failures
    - bin/bgsd-tools.cjs does not contain the 3 removed rule symbols
    - Final test count is lower than before (expected: ~30 fewer tests from removed rule tests)
  </done>
</task>

</tasks>

<verification>
1. Grep workflows/ and agents/ for the 3 removed rule IDs — zero matches confirms no silent consumers
2. DECISION_REGISTRY.length === 19 (was 22, removed: agent-capability-level, json-transform-mode, phase-dependencies)
3. handoff_tool_context in enricher output contains only { capability_level }
4. execute-phase.md and map-codebase.md still reference capability_level (not broken — they read from handoff_tool_context.capability_level, not from decisions.agent-capability-level)
5. execute-plan.md still references decisions.file-discovery-mode and decisions.search-mode (not broken)
6. npm test passes — zero failures
</verification>

<success_criteria>
1. handoff_tool_context enrichment field simplified to { capability_level } only — its only confirmed consumers (execute-phase.md, map-codebase.md) continue to work unchanged (PRUNE-01)
2. Three orphaned decision rules removed from registry, resolve functions deleted, exports cleaned (PRUNE-02)
3. Contract test isConsumer() is accurate — checks decisions.{rule-id} pattern only, not tool_availability as blanket match
4. All existing tests pass after pruning — npm test exits 0 with zero failures (SC-3)
5. bin/bgsd-tools.cjs rebuilt and does not contain removed symbols
</success_criteria>

<output>
After completion, create `.planning/phases/140-infrastructure-pruning/140-01-SUMMARY.md`
</output>
